# Project memory — 灰の遠征

更新日: 2026-08-31（UTC）

## 目的

作者自身が、内容を理解した後も iPhone で繰り返し遊びたいゲームを作る。選択の結果が見え、次の一手を自分から試したくなることが成功条件です。

## 現在の設計判断

- 仲間を長期的な愛着の主語にする。
- 戦闘前に隊列、技能、装備、敵の狙いを組む。戦闘中は自動で進み、リプレイで因果を確認する。
- 複数の小さな rule が、共有イベント、資源、状態を介して連鎖する構造を採用する。
- 既存の語彙で content を増やし、engine へ個別人物や個別技能の分岐を足さない。
- 遠征内の有限資源と、Profile に残る長期資源を分離する。\n- R10では、New Game / Continue / Load Game を分け、旧セーブ互換を切る。オートセーブと手動セーブ枠を別に持つ。
- 初期4 Stage は本編の完成形ではなく、5人とゲームの文法を覚えるチュートリアルとして扱う（R9）。
- 生成装備は「意図して選ぶ主構築」ではなく、「現在の構成を壊して再評価させる副構築」に置く（R8 §3.5）。

## 現行実装

- 本編は ecology/ の EXP-18 R8 Campaign + R9 チュートリアル構成。
- Campaign Stage 0〜3 は 2人 → 5人へ、Stage ごとに1人ずつ加入する。
- pack は累積する（E → E+W → E+W+T → E+W+T+C）。新しい pack はその Stage では入口だけ、次の Stage から全体が出る。
- Stage 0 の初回だけ、本当に負ける配置で始まる一戦（灰の門）があり、一手変えると勝てる。会話はいつでも飛ばせ、一度クリアした Stage では出ない。
- 会話は立ち絵つきの一行送り（AUTO・履歴・スキップ）で見せる。立ち絵は画像ファイルではなく SVG を組み立てる純関数で、表情は同じ骨格の parameter 差として持つ。**行数と文言は増やしていない**（R9 §7 の「説明文にしない」を保つ）。
- 一度クリアした Stage を遊び直すときは、5人を自由に選べる。
- 行動3、反応3、常設2、装備2枠を各人物へ割り当てる。
- 1遠征は3幕12戦。4、8、12戦目がボス。敵の数と threat budget は遠征の人数に合わせる。
- HP は通常戦・精鋭戦の間で持ち越し、4・8戦目のボス後に全回復する。遠征内の補給は有限です。
- 次の戦闘は敵の狙い、法則、変異、脅威の概要を exact preview で確認できる。
- 報酬の装備2枠のうち一つは手続き生成装備で、rule は最初から全部読める。
- 遠征終了時に、見つけた生成装備が Blueprint として残る（勝利2・安全撤退2・敗北1）。次の遠征へ持ち込める枠は初期1、最大5。
- Profile、Run、Battle を分けて保存し、終了時に D1 へ記録できる。ローカルにはR10の新しい保存形式でProfile・Run・物語フラグ・Blueprintを一体保存し、旧saveは移行しない。
- ルールエンジンは決定的で、同じ入力と seed から同じイベント列を再生する。

## 現在の評価

実装と自動検査は進んでいますが、作者は現行版をまだ通しで評価していません。したがって、面白さ、難しさ、再プレイ欲、長期経済について支持判定は出していません。

R8 §18 は Phase C（生成装備）を Gate 2（作者による Stage 0〜3 評価）の後に置いていました。作者の指示で先行実装しています。順序を守っていないことは R8_IMPLEMENTATION_PHASE4_STATUS.md に記録しました。

## 既知の未実装・保留

- Stage 0〜3 の敵系統、act boss 法則、mutation は現時点で共通の暫定定義を使う。
- Stage-specific law と stage ごとの敵族は未確定。人数に合わせた敵の切り詰めは比例で割っただけの未調律値。
- 横断pack `pack_relay`（余波と受け渡し）は試作で、Campaign には入れていない。Free / Endless からだけ引ける。
- `pack_barrage` は Stage 3 が C（手当てと余剰）になったため、Campaign から外れて Free / Endless に残っている。
- affix family の購入（R8 §3.7）は未実装。pool はその遠征の pack から決まる。
- Campaign Stage 4 以降と Endless は未実装。
- 装備耐久は現状、戦闘ごとにリセットされる。
- 野営治療の対象は現状自動選択で、手動の細かな選択にはしていない。
- Free mode は旧仕様として残るが、旧 save migration は廃止した。本編の新しい保存形式では旧セーブを読まない。

## R11 — 人物・数値・ラダーの作り直し

作者との対話で本編5人を新規に設計し直し、数値系の穴を塞ぎ、Stage ラダーを組み替えた。

「5人のうち3人が守り役では多すぎる」という指摘を測ったところ、配役ではなく
**数値系が守りに2軸（防壁 `focus`・軽減 `guard`）、攻めに1軸（`might`）しか
持っていなかった**ことが原因だった。R6 §4.4 は「支援役にも technique 攻撃を付けられる」と
宣言していたが、実装は片側だけだった。**`focus` を「技の威力」へ格上げして、宣言のとおりに
実装した。**

さらに「腕力の技と集中の技の違いが分からない」という指摘に対し、名前や属性ではなく
**位置の規則**で答えた。**腕力で伸びる攻撃は後列から出すと 40% まで落ち、集中で伸びる
攻撃は落ちない。**engine への追加はこの一点（`effects.mjs` の `afterRearFalloff`）だけで、
人物固有の分岐は足していない。これで人物の設計がそのまま隊列の設計になり、カイは
「前で落ちるか、後ろで火力を捨てるか」の二択を背負う。

その二択を軸に加入順を組み替えた。**Stage 1 でカイが問題を持ち込み、Stage 2 でスミが
それを解く。問題を出してから、その解決を渡す。**最初の2人は主人公シキと回復役ナズナ。

序盤の一戦は、**片方が倒れた拍で表示を打ち切り、時間を巻き戻して同じ盤面を戦い直す**
演出にした。engine は最後まで走らせて本当の敗北を出している。この一戦の4通りの配置で、
武器と技の違いの両側が出る。

**`ecology-campaign-smoke` は削除した。**隊列を一度も変えず、装備の配り先も
技能点の使い方も選ばない状態で「Stage 2 が12戦通せない」と言っていたが、
隊列は R11 で中心のメカニクスになったので、測っている内容自体が smoke として
成立していなかった。閾値を下げて通す代わりに検査そのものを外し、
Stage 0〜3 の build 到達可否は作者が遊んで判断する。

詳細・実装記録・**まだ足りない部分**は
analysis/experiments/exp-18/R11_CHARACTER_DESIGN_FIVE.md。**§9 が引き継ぎの本体**で、
立ち絵の画像化、回復の集中連動（作者案の「戦闘終了時に一度だけ回復・常時AP-1」）、
根城の場面、読める設定、未測定の軸をそこに並べてある。

**面白さの判定は出ていない。**通っているのは機械検査と与ダメージ測定だけである。

## 次の一手

作者が現行 Campaign Stage 0〜3 を1〜2遠征遊び、次を記録する。

- 最初の2人の会話と、負ける一戦で、人物と戦闘への関心が生まれるか。
- 新しい仲間が加入したとき、何が得意かを説明できるか。
- 新しい pack を一文で説明できるか。最低2通りの使い方を試したくなるか。
- 自分の編成・配置・技能変更が、どの結果を変えたか一文で説明できるか。
- 生成装備を拾ったとき、いまの構成を組み替えたくなるか。残したい品があるか。
- 遠征後にもう一度開きたい理由があるか。
- 説明不足、判断過多、消耗、テンポのどこでつまずいたか。

その記録をもとに、content と表示を調整するか、Stage 4 以降へ進むかを決める。
