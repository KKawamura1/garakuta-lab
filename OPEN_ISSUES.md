# 残論点 — 次の実装でだけ気をつけること

**片付いた項目はその場で削除してください。**ここは履歴ではなく作業キューです。
恒久的なルールは AGENTS.md、仕様と経緯は `docs/` にあります。

---


## 1. 直しどころが分かっている不具合

### 1.1 能力値 `focus` の表示名が「集中力」のままで、状態異常の「集中」と衝突している

**別 PR で対応すると決めた項目です（作者判断・R13）。**

設計文書と `characters.mjs` のコメントは能力値を「**技術**」と呼んでいますが、
**プレイヤーが読む文字列は全部「集中力」のまま**です。

- `ecology/content/skill-tree.mjs` の技能説明（「最も弱った敵へ集中力125%」など）
- `ecology/content/story.mjs` の footer（「集中で通す技は落ちない」）
- `ecology/app.js` の巻き戻し後の説明文

**単純な一括置換では壊れます。**`集中` は**状態異常の名前としても使われている**からです
（`steady_aim`「自分に『集中』を1つ付ける」、`block_focus`「受け返しの集中」、
passive の `集中力`）。改名すればこの重複が解消されて読みやすくなるはずですが、
能力値と状態と技能名を切り分けてから触る必要があります。

## 2. 賭けの副作用を見る（R14 の変更由来）

- **技能の取り直し不可が、遠征中盤で「配られた技能点の使い道が無い」を作らないか。**
  作るなら、まず報酬側（技能点の配り方）を疑ってください。技能を戻せるようにするのは
  最後の手段です（戻せると予測が完全な以上、賭けではなく最適解探しになる）。
- **予測が常に見えることで、戦闘の再生を見なくなっていないか。**予測は数字だけで因果を
  出しません。再生を見る理由が残っているかは作者のプレイでしか分かりません。もし
  見なくなっているなら、対策は予測の制限ではなく、変更前後の chain 差分の表示です。
- 予測帯は5人ぶんで 390px 幅にちょうど収まります。**Stage 4 以降で6人以上になると
  横スクロールになります。**


## 3. 未実装（作者評価の後で判断する）

- Campaign Stage 4 以降と Endless。
- stage 固有の敵法則、affix family の購入。
- `pack_barrage`（連撃と刻印）と `pack_relay`（余波と受け渡し）は content として存在
  しますが Campaign には入っておらず、**いまはどこからも引けません。**R16 で
  barrage へ5本、relay へ6本足したので、**引けない技能は 34件**になりました
  （`analysis/ecology-skill-catalog-smoke.mjs` が「最終 Stage から引けるのは○件」を
  毎回印字します。節123件のうち89件）。内訳は barrage 12・relay 12・
  **pack_tempo の full 10**です。
- 装備耐久は戦闘ごとにリセットされます。
- 野営治療の対象は自動選択で、手動の細かな選択にしていません。
- 回復が集中（`focus`）に連動していません。anti-stall を壊さないため意図的に触って
  いません。作者案「戦闘終了時に一度だけ集中に応じた回復・常時 AP-1」は、常設 passive
  として作れば抵触しない見込みですが**未実装・未検証**です。触るなら
  `analysis/ecology-anti-stall-audit.mjs` を通すところまで確認してください。
- **`pack_tempo` の full が Stage 4 の不在で塞がっています。**tempo は Stage 3（最終）で
  core として入るので、full 側（`prep_spiral`・`call_the_slow`・`feint`・`set_the_pace`・
  `counter_order`・`stall_the_blow`・`first_order` など10件）は一度も出ません。
  R16 以前から `prep_spiral` が同じ状態でした。Stage 4 を作るか、tempo の core を
  10件の上限まで組み替えるかの判断が要ります（story.test.mjs が core 7〜10 を押します）。
- 旧 difficulty rank の欄（`newRun` の `difficulty`、`composeEncounter` の rank 引数）は
  到達不能なまま残っています。Stage 4 以降で難度を動かすなら、ここを再利用するか
  Stage 側の係数で持つかを決めることになります。

## 4. 表示・読み物で判断していないこと

- **立ち絵は SVG のままです。**画像化は `portraits.mjs` 冒頭の3つの前提（決定性・
  ネットワーク往復ゼロ・表情を数値差で持つ）を崩すので、人物と会話が固まったあとに
  着手してください。まずヒバナの `neutral` と `smile` の2枚だけ出して、同一人物の
  表情で顔が揃うかを見るのが安全です。
- 根城の画面が長い（5人ぶんの名簿を全部開くと 390px 幅で9画面ぶん近い）。畳むべきかは
  判断していません。
- 根城の日常の場面は6本、家にあるものは9件で、**Stage 3 まで進めると全部見終わります。**
  Stage 4 以降を作るなら一緒に増える前提です。
- 幕の断片を act boss の直前に置いたので、12戦のテンポを切る可能性があります。
- 名簿・図鑑・根城は戦闘に影響しません。素通りされるかどうかは遊んでもらうしかありません。

## 5. 会話（R15 で Stage 0 だけ直した。残りは未着手）

Stage 0 は書き直しました（`docs/HISTORY.md` §3.8）。**以下は同じ読み合わせで見つかった
未処理分です。**深いほうの話は「五人が揃ってから」という方針（`dialogue.mjs` 冒頭）に
沿って置き直すことになります。

- **Stage 1 が同じ場面を3回やっている。**`join`「えっ。あの、わたしですか」→
  `act1`「えっ。あの、わたし、後ろのほうが」→ `act3`「なんで、毎回わたしなんでしょうか」。
  出だしの文字列までほぼ同じで、どれも「ナギが渋る → 二人が説得 → 折れる」です。
  act1 を「もう受け役として動いている前提」の場面に振り替えると重複が消えます。
- **兄の助走がどこにも無い。**R15 で Stage 0 から外したぶん、`stage_3_act2` の
  「お兄様は、ご存命ですよ」がいきなり来ます。**Stage 1〜3 のどこかに一拍だけ置く**
  必要があります（Stage 0 へ戻すのは、R15 の判断に反するので採りません）。
- **ツグミの望みが台詞に一度も出ていない。**47行で最多なのに、「自分に試していて
  記憶剥離が異常に速い」が一行も無い。他人の数値は測るのに自分の数字だけ言わない、
  という形が使えます。**根城の場面向け**です（深いので五人が揃ってから）。
- **ゲンゾウの枠が足りない。**16行、うち12行が `calm`。「甘いものに目がない」
  「若者言葉を覚えようとして必ず失敗する」が1行も出ていません。Stage 3 加入なので
  構造的に出番が少なく、**根城の場面を1本足すのが一番安い**です。
- **ゴウの「暗いところが苦手」が、坑を舞台にして一度も出てこない。**
- 台詞149行のうち40行（27%）が「……」で始まります。ナギは19行中9行。
  「間」の演出が一種類しかないので、5人が同じ呼吸で喋って見える瞬間があります。
  ゴウの「……」は R15 で意図的に足したもの（折れる直前の一拍）なので、減らすなら
  他の三人から先に見てください。

## 6. 検査の穴

- 画面の通し（`ecology-tutorial-trial.mjs` / `ecology-trial.mjs`）は `check-all.sh` に
  入っていません。画面に触ったら手で走らせてください。

## 7. R16（技能54本の追加）で残したもの

**数値は一つも遊んで確かめていません。**係数・段数・コスト・前提・tier はすべて
机上です。作者が遊んで動かす前提で置いてあります（AGENTS.md の soft data）。
特に見てほしいところ:

- **技能ツリーの画面が長くなりました。**節が 69件 → **123件**です。攻撃系統だけで
  30件を超えます。390px 幅では系統ごとにかなりの縦スクロールになります。
  畳むか、系統を増やすか（`app.js` の `SKILL_TREE_BRANCHES` は現在5つ固定）、
  tier で段を切るかは**判断していません。**
- **一遠征の技能点は開始2点＋報酬です。**技能が倍になっても取れる数は変わらないので、
  増えたのは「選択肢」であって「強さ」ではないはずですが、**未検証です。**
  もし「毎回同じ数本しか取らない」なら、それは技能の差が読めていない信号です。
- **状態を3つ足しました**（怯み・守勢・裂傷）。画面の状態表示は汎用なので出ますが、
  **5種類が同時に乗ったときに読めるか**は遊んでもらわないと分かりません。
- **守勢（warded）は防壁・受け構えと役割が近い**ので、三つ巴が実際に成立しているか
  （多段には守勢、大技には受け構え、総量には防壁）を見てください。成立していなければ、
  減量を固定8から段数比例や割合へ変える余地があります。
- 反応技能が 28件 → **47件**になりました。反応枠は3〜4なので選択は絞られますが、
  **反応権（RP）の取り合い**が起きます。RP2の人物が「衝撃を殺す」「揺れない手」
  「痛みを分ける」を同時に持つと、実際に発火するのは1つです。これは意図した緊張ですが、
  **「持っているのに一度も出ない」が続くなら、説明文が嘘に見えます。**
- 極端な編成（5人全員が反応4枠を埋め、敵5体）で戦闘イベント上限（4096）に当たる場合が
  あります。**R16 前と同じ頻度**（同条件の乱択800戦で 25件 / 25件）なので R16 由来では
  ありませんが、当たると戦闘そのものが例外で落ちます。上限を上げるか、chain の
  枝刈りを入れるかは未判断です。
- `analysis/ecology-tutorial-trial.mjs` が 9回に1回ほど selector の待ちで落ちます
  （8秒の `waitForSelector`）。R16 の前後どちらでも再現するので content 由来ではなく
  **実行環境の揺れ**と見ていますが、CI で赤くなったらまず再実行してください。
