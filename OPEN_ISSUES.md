# 残論点 — 次の実装でだけ気をつけること

**片付いた項目はその場で削除してください。**ここは履歴ではなく作業キューです。
恒久的なルールは AGENTS.md、仕様と経緯は `docs/` にあります。

---


## 1. 直しどころが分かっている不具合

### 1.1 巻き戻したあとの初期隊列が、すでに勝てる配置になっている

灰の門で負けたあと camp が作る隊列は `defaultFormation`（ゴウ front_left・ツグミ
rear_right）で、**負けた配置（ツグミ front_left）が引き継がれません。**つまり
プレイヤーは何も直さずに勝ててしまい、「一手直して勝つ」という導入の体験が成立して
いません。最小の手は、巻き戻し時に `run.formation` を `PROLOGUE.formation` へ
合わせることです。

### 1.2 能力値 `focus` の表示名が「集中力」のままで、状態異常の「集中」と衝突している

**別 PR で対応すると決めた項目です（作者判断・R13）。**

設計文書と `characters.mjs` のコメントは能力値を「**技術**」と呼んでいますが、
**プレイヤーが読む文字列は全部「集中力」のまま**です。

- `ecology/content/skill-tree.mjs` の技能説明（「最も弱った敵へ集中力125%」など）
- `ecology/content/story.mjs` の footer（「集中で通す技は落ちない」）
- `ecology/app.js` の巻き戻し後の説明文

**単純な一括置換では壊れます。**`集中` は**状態異常の名前としても使われている**からです
（`steady_aim`「自分に『集中』を1つ付ける」、`block_focus`「受け返しの集中」、
passive の `集中力`）。改名すればこの重複が解消されて読みやすくなるはずですが、
能力値と状態と技能名を切り分けてから触る必要があります。

## 2. リブートで決めたが、まだ実装していない system

`analysis/CAST_REBOOT_TWELVE_TOMORROWS.md` で設計を決めた項目のうち、**コードが
まだ追いついていないもの**です。人物・会話・数値・世界観の文言は入れ替え済みです。
（`focus` の表示名は §1.2 に別項があります。）

### 2.1 位置を使う技能がほとんど無い ★最大

**ヒバナ（`guardian`・役割「遊撃」）は AP2 を持っていますが、使い道がありません。**
`content/skills-active.mjs` に位置を動かす能動技能が一つも無く、`swap_positions` /
`actor_moved` を読んでいるのは `affixes.mjs` と `skills-reactive.mjs` だけです。

移動は `effects.mjs:578` の `swap_positions` なので**必ず誰かと入れ替わり**、ヒバナは
HP120・受け3 の紙で、前列は `sort: position_asc` により先に狙われます。
**明確な利得先を先に用意しないと、誰も使いません。**設計 §12.5 に三つ置いてあります。

- 撃つと前へ出るツグミを、ヒバナが引き戻す
- 敵を寄せてから行／列単位で薙ぐ
- `actor_moved` を読む reactive（`stride_into_reach` が実例）

### 2.2 speed がまだ生きている

`engine.mjs:551` は**いまも speed の降順で行動順を決めています**。`CHARACTER_STATS`
にも speed が残っています。設計 §12.3 は speed を廃止し、**行動順を隊列
（前列→後列、同列は左→中→右）から決める**としています。

speed は round 内の並び替えキーにしか使われていないので、**相手より1速いかどうかだけが
効く閾値**になっています。廃止すると、隊列という既存の決定に「行動順」という意味が
一つ増えます（ノブは増えません）。

### 2.3 防壁が能力値依存の active のまま

`bulwark` は `content/skills-active.mjs` にあり、`skills-active.mjs:112` で
`{ stat: "focus" }` を読んでいます。設計 §3.4 は、

- 付与量を**能力値非依存の固定値**にする（治療と役割が被るため）
- **active から reactive へ移す**（AP が余っている人が張るのが最適になり、
  ヒバナが性格に合わない支援役へ引っ張られるため）

としています。**外すだけでは足りず、置き場所も変える必要があります。**

### 2.4 pack の解禁順が加入順と噛み合っていない

`content/campaign-stages.mjs` は `pack_care → pack_edge → pack_wall → pack_tempo` の
ままですが、**加入順は入れ替わっています。**

| Stage | 加入する人物 | 役割 | その Stage の新 pack |
|---|---|---|---|
| 1 | ナギ（`lancer`） | 守護・受け | `pack_edge`（溜めと大打撃） |
| 2 | ヒバナ（`guardian`） | 遊撃・位置 | `pack_wall`（庇護） |
| 3 | ゲンゾウ（`tactician`） | 後詰・割り込み | `pack_tempo`（順番） |

**新しく加入した人物が、その Stage の新 pack を活かせません。**並べ替えるか、
pack の中身を役割に合わせるかの判断が要ります。

### 2.5 最終盤の「先見不能」戦闘

設計 §17.2。**最終盤の一戦だけ予測を出さない。**予測と本戦を食い違わせるのではなく
出さないだけなので、「同じ入力から同じイベント列」の契約は無傷です。
Scenario 1 の第一戦（機械の故障で先見不能）と対になります。

このゲームは**強大な敵でも勝敗が戦う前に分かってしまう**のが弱点で、
一度だけ例外を作ると緊張が戻ります。**一発限りにすること。**常設すると読み合いが
ただのシステムになり、ややこしいだけになります。

### 2.6 数値を12戦で通していない

`CHARACTER_STATS`（R13）は**序章の四通りだけ実測で直した**ものです。
`analysis/ecology-trial.mjs` は通りますが、12戦の難度が妥当かは見ていません。
とくに次の二点が未確認です。

- ゴウが HP と腕力を両取りしている代償（受け1・技術6・AP1）が釣り合っているか。
  **受け1なので多段が全部素通りする**はずですが、実際に脆いかは確かめていません
- ヒバナは §2.1 が無いあいだ、ほぼ何もしていません

### 2.7 物語の残り

- **Scenario 2・3 の題名と、Want / Conflict / Choice の中身。**1と4だけ決まっています
- **ヒバナの「拾う」が未設計です。**戦闘中に坑の物を拾う行動を持たせるなら、
  戦闘と遠征資源の境界に新しい event が要ります
- **表情差分が足りません。**現行8種（neutral / wry / calm / firm / shock / worry /
  smile / hurt）に、怒・照れ・呆れ・得意 あたりが要ります（`content/portraits.mjs`）

---

## 3. 賭けの副作用を見る（R14 の変更由来）

- **技能の取り直し不可が、遠征中盤で「配られた技能点の使い道が無い」を作らないか。**
  作るなら、まず報酬側（技能点の配り方）を疑ってください。技能を戻せるようにするのは
  最後の手段です（戻せると予測が完全な以上、賭けではなく最適解探しになる）。
- **予測が常に見えることで、戦闘の再生を見なくなっていないか。**予測は数字だけで因果を
  出しません。再生を見る理由が残っているかは作者のプレイでしか分かりません。もし
  見なくなっているなら、対策は予測の制限ではなく、変更前後の chain 差分の表示です。
- 予測帯は5人ぶんで 390px 幅にちょうど収まります。**Stage 4 以降で6人以上になると
  横スクロールになります。**


## 4. 未実装（作者評価の後で判断する）

- Campaign Stage 4 以降と Endless。
- stage 固有の敵法則、affix family の購入。
- `pack_barrage`（連撃と刻印）と `pack_relay`（余波と受け渡し）は content として存在
  しますが Campaign には入っておらず、**いまはどこからも引けません。**
- 装備耐久は戦闘ごとにリセットされます。
- 野営治療の対象は自動選択で、手動の細かな選択にしていません。
- 回復が集中（`focus`）に連動していません。anti-stall を壊さないため意図的に触って
  いません。作者案「戦闘終了時に一度だけ集中に応じた回復・常時 AP-1」は、常設 passive
  として作れば抵触しない見込みですが**未実装・未検証**です。触るなら
  `analysis/ecology-anti-stall-audit.mjs` を通すところまで確認してください。
- 旧 difficulty rank の欄（`newRun` の `difficulty`、`composeEncounter` の rank 引数）は
  到達不能なまま残っています。Stage 4 以降で難度を動かすなら、ここを再利用するか
  Stage 側の係数で持つかを決めることになります。

## 5. 表示・読み物で判断していないこと

- **立ち絵は SVG のままです。**画像化は `portraits.mjs` 冒頭の3つの前提（決定性・
  ネットワーク往復ゼロ・表情を数値差で持つ）を崩すので、人物と会話が固まったあとに
  着手してください。まずヒバナの `neutral` と `smile` の2枚だけ出して、同一人物の
  表情で顔が揃うかを見るのが安全です。
- 根城の画面が長い（5人ぶんの名簿を全部開くと 390px 幅で9画面ぶん近い）。畳むべきかは
  判断していません。
- 根城の日常の場面は6本、家にあるものは9件で、**Stage 3 まで進めると全部見終わります。**
  Stage 4 以降を作るなら一緒に増える前提です。
- 幕の断片を act boss の直前に置いたので、12戦のテンポを切る可能性があります。
- 名簿・図鑑・根城は戦闘に影響しません。素通りされるかどうかは遊んでもらうしかありません。

## 6. 検査の穴

- 画面の通し（`ecology-tutorial-trial.mjs` / `ecology-trial.mjs`）は `check-all.sh` に
  入っていません。画面に触ったら手で走らせてください。
