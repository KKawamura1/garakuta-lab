# 作業の約束

## 作者の生活時間

**作者は 23:30〜07:30（JST）に寝ていることが多い。**

判断を仰いで作業を止めると、その時間帯は**朝まで何も進まない**。
手を止める前に `TZ=Asia/Tokyo date` で時刻を確認し、就寝時間帯なら
**自分で進められないかを先に検討する。**

止めてよいのは、本当に作者にしか決められないとき（好みの分岐、公開の可否、
これまでの合意を変える判断）だけ。**手が一択しかないなら、聞かずに進める。**

## 作者に手を動かしてもらう必要が出たら、通知する

**`PushNotification` を使う。設定も鍵も要らない。**（2026-08-22 に実測で確認）

- アプリを**開いている**と抑制されて届かない。**閉じていれば届く。**
- なので「返事が無い＝読んでいない」ではない。**届いていないだけのこともある。**
- `ntfy` はこの環境からは使えない。代理サーバが CONNECT を 403 で拒否する
  （公開先を読めないのと同じ egress ブロック）。どうしても要るなら GitHub Actions 側から叩く。

**飛ばすのは、作者の行動が要るときだけ。**

| 飛ばす | 飛ばさない |
|---|---|
| 遊べる版を公開した（＝プレイ待ち） | 調律機を回し始めた |
| 公開先の確認を頼みたい | 途中経過が出た |
| 作者にしか決められない分岐に当たった | 自分で決めて進めた |
| 長い作業が終わって、次が作者の番 | まだ自分の番が続く |

進捗報告で飛ばさないこと。**「次はあなたの番です」以外は通知にしない。**

## 評価器を書き換えるときの線引き

この企画は事前登録で進めている（`agents/PROTOCOL.md`）。数字を見てから条件を緩めるのは禁じ手。
ただし**評価に誤りがあったので直す、は妥当**である。区別は次のとおり。

| してよい | してはいけない |
|---|---|
| 登録文と実装の食い違いを、登録文に合わせて直す | 通らないから閾値を動かす |
| 作者の言葉と指標の対応がずれていたのを直す（理由を原文つきで残す） | 適用範囲を後から広げて通す |
| 標本・分解能の不足を直す | 分母をすり替えて数字を良くする |

**直したら、なぜ直してよいのかを記録に残す。** `analysis/smoke-gate.mjs` が
登録文とコードの閾値を機械で突き合わせているので、こっそり変えると落ちる。

## 検証を書くときの規則（第11回で8つ踏んだ結果）

1. **標本が端（全部／ゼロ）を指したときだけ確かめる。** 中間の値は標本でよい。
2. **閾値を判定できない標本数なら、数字を出さずに止める**（1/(1-閾値) 未満は不可）。
3. **二つの条件を一つの探索に入れない。** 別々に探して、両立しないなら両立しないと言う。
4. **関門が満たせないとき、まず「既に良いと分かっているもの」を同じ関門にかける。**
   参照点の無い関門は、自分が壊れていることを教えてくれない。

## プレイ記録の取り出し

**エクスポートは Claude 側から起動できる**（`workflow_dispatch`）。作者に頼まない。
手順は [`docs/EXPORT.md`](./docs/EXPORT.md)。D1 を直接読むことはできない（資格情報が無く、外向き通信も遮断）。

## 公開の手順

`main` へ push すると Cloudflare Pages が公開する。**遊べる状態でないものは main に出さない。**
公開前に `for f in analysis/smoke-*.mjs; do node $f; done` を全部通すこと。
この環境からは公開先を取得できない（egress がブロックされている）ので、**公開の確認は作者に頼る。**
