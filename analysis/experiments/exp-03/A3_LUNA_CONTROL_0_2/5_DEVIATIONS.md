# EXP-03 CONTROL 0.2 — 逸脱・停止記録

## 逸脱

実験票からの無断の仕様変更・閾値変更・敵生成範囲変更は行っていない。

既存のCONTROL 0.1実装とUIも変更していない。0.2用に次を独立追加した。

- core/control02.mjs
- analysis/gate-control02.mjs

## 停止理由

Gate Fは先に実行してPASSした。続くseed 1〜10000の昇順探索で、Gate E単独通過seedが0件だった。

R3は、seedが見つからない場合に範囲・閾値を変えず停止するよう定めている。そのため、数値を見てから条件を緩めず停止した。

## 公開条件との関係

Gate不合格のため、UI・公開・本番E2Eへ進めていない。作者へ代行も依頼していない。したがって、未実施の共通公開条件をPASS扱いしていない。

## Luna試用の判定

必須ゲート未実施のままURLを渡す、無断で仕様・閾値を変える、公開後に条件不足が発覚する、といった工程違反は発生していない。

ゲームの面白さを判定する人間テストには進んでいないため、面白さの判定は行っていない。