# CONTROL 0.2 Gate Audit — 初版

対象: `analysis/experiments/exp-03/R3_SOL_CONTROL_0_2.md`  
監査対象コミット: `4735fee2983018566c71722a963868fd437ca983`（main）  
作成日: 2026-08-24  
範囲: Gate実装の意味的適合性。ゲーム数値・探索範囲・閾値は変更しない。

## 監査結論

Lunaの停止判断（Gate E=0件のためURLを出さない）は正しい。しかし、既存 `core/control02.mjs` と `analysis/gate-control02.mjs` はR3の直接ゲートを十分に検査していない。したがって既存のGate A/F PASSやGate E 0件は、R3どおりの再評価結果としては使用できない。

## 確認した欠落

| Gate | R3要求 | 既存実装 | 監査判定 |
|---|---|---|---|
| A | 画面・内部計算・保存イベントの三者一致 | 計算ログだけを検査 | 完全Gate Aは未実施。計算・イベント検査をA-calcへ分離する |
| B | 高攻撃で偏向板が唯一最適、0では非最適 | 最適集合の差だけ | 不足 |
| C | nextAttackを無視して同型状態では同一行動の決定的方策 | 4方策だけ | 不足 |
| D | 最大HP全勝利列、過剰防御、高攻撃全防御／一部受け早期決着 | 偏向板有無の勝利列と過剰使用の存在だけ | 不足 |
| E | 勝敗込みの比較、同型状態の行動変化 | 勝利列だけで比較し、同型状態を未検査 | 不足。敗北/timeoutを不当に除外 |
| F | 5参照点をA〜Eの意味的条件へ実入力 | 生成範囲外を棄却理由に使用 | 不足 |
| 出力 | seed不採用でも証人・完全最適列・固定方策結果 | selected seed時しか詳細を出さない | 不足 |

## 修復方針

- R3の文言を変更せず、Gateの評価器と証拠出力を修正する。
- 条件ごとにpositive fixture、1点だけ壊すnegative fixture、negativeが落ちる検査、非vacuous検査を追加する。
- Gate Aは `A-calc`（計算・イベント）として評価し、UI／保存ログを含む完全Gate Aは今回未実施と明記する。
- UI、デプロイ、本番E2E、D1、作者URLは実施しない。

## 次段階

本初版とトレーサビリティ初版をコミットした後、コード修正、fixture検査、seed 1〜10000再探索へ進む。