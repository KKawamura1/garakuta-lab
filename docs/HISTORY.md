# 歴史的経緯 — なぜ今の形になったか

いま実装がこうなっている**理由**だけを残します。当時の実験記録
（`analysis/experiments/exp-18/` の R1〜R14 と A1〜A9、`PROJECT_MEMORY.md`、
`analysis/CURRENT.md` など）は削除しました。原文が要るときは Git 履歴を辿ってください
（`git log --diff-filter=D -- analysis/experiments`）。

## 1. EXP-18 に至るまで

ガラクタ・ラボは、作者一人が遊ぶゲームを何本も試作した場所です。EXP-18 以前の
プロトタイプと運用資料は現行ツリーから整理済みです。繰り返し観測されたのは次の点でした。

- 固定問題とデザイナー指定の組み合わせは、一度理解すると再プレイ理由を失う。
- 数値・勝率・解の存在・構造ゲート・コンテンツ数は、面白さの代理にならない。
- 内部で多数の計算が起きていても、因果が追えなければ「何も分からない」になる。
- 一つの万能な部品が全状況を支配すると、後続の報酬と判断が消える。
- ランダムな内容が毎回違っても、プレイヤーの判断が同じなら再プレイ性は増えない。

ここから抽出した一般設計メモが `docs/RULE_ECOLOGY.md` です（個別ゲーム仕様ではありません）。

## 2. EXP-18 の判断の流れ

| 記録 | 判断 |
|---|---|
| R1 | ルールエンジンの教材となる `frontier/` を作る |
| R2 | 仕組み先行の大きな system ではなく、人物と具体的な選択を中心に置く |
| R3 | 永続する人物を愛着の主語にし、技能・装備を交換可能にする |
| R4 | 小さな rule がイベントと状態を介して創発的に連鎖する構造を採用 |
| R5 | 決定的な ecology engine、validator、停止性・拡張性検査を実装 |
| R6 | 3幕12戦、Profile / Run 分離、有限補給、長期 loot の設計を固定 |
| R7 | system と content を段階的に実装し、各段階で作者評価を挟む順序を固定 |
| R8 | 難易度 rank ではなく **Campaign Stage** を本編の進行軸にし、pack・損耗・exact preview を統合 |
| R9 | 初期4 Stage を「5人とゲームの文法を覚える**チュートリアル**」として作り直す |
| R10 | New Game / Continue / Load Game を分け、旧セーブ互換を切る |
| R11 | 本編5人を作り直し、**攻めの軸を2本**にし、武器と技の違いを**隊列**で表す |
| R12 | Free mode と本編に出ない3人を削除し、未公開情報を画面から塞ぐ |
| R13 | **根城**と**図鑑**を作り、名簿を根城へ移す |
| R14 | **戦闘予測をキャンプ上部へ常設**し、技能を取り直し不可にし、偵察を廃止 |
| R16 | 技能を54本・状態を3つ足し、**engine が持っていて content が使っていなかった語彙**を使い切る |
| R18 | 取得済み技能を無制限に装着し、順番変更と一時的なオフを可能にする |
| R23 | **第一部を10 Stage** にし、Stage ごとに12戦を持たせ、敵を家系で分け、活動資金と鍛錬を釣り合わせる |
| Issue #175 | 反応・連鎖の安全ゲートを追加し、AP/RP循環・再発火・自傷コスト・過剰回復・rule limitを、現行定義・event trace・生成装備で検査する |

実装は A5（engine と基礎検査）→ A6（replay と画面）→ A7 / A8（Phase A の契約と実装）→
A9（Phase B の遠征・経済・Profile / Run）→ R8 Phase 0〜2（Campaign Stage、損耗、
anti-stall）→ Phase C（生成装備と Blueprint）の順に進みました。

## 3. 効いている判断の理由

### 3.1 難易度 rank をやめて Campaign Stage にした（R8）

現行 content の実測で、作者の凝った構成は素朴な役割配分に対して約1.09倍にしかならず、
仕込んだ AP・RP・人物・技能枠に見合う超過利得が出ていませんでした。seed が4 pack 中
3 pack を選ぶ方式では毎回75%の語彙が残り、学習順や旧技能の再解釈を設計できませんでした。
そこで Stage ごとに pack 構成を固定し、**新 pack と選ばれた過去 pack の接続**を設計する
方針へ変えました。数値倍率で難度を作る案は、同じ理由（判断が増えない）で撤回しています。

### 3.2 初期4 Stage は累積、第2章は回転をやめた（R9 §2 / R23）

有効 pack 数の式は R8（`1 + ceil(sequence/2)`、過去 pack を回転）と R9（`sequence + 1`、
累積）で違いました。初期4 Stage は累積を採りました。理由は、前の技能がまだ使える／
新しい人物が古い pack を別目的で使う／新 pack は以前の価値を一つだけ変える／
全部理解しなくても進める、の4点です。

**R23 で、Stage 4 以降の「回転」も採らないことにしました。**技能点は一遠征15点で
固定なので、pack が増えても同時に取れる技能の数は増えません（増えるのは選択肢だけ）。
そこから覚えた pack を取り上げると、「前の Stage で組んだ形が今回は作れない」という
理由だけの難度になります。第2章は Stage 4・5 で残り2 pack を足し切り、以降は6 pack の
まま据え置いて、新しさは**相手の家系**（灰塵・灰織・灰炉）の側で出します。
`ladderMode` は `tutorial` と `campaign` の二つになりました。

影響として Stage 3 の有効 pack が3から4へ増えたので、core / full の二段で相殺しています
（新 pack はその Stage では入口だけ）。また R8 は Stage 3 を B（連撃と刻印）にしていましたが、
HP 持ち越しがある以上、回復の意味を Stage 3 までに見せる必要があるため C（手当てと余剰）を
採りました。`pack_barrage` は Stage 4 の新 pack として第2章の入口に置き直しています。

### 3.3 攻めの軸を2本にした（R11）

「5人のうち3人が守り役では多すぎる」という作者の指摘を測ったところ、原因は配役ではなく
**数値系が守りに2軸（防壁 `focus`・軽減 `guard`）、攻めに1軸（`might`）しか持っていなかった**
ことでした。攻撃28件が全部 `might`。R6 §4.4 は「支援役にも technique 攻撃を付けられる」と
宣言していたのに、実装は片側だけだったのです。**`focus` を「技の威力」へ格上げして、
宣言のとおりに実装しました。**

続く「腕力の技と集中の技の違いが分からない」には、名前や属性ではなく**位置の規則**で
答えました。**腕力で伸びる攻撃は後列から出すと 40% まで落ち、集中で伸びる攻撃は落ちない。**
engine への追加はこの一点（`effects.mjs` の `afterRearFalloff`）だけで、人物固有の分岐は
足していません。これで人物の設計がそのまま隊列の設計になり、カイは「前で落ちるか、
後ろで火力を捨てるか」の二択を背負います。

その二択を軸に加入順を組み替えました。**Stage 1 でカイが問題を持ち込み、Stage 2 で
スミがそれを解く。問題を出してから、その解決を渡す。**

同じときに `ecology-campaign-smoke` を削除しました。**隊列を一度も変えず**、装備の
配り先も技能点の使い方も選ばない120通りで「Stage 2 が12戦通せない」と言っていましたが、
隊列は R11 で中心のメカニクスになったので、測っている内容自体が smoke として成立して
いませんでした。閾値を下げて通す代わりに検査そのものを外し、Stage 0〜3 の build 到達
可否は作者が遊んで判断します。（スミの腕力を上げれば通りましたが、「攻撃を持たない
庇護役」という設計が壊れるので採りませんでした。）

### 3.4 Free mode と3人を削除した（R12）

ストーリー上まだ公開していない情報を画面へ出さないため、編成の「後で加入する仲間」、
ギルド投資の未加入者タブ、Stage 選択カードの加入者名、遠征開始画面の未解禁 pack、
技能ツリーの manifest 外ノードを塞ぎました。来歴を持たない同業者3人（トキ / ヨリ /
アカリ）を編成画面に並べておくと、未公開の加入者と区別できず物語の先が割れるため、
Free mode ごと削除しています。付随して Free mode 補助の
`ecology-expedition-smoke.mjs` と `ecology-decision-space-smoke.mjs` も消しました。

旧 difficulty rank は画面から到達不能になりましたが、`newRun` の `difficulty` 欄と
`composeEncounter` の rank 引数は残してあります（Campaign は 0 固定）。

### 3.5 根城と図鑑（R13）

R11 が「日常の場面と設定の厚みはここに置く」と決めていたのに、どこにも無かった画面です。
名簿はギルドのタブに埋まっていたので、遠征のたびに一度必ず通る根城へ移しました。
図鑑は R8 §3.2 が ProfileState に挙げたまま未実装だったものです。**engine には一行も
足していません**（Profile に `bestiary` の一欄が増えたので contract-snapshot を作り直しました）。

### 3.6 予測を中心に据え直した（R14）

作者の指摘は「次のステージの完全な戦闘予測が得られる、というのがこのゲームのキモ」でした。
R8 §11 は exact preview を「無料・副作用なし・実行と完全一致」と定義していましたが、
**置き場所を「戦闘前の最終確認」画面にしか作っていませんでした。**組み替えの手は
キャンプのタブにあり、一手触るたびに進んで、見て、戻る。中心のはずのものが往復の先に
ありました。

- 予測をキャンプ全タブの上端へ常設。計算経路は増やさず、実行と同じ道を通す。
  毎 render で戦闘を1回走らせるので入力の署名で memo している（engine は決定的なので
  結果は変わらない）。
- **予測が別の盤面を走らせていた不具合を直した。**灰の門は12戦の梯子に属さないのに、
  画面は `composeEncounter` を固定で呼び、目の前にいない敵で試算した結果を出していた。
- 巻き戻す前の一戦では予測を出さない（読めても直せない予測は脅しにしかならない）。
- **技能を取り直し不可に。**予測が完全だと、技能の出し入れは賭けではなく最適解探しの
  作業になる。装備は従来どおり自由（都度の調整はそちらへ寄せる）。
- **偵察を廃止。**次の一戦が完全に読める以上、先の幕を1つ前倒しで覗く枠に値段がつかない。
- engine への追加は `startingHp` 一点だけ（減少量を出す側が passive の max_hp 補正規則を
  再現しなくて済むように）。

R6 §17.2 は「reset は使った点をそのまま戻す。遠征の途中で組み替えられないと、報酬で
得た技能が死に札になる」と言っていました。**この懸念は残っています**（GitHub Issue #102）。

### 3.7 人物を作り直し、題を `One Battle Ahead` にした（R13）

R11 の5人（シキ／ナズナ／カイ／スミ／レイ）を、ゴウ／ツグミ／ナギ／ヒバナ／ゲンゾウへ
差し替えました。全員が観察者で、全員が感情を抑制し、望みが抽象的で、体格と声量が揃い
すぎている——というのが差し替えの理由です（設計は
`analysis/CAST_REBOOT_TWELVE_TOMORROWS.md`）。

**engine の id は据え置きました。**`lancer` が受け役、`guardian` が遊撃役になり、id の
語義と中身は一致しません。id は技能・pack・contract の対応を保つ鍵であって役割名では
なく、改名は公開済み ID の引っ越しになるためです。代わりに、id を前提にしていた場所
——roster の役割・図像・既定配置・初期技能、Stage の問いと学習目標、docs——を全部
洗い直しました。

**pack の解禁順は動かしていません。**`campaign-stages.mjs` の検査が「sequence 0 以外は
primary_offense pack が残っていること」を要求し、campaign の primary_offense は
`pack_edge` 一つしか無いので、edge を後ろへ動かせません。**直したのは問いのほうです。**
問題は Stage 0 で既に出ている（主火力のツグミが一番柔らかく、ゴウは受け1）という形に
読み替え、Stage 1 は「誰が前に立つと、誰が振り抜けるか」、Stage 2 は「隊列を動かして、
何を得るか」に変えました。

**初期技能は数値の傾きで選び直しました。**旧 roster は R11 の数値向けのままで、
腕力16のナギに腕力依存の主力2枠、受け3のヒバナに受け役一式、集中6のゴウに集中依存の
防壁が入っていました。ゴウの active を一本だけにしてあるのは、Stage 0 に腕力で読む
技能が `steady_cut` しか無いからです（`strike` は枠を使わず必ず出る）。

**題を `One Battle Ahead` にしました。**検討の全文は `analysis/TITLE_STUDY.md` にあります。
決め手は尺度で、戦闘予測が見せるのはちょうど一戦先の確定結果であり、"one" が
「二戦先は見えない」という上限まで同時に言っている点です。日本語の副題は付けません。

### 3.8 チュートリアルで人物を掘るのをやめた（R15）

R13 は Stage 0 の会話に兄の失踪を置いていました（`stage_0_opening` の「お兄さんの記録が
出てから睡眠一時間減、朝食半分、脈が二」と、`stage_0_act2` の「ゴウは少し長く喋った。
ツグミは帳面に何も書かなかった」）。**開幕の一枚が喪失の場面になり、ゴウが最初から
寡黙な人物として読める**というのが作者評価でした。設定上のゴウは「声がでかい。ガサツで、
口が上手くて、調子がいい」であり、**別人になっていました。**

Stage 0 の8断片を、二人の**調子**だけを渡すものへ書き直しました。ゴウは減らず口で押し、
都合が悪くなると逸らし、押し切られると「へいへい」で流す。ツグミは早口で被せ、
丁寧語のまま容赦なく数を突きつけ、流されると「もう！」で丁寧語が剥がれる。
兄・記憶の速さ・言い過ぎた後悔は**全部外しました。**

**ゴウを素直に折らないことにしました（作者指摘）。**初稿は開幕を「……はい」で締めて
いましたが、**大男が急にしおらしくなる理由が読み手に無く**、しかも直後に一分で立って
先に行くので**言行が一致していません**でした。「へいへい」は口だけ合わせて従わない返事
なので、そこでやることと合います。**対等に見えるのは、折れる側がいないからです。**
`character-lore.mjs` の `warden.figure` も同じ形へ直しました（設定と台詞が食い違うと、
R13 で起きた乖離がそのまま再発します）。素直になるのは巻き戻したあとの
「……ああ。今日は、三分待つ。」**一度だけ**にしてあります。

**厚み（行数）とは別に、深さの勾配を置くことにしました**（`dialogue.mjs` 冒頭）。
望み・秘密・過去・後悔は五人が揃ってから出します。まだ誰のことも知らない読み手に
重さだけを渡すと、人物の調子が沈み、**後で沈んだときに意味が出なくなる**からです。

**兄の伏線は Stage 0 から消えました。**ゲンゾウが Stage 3 で「お兄様は、ご存命ですよ」と
出す側は動かしていませんが、**そこまでの助走がいまどこにも無い**状態です
（GitHub Issue #118）。名簿の `warden.field` / `warden.will` には残っているので、
読みに行った人だけが知っている形になっています。

### 3.9 冒頭戦闘を「他者手当て」の学習にした（R15）

冒頭は、ゴウを前列に置いても受け1の細かい被害で瀕死になり、並べ替えが「ツグミ死亡」から「ゴウ瀕死」へ変わるだけだった。敵のHP・攻撃順・seedを変えず、ツグミの初期 `triage` だけに `not_self` を付け、自分以外の味方を被弾量の50%だけ回復するようにした。既存の `emergency_treatment` は自己防衛として残したので、ツグミを前列に置くとゴウを救えず、後列に置くと自分をつなぎながらゴウを支えられる。隊列と役割の因果を一戦で見せつつ、RP1・chain1の被弾連動なので anti-stall も崩さない。さらに、巻き戻し時に `run.formation` を `PROLOGUE.formation` へ戻し、失敗配置を引き継ぐようにした。何も変えずに勝てる抜け道を塞ぎ、「一手直して勝つ」を画面でも成立させる。


### 3.10 冒頭の軽口を、関係の良さが先に立つ形にした（R15追補）

R15 の開幕は、ゴウの「見てんじゃねえよ」とツグミの「見るのが仕事です」、さらに過去の
負傷数を一息で突きつける応酬によって、**親しい二人の軽口より拒絶と詰問が先に読める**
状態でした。作者評価は「ゴウもツグミも、やや嫌な人物に見える」でした。

三分待たない筋と、ゴウが「へいへい」で流す性格は維持しています。そのうえで、ツグミは
校正の三分を診察に使おうとする世話焼き、ゴウは名前を呼んで観察眼を認め「先生」と頼る側へ
言い換えました。ツグミも「隠すのが下手」と笑って返します。**棘を抜くのではなく、棘を
向けられるほど近いことを先に見せる**変更です。戦闘へ入る理由、敗北、巻き戻し後の
「今日は、三分待つ」、勝敗条件は変えていません。


### 3.11 初戦の初期資源を空にし、勝利報酬を全員配布に戻した（2026-09-05）

R6 の当初仕様に合わせ、1戦目は技能点0・固定初期装備0から始めるようにした。通常戦の勝利時は技能点1を現在の編成全員へ自動で付与し、報酬候補から技能点カードと配布先の選択を外した。プロローグは遠征外として、活動資金と技能点の報酬を発生させない。Blueprint の明示的な持込品は、出発前の選択として残している。
### 3.12 冒頭の正解配置を余裕のある勝利にした（2026-09-05）

§3.9 で「一手直して勝つ」は成立したが、実際にはツグミを後列へ動かしてもゴウとツグミのHPが大きく削られ、位置変更の成功体験が細かった。敵HPだけを下げ続けると初期配置まで勝ってしまうため、通常戦の敵定義を変えず、prologueだけ敵HPを60%、攻撃（might / focus）を50%に調整した。初期配置はツグミが2ラウンド目に倒れる敗北を残し、正解配置は4ラウンドでゴウ167/300、ツグミ70/110を残して勝つ。位置だけを変える操作と「予測で確認して直す」ループを保った、チュートリアル専用の調律である。


### 3.13 装備を一つの呼称へ統一した（2026-09-05）

固定装備と手続きで組み上がる装備を報酬画面で分けると、プレイヤーには装備が二種類あるように見える。現行の報酬候補を装備2件と補給1件に固定し、画面・設計図・目利きの説明ではすべて「装備」と呼ぶことにした。装備の rule の完全開示、決定性、Blueprint への保存、持込時の復元は残している。過去の保存や検査を読めるよう、固定定義と generatedEquipment の内部形式は互換用に保持するが、通常プレイの報酬や装備画面では提示しない。

### 3.14 技能を54本足し、状態を3つ増やした（R16）

作者の要望は「面白い技能をたくさん」でした。**数だけ増やすと、同じ技能の係数違いが
並ぶ**——それは R6 §17.1 と R9 §4.2 が繰り返し禁じてきた形です。だから増やす前に、
**engine が既に持っているのに content が一度も使っていない語彙**を数えました。

| 使っていなかったもの | R16 で使い始めた技能 |
|---|---|
| `history_count`（content 全体で1箇所だけ） | 二の太刀・散らし斬り（同じ相手を続けたか）、静かな一手（このラウンド殴られたか） |
| `swap_positions` を**敵に**使う | 引きずり出す（敵の前後を入れ替えて後列へ刃を通す） |
| `timing: "interrupt"` を player 側で | 衝撃を殺す（飛んでくる数字を削る）、差し込む号令、出鼻を挫く（宣言そのものを消す） |
| `status_added` / `status_removed` を読む | 隙に応じる、目を離さない、刻印の残響、怯みを回す、裂傷の余波 |
| **敵の** `preparation_started` / `_advanced` | 溜めを読む、溜めを崩す（`interrupt_preparation` も初使用） |
| `status_stacks_scaled` | 積もる刻印（隙の段数そのものがダメージ量になる） |
| `lose_hp` コスト | 痛みを分ける（自分のHPで仲間の傷を買う） |
| `max_hp` スケール | 意地の壁（術力ではなく体で張る） |

**状態を3つ足したのは、相方が「隙」と「集中」しか無かったからです。**この2つだけを
相方にすると、新技能はどれも「隙を付ける／集中を得る」の言い換えになります。

- **怯み（staggered）** … 持ち主が**出す**ダメージが1段につき8軽くなる。
  倒さずに相手を細くする軸で、攻め手側から守りを作れるようになりました。
- **守勢（warded）** … 持ち主が**受ける**ダメージが1段につき8軽くなる。
  防壁（総量を吸う）・受け構え（回数を止める）に続く**三つ目の守り**です。
  減る量が固定なので**細かい多段に強く、大技には薄い**——既存の二つと三つ巴になります。
- **裂傷（bleeding）** … ラウンド終わりに一度だけ、1段につき12を**受け無視で**刻む。
  受けの厚い相手へ通る細い線です。

守っていること:

- **engine と schema の語彙は1つも増やしていません。**54本すべてが既存の event・
  effect・predicate・target で書けています（`analysis/ecology-contract-smoke.mjs`）。
- **完全上位互換を作っていません。**数字が大きいものには必ず、条件（1ラウンド目だけ・
  HP50%以下だけ・同じ相手を続けたときだけ）か代償（自分に「隙」・自分のHP・
  反応権2・戦闘1回きり）が付いています。
- **anti-stall。**戦闘防壁（ラウンドで消えない）を配る2本は、開幕2ラウンドに限りました。
  v1 schema では active に limit を書けないので、round で閉じるしかありません。
  裂傷も round で消えるので、待って積み上げる形にはなりません。

### 3.15 前提が届かない節を3つ直した（R16）

R16 で `analysis/ecology-skill-catalog-smoke.mjs`（定義・パック・節・説明文の四点照合）
を足したところ、**既存の節が3つ、前提を別 pack に置いていた**ことが分かりました。

- `brace_after_hit`（pack_care）の前提が `cover_ally`（pack_wall）→ Stage 0・1 で死に節
- `rear_hunt`（pack_edge の入口）の前提が `guard_crush`（pack_edge の全体）→ Stage 1 で死に節
- `reposition`（pack_wall）の前提が `relay_order`（pack_tempo）→ Stage 2 で死に節

R12 で「manifest に無い節は出さない」に変えたため、**前提だけが見えないまま節が並ぶ**
形になっていました。三つとも前提を baseline（`strike` / `bulwark`）へ移しています
（R9 §9.2「前提は各 pack の入口技能に置く」）。

### 3.16 初回勝利の後に補給を教える（2026-09-05）

隊列を直して本編第1戦に勝つ学びの直後に、HPの持ち越しと補給の意味を接続するため、報酬取得後に補給タブを自動で開き、「集中治療」を1回使う案内を追加した。プロローグ「灰の門」には報酬も補給導線も出さず、案内の完了印は ProfileState の storyFlags に保存する。既存のキャンプ治療と補給の三用途は変えず、追加の会話や戦闘ルールは持ち込まない。

### 3.17 巻き戻しの事実をゴウが伏せる（2026-09-05）

チュートリアル敗北後の会話から、ゴウが「杭を使った」「ツグミが死んだ」と明かす台詞を外した。
プレイヤーには敗北の地の文で巻き戻りを示す一方、ツグミにはまだ答えを渡さず、戦闘1後に
同じ盤面とゴウの言葉から違和感を拾わせる。戦闘1後の会話本文は変更していない。

### 3.18 `focus` の表示名を「技術」に統一（2026-09-05）

`focus` はエンジンと保存データで使う内部 ID のまま維持し、プレイヤー向けの能力値表示だけを「技術」に統一した。状態 `focused` の表示「集中」や、状態を使う技能名は変更していない。技能説明、人物カード、遠征情報、チュートリアル、検査の期待値、仕様書を同じ語彙へ揃えた。

### 3.21 行動を陣営フェーズへ変更した（2026-09-05）

速度や内部 ID を全員の横断的な行動順に使うと、決定的でも次に誰が動くかを
プレイヤーが読みにくかった。PR #99 の隊列順を踏まえ、round 開始時の AP 回復後は
味方が隊列順に一行動ずつ、その後に敵が一行動ずつ進み、AP が残る間はこの順を
繰り返す形へ改めた。AP2 は同じ phase で連続行動せず、次の自軍 phase へ戻る。
味方先行で敵の先手を失わせるため、prologue の数値を調整し、初期配置が負け、
ツグミを後列へ下げる配置が勝つチュートリアル契約を保った。

### 3.48 Issue #209 — 補給チュートリアルを必須操作にした（2026-09-08）

従来は本編第1戦の勝利後に集中治療を強調するだけで、補給を使わず「この敵に挑む」へ進めた。補給を使うまで、Stage 0 の補給タブだけを有効にし、集中治療を選んだ後に負傷した仲間をプレイヤーが明示して選ぶ導線へ変更した。対象確定までは補給を消費せず、確定後に1個だけ消費し、対象・残数・チュートリアル完了を同じ画面へ返す。通常時も集中治療と蘇生の自動対象を廃止し、対象選択を共通化した。

開始補給は通常遠征で0個へ戻し、New Game の Stage 0 だけをチュートリアル用の1個にした。これで再挑戦・引き直し・野営治療の三用途が同じ有限資源を取り合う意味を保ちつつ、導入の一手だけは必ず実行できる。

型の比較では、XCOM 2 の短い手順ゲート、Pokémon 系の任意に戻す説明型、Into the Breach の目的と合法手の同時提示、Hearthstone のカード・対象・即時結果を並べた。採用したのは、前者の「必要な一手が終わるまで次へ進めない」と後二者の「対象を自分で選び、その結果を同じ画面で確認する」の組み合わせである。長いスクリプトや専用画面を持ち込まず、既存の RunState / campTreat 経路に閉じることで、チュートリアルだけが通常の有限資源ルールを上書きしないようにした。

## 4. 順序を守っていないところ

R8 §18 は Phase C（生成装備と Blueprint）を Gate 2（作者による Stage 0〜3 評価）の
**後**に置いていました。作者の指示で先行実装しています。

## 5. 一度検討して採らなかった案

`docs/DESIGN.md` §10 に一覧があります。再提案するときは、当時の理由と何が変わったかを
添えてください。

### 3.16 装備の等級を item / effect の二軸へ広げた（R17）

装備がほぼ毎回「補給より少し強い」だけになり、補給を選ぶ理由が消えていました。
一方で、10以上の Stage を各12戦・複数周回する前提なら、すべての drop を当たりにするのではなく、
大半はその遠征中に使える、少数だけ長期採用できる、という山が必要です。

そこで旧4等級を ID として残したまま、`mythic` と `oopart` を追加しました。item rarity は
rule 数・affix 予算・耐久・発動上限を広げ、各 payoff は別々の effect rarity を持ちます。
一つの item に最高品質を必ず一つ置き、残りは下位品質も混ぜるため、オーパーツでも
「基礎効果は遺物、効果1はオーパーツ、効果2は希」のような組み合わせが生まれます。
continuous effect は品質に応じて 100% / 115% / 135% / 160% / 195% / 240% に伸びます。
drop weight は 55% / 28% / 12% / 4% / 0.8% / 0.2% とし、何百個かに一つの偶然を残します。

既存 Blueprint の descriptor・定義・来歴は捨てず、新 generator version で新規品だけを差別化します。
生成に失敗した pool はこれまでどおり 50 attempt 後に診断 error とし、補給への単純な退避を
報酬設計の前提にしないことを狙います。

### 3.19 装備レアリティを読める発見へ整えた（2026-09-05）

item rarity と effect rarity の二軸は R17 で実装済みだったが、画面では短い色チップと
文章中の角括弧だけだったため、特に「基礎効果はレア、追加効果1はオーパーツ」のような
当たりの中身を一目で比較できなかった。

表示名をコモン／レア／エピック／レジェンダリー／ミシック／オーパーツへ統一し、格番号を
併記した。装備・報酬・Blueprint では「装備レアリティ」と「効果の格」を分け、基礎／追加
スロットごとのバッジ、全文の条件・代償・発動回数を同時に表示する。レジェンダリー以上は
報酬カードの枠と短い発見表示だけを強め、情報を隠す演出や、レア品を自動的に強制採用させる
演出は入れていない。

### 3.20 効果の重複表示を減らし、当たりを先に読む（2026-09-05）

前回のレアリティ表示では、効果ごとの格をまとめた「効果の格」欄と、その直下の個別効果欄が
同じ情報を二重に見せていた。まとめ欄を削除し、個別効果欄だけに格番号・名称を残した。
複数の効果は effect rarity の高い順に並べ、同格なら元の効果順を保つ。item rarity と
effect rarity の二軸、条件・代償・数値・発動回数の完全開示、保存データの互換性は変更していない。

### 3.22 取得済み技能を無制限に装着し、順番と一時停止を保存する（R18、2026-09-05）

技能点を使った取得は一度だけで、忘却や払い戻しはできない。取得済みの行動・リアクティブ・常設は人数や枠数を理由に装着できなくならず、行動とリアクティブの順番は loadout に保存する。同じ条件を待つ同じ仲間のリアクティブは、その並び順で上から発火する。

装着済み技能には一時停止を保存できる。「誰を伸ばす？」でオフにした技能は取得状態や前提を失わず、戦闘と exact preview の BattleInput からだけ除外される。旧 save にこの欄が無い場合は全技能を有効として読み込み、技能の旧第4枠投資は購入不可のまま履歴だけを保持する。

### 3.23 技能ツリーを tier の格子から、派生の森へ（R19 / issue #137、2026-09-05）

それまでのツリーは「系統（攻撃／指揮／支援／守り／基礎）× T1・T2・T3」の格子だった。
節は tier ごとの箱に並ぶだけで、**どの節がどの節から派生したのかは画面に出ていなかった**。
前提は節を開かないと読めず、開いても名前が一つ書いてあるだけで、そこから先に何が生えるのかは
分からない。AP を払う行動と RP を払う反応が同じ箱に混ざるので、どちらの資源を伸ばす話なのかも
読めなかった。123 節まで増えた時点で、取得方針が立たなくなっていた。

ツリーを `requires` から組んだ森へ置き換えた。座標は手で書かず
`ecology/content/skill-tree-layout.mjs` が組む（x = 前提からの深さ、y = 行）。手で書くと
`requires` を直したときに黙ってずれるためで、代わりに座標が満たすべき決まり——座標重複・
x 列違反・線の交差・循環・分岐数不足——を `validateSkillTreeLayout()` と
`analysis/ecology-skill-tree-smoke.mjs` が全節と Stage ごとの部分森の両方で見る。

系統は種別（行動 / 反応 / 常設）の三つのツリーへ分け、画面上のタブで切り替える。種別をまたぐ
前提は普通の派生に混ぜず、**橋渡しの節**として明示する（反応ツリーの左端に「橋渡し ← 斬撃」が
立ち、そこから反撃たちが生える）。節を押すと前提ルートと派生先だけが強調され、開いた説明の
「前提」「派生先」は押せるので、iPhone でも横スクロールせずにルートを辿れる。節の上には
種別・消費（AP / RP）・対象または発動条件・深さ・分岐の数が出る。

既存の取得・装着・並べ替え・オン／オフ・AP/RP 消費は変更していない。

### 3.24 技能レベルと、x=1〜10 の深さ（R19 / issue #137 の残り、2026-09-05）

3.23 では「スキルレベル制と x=3 / x=5 / x=10 の大分岐は content 側の再設計なので入れて
いない」と書いた。**それでは設計を検証できない**（作者判断）ので、content と統合を同時に
進めることにし、各 content ファイルの冒頭にあった「ここを触ってよいのは X 担当だけ」の
分業規定を削除した。

**技能レベル。** 同じ効果の上位互換を別技能として増やす代わりに、一つの技能を段階的に
上げる。解禁は Lv1、そこから 1点ごとに 1段（最大 Lv10）。1段につき連続量（damage /
heal / barrier とその増減）が 12% 上がり、AP・RP・段数・回数・耐久は変わらない。連続量を
持たない技能（位置替え・号令・隙を刻む など）は「レベルなし」と画面に書き、点を払わせない。
上限は手で書かず、定義が連続量を持つかどうかから引く（`content/skill-levels.mjs`）。
engine は技能 ID で分岐せず、`ally.skillLevels` の表に載っている技能の連続量へ係数を掛ける
だけである（`effects.mjs` の `afterSkillLevel`）。**Lv1 は係数 1.0 ちょうどで掛け算そのものが
起きない**ので、レベルを知らない入力・保存・replay の結果は1バイトも変わらない。
`BATTLE_SCHEMA_VERSION` を `ecology-battle-4` へ、content contract を 12 へ上げた。

**深さと分岐。** 節を tier（0/1/2）の平面から、`requires` の入れ子で書く森へ置き換えた。
字下げがそのまま x（前提からの深さ、1〜10）になり、手で座標を書かない。x=1 が入口、
x=3 が1回目の役割分岐、x=5 が2回目、x=10 が最終到達点である。**技能も pack も1本たりとも
増減していない**（123 節のまま）。値段は 1節 1点に揃え、x=10 まで 9点——遠征1回で配られる
技能点とほぼ同じ——にした。

**届かない到達点を作らない。** 行動ツリーと反応ツリーには、最終 Stage までに実際に取り
切れる x=10 の道を一本ずつ通した（行動は「捨て身の一振り／手負いの突撃」、反応は「溜めを
崩す」）。`analysis/ecology-skill-tree-smoke.mjs` が、x=3 / x=5 の分岐・x=10 の複数到達点・
campaign からの到達可能性・宣言した座標と組み直した座標の一致を、全節と Stage ごとの
部分森の両方で見る。

**取得済み技能の前提も取得済みにする。** ツリーが一本道になったので、starter が x=5 の節
なら、そこまでの道も一緒に開く（`initialUnlockedSkills` が前提の閉包を取る）。線を辿れる
ようにしたのに、線の途中が欠けているのは嘘である。

### 3.25 速度能力値を削除した（2026-09-05）

速度は行動順から外した後、速度順の対象選択と「速さ」パッシブにしか残っていなかった。
用途が隊列と重複し、数値を上げても戦闘の読みやすさや選択肢を増やさないため、
character / enemy の定義、結果 log、速度順ソート、速度依存の技能・装備表示を削除した。
隊列を使う対象選択は `position_asc` / `position_desc` へ移し、保存済み run と結果の
schema version を更新した。戦闘 replay の再生速度は演出設定として別機能なので維持する。

### 3.26 種別をまたぐ前提（橋渡し）を廃止した（2026-09-05）

3.23（R19）は種別（行動 / 反応 / 常設）をまたぐ前提を「橋渡しの節」として明示する形を
試した。実際に説明すると「橋渡し ← 手当て」が何を意味するのか一目では読めず、
`from()` という別構文を覚えないと前提を辿れないため、分かりにくいと判断して廃止した。

前提をまたいでいた9箇所を、同じ種別の中の別スキルの派生として繋ぎ直した
（`aimed_shot` 系は行動の `strike` 配下、`counter_blow`／`brace_after_hit`／
`guarded_opening` 系は反応の `mend` 配下、`steady_hands`・`first_blood`・`mark_reader`・
`opening_guard`・`held_breath` は常設の対応する `foundation_*` 配下）。深さ（x）と
費用は変えていない——橋渡しの直下の子は元々 x=2 だったので、同じ種別内の根の直下へ
繋ぎ直しても座標は一致する。

`skill-tree.mjs` から `from()` を削除し、種別をまたぐ前提そのものを書けなくした。
`skill-tree-layout.mjs` の橋渡し表示（bridge anchor）と、`app.js` / `styles.css` の
橋渡し用UI（「橋渡し ← X」の節・押すとツリーを切り替える導線）も削除した。
`analysis/ecology-skill-tree-smoke.mjs` に、種別をまたぐ前提が無いことを見る検査を足し、
自己検査（末尾）で実際に検出できることも確かめている。

### 3.27 巻き戻したあとの一戦を、本編第1戦として commit する（2026-09-05）

R11 §5 は「巻き戻したあとの勝利は本編1戦目」と DESIGN.md 6.4 の文面では既に書いていた
（「戦闘1後の観察」）が、`app.js` の実装は `prologueActive` が真のあいだ全部（本当に
負ける一回・巻き戻したあとの一回の両方）を「遠征外」として扱っていた。その結果、
巻き戻して勝っても持ち越し HP が commit されず（次戦は毎回全回復）、技能点も報酬候補も
出ないまま、そのあと別途フルスペックの本編第1戦（「灰の入口」）をもう一度戦わされていた。
実際に遊んだ作者から「巻き戻ったあとの一戦が1戦目として扱われていない」と指摘され、
文面どおりに直した。

**本当に負ける一回だけ**は `startPrologue()` が `simulate` action を経由せず直接
`simulateBattle` を呼ぶので、run を一切変更しない。**巻き戻したあとの一回**は通常の
`camp → battlePreview → simulate` を通るので、`simulate` handler から `prologueActive`
の特別扱いを外し、勝てば `recordEncounterCleared` / `grantRunSkillPointsToAll` /
`commitBattleResult` を通常戦と同じ経路で呼ぶ（encounterIndex は最初から 1 なので、
新しい index を発行する必要はない）。結果画面・報酬画面をそのまま出すため、
`prologueActive` を落とすのは `advanceAfterReward`（報酬を受け取り、次の戦闘へ進む瞬間）
まで遅らせた。ここで落とすまでは `currentEncounter()` が「灰の門」を指し続けるので、
結果・報酬画面のタイトルも正しいまま出る。

これにより、フルスペックの「灰の入口」を初回プレイでは戦わなくなった（2周目以降は
`prologue_seen` フラグにより従来どおり第1戦として出る）。功績・活動資金は
`recordEncounterCleared(run, 1)` が `expeditionEncounter(1)` の `kind`（"normal"）から
引くので、実際に戦った敵構成と無関係に、通常の第1戦クリアと同額になる。

補給チュートリアル（DESIGN.md 6.2）の表示条件は変えていない。`shouldShowSupplyTutorialAfterReward`
は encounter 1 の勝利を見ているだけなので、その勝利が巻き戻したあとの一戦へ前倒しに
なったことで、案内も自動的に「最初の敵を倒した直後」（＝本編第1戦の報酬を受け取った
直後）へ前倒しになる。

あわせて、`prologueActive` のあいだは画面ヘッダーの「安全に撤退する」導線を出さないように
した（`shell()`）。以前はチュートリアルの最中でもこのボタンが出ており、隊列を直しきる前に
遠征を放棄できてしまっていた。`abandon-run` / `back-title` の action handler 側にも同じ
条件の早期 return を足し、ボタンを消すだけでなく経路そのものも塞いだ。

`analysis/ecology-tutorial-trial.mjs` を新しい経路（巻き戻して勝った直後に結果・報酬画面が
出て、そのまま補給チュートリアルへ続く）に合わせて更新した。

### 3.28 技能ツリーの派生線を、インデントの目分量から実座標の直線へ（2026-09-05）

R19（3.23）で座標（x=深さ・y=行）は `requires` から機械で組むようになったが、画面は
その座標を**使っていなかった**。節を縦一列に並べ、前提の深さぶんだけ左に空白とレールを
足す、ファイラーの階層表示と同じ描き方だったため、兄弟が増えると「どのレールがどの節へ
つながっているか」を目で追う以外に手段が無く、作者本人から「どこと繋がっているか非常に
分かりにくい」と指摘された。横スクロールも `min-width` を少し超えるだけで、深い列を
押し込めて縮める方向に働いていた。

`ecology/content/skill-tree-layout.mjs` が既に持っている x・y をそのまま CSS Grid の
`grid-column` / `grid-row` に渡し、列を固定幅（`--tree-col-width`）にした。列を縮めない
ことで、深いツリーほど横スクロールで実際に場所を取る（これが「広く場所を取る」の実体）。
前提→派生の線は、`layoutSkillTreeConnectors()` が render() の直後に前提節と派生節の
実際の位置（`offsetLeft`/`offsetTop`、スクロール量に左右されない）を測り、列の間の余白へ
直角線の SVG `<path>` を引く。選択中の強調（前提ルート＝青・派生先＝緑・それ以外＝淡）も、
インデントのレールではなくこの線自体の色で見せる。

**ハマった点**: `<svg>` に `position:absolute; inset:0` だけを指定しても広がらない。
`width`/`height` を明示しない `<svg>` は既定で 300×150 の内在サイズを持ち、置換要素の
absolute 配置はその内在サイズを優先するため、`inset:0` は無視される（Chromium で確認）。
`width:100%; height:100%` を明示して初めて forest 全体を覆う。

古い `.tree-row` / `.tree-rails`（rail・elbow）は削除し、`.tree-cell` + `.tree-lines` に
置き換えた。座標を組む `skill-tree-layout.mjs` 側（x・y・requires からの森の組み方、
`validateSkillTreeLayout()` の検査）は変更していない。`analysis/ecology-tutorial-trial.mjs`
の「派生の線が引かれている」検査を、レールの elbow 数から `.tree-lines path` の数へ
合わせて直した。

### 3.29 技能ツリーの y、最初の子は親と同じ行を継ぐ（2026-09-05）

3.28 で座標を実線へ結んだ直後、作者から「1つ目の派生を1段下げる必要はない」と指摘された。

```
A-|              A---B---D
  -B-|             |-C---E
     -D               |-F
  -C-|
     -E
     -F
```

左（旧）は、枝分かれしていない一本道（A→B→D）も律儀に1行ずつ下げていたため、
「まっすぐ伸びる一本」と「実際に分かれる場所」が画面上で区別できなかった。右（新）は
最初の子が親と同じ行を継ぐので、一本道は文字どおり一本の水平線になり、行が増えるのは
実際に枝分かれした瞬間だけになる。

`skill-tree-layout.mjs` の `walk()` を直した。`y` を「訪れた順の行番号」ではなく、
外側の可変カウンタ `nextY` から引く値にし、**最初の子だけ親の `y` をそのまま渡し、
二人目以降の子だけ `nextY++` で新しい行を取る**。同じ親の子を再帰で処理する間、
`nextY` は前の兄弟の部分木が使った分だけ既に進んでいるので、次の兄弟は自然に
「前の部分木の直後」から始まる。根も、根どうしで行を共有しないよう `nextY++` から引く。

**「部分木がどこまで使ったか」の数え方が変わった。**旧実装は `y + descendants.length`
（部分木の節数）で部分木の終端行を出せたが、最初の子が行を増やさなくなったので、
節数と行数がもう一致しない。代わりに、部分木が実際に使い切った最終行 `maxY` を
子から親へ畳み込んで求める（配列は親が子より先に入っているので、逆順に読めば
子の `maxY` が先に確定している）。`validateSkillTreeLayout()` の「線の交差」検査も、
最初の子は `child.y === row.y` を許し、二人目以降は `child.y` が前の兄弟の `maxY + 1`
から始まることを見るように直した。

座標を CSS Grid にそのまま渡す `app.js` 側（`grid-column`/`grid-row`）と、実位置を測って
線を引く `layoutSkillTreeConnectors()` は無改修で動く——同じ行を共有する2枚が並ぶだけで、
CSS Grid は列さえ違えば同じ行番号を複数のセルへ割り当てられるためである。ただし
`align-items` の既定値 `stretch` のままだと、同じ行に「選択中で説明が開いた高い節」と
「短い節」が並んだときに短い方まで伸びてしまい、`layoutSkillTreeConnectors()` が測る
中心もズレる。`.skill-tree-forest` に `align-items: start` を足して、各節が自分の
自然な高さのまま上寄せで並ぶようにした。

### 3.30 技能ツリーの線、カード中心ではなく丸印（.node-icon）を測る（2026-09-05）

3.29 を見た作者から「同じ行のはずなのに横線がずれていないか」と指摘された。実際にずれて
いた。原因は `layoutSkillTreeConnectors()` が線の高さを**カード全体の中心**（`offsetHeight`
の半分）で決めていたこと。カードの高さは中身しだいで変わる——分岐バッジ「分岐2」が
付く節と付かない節、バッジが1行で収まる節と2行に折り返す節があり、同じ行を継ぐ最初の子
どうしでも高さが違えば中心もズレる。3.29 で最初の子の `y` を親と揃えても、揃うのは
座標（行）だけで、カードの見た目の中心まで揃う保証は無かった。

線の基準をカードの中心から丸印（`.node-icon`、直径27pxで全節に共通）の中心へ変えた。
丸印はボタンの左上に固定の位置にあり、`align-items: start`（このパッチで `center` から
変更）と合わせれば、右側の説明文が1行だろうと2行だろうと、カード先頭からの位置が
動かない。`layoutSkillTreeConnectors()` は `getBoundingClientRect()` で forest 原点からの
相対位置を測るよう書き直した（`offsetTop`/`offsetLeft` は測る要素の `offsetParent` に
依存するが、丸印は `.tree-cell` の子孫なので `offsetParent` が変わる。`getBoundingClientRect`
の差分ならどの階層の要素でも forest 基準の座標に落とせる）。

これで、同じ行を継ぐ節（＝最初の子として直線で繋がる節）は、カードの高さが違っても
丸印どうしが必ず同じ高さに並び、線がまっすぐになる。

### 3.31 戦闘から報酬までの標準導線を整理した（issue #138、2026-09-05）

R14（3.6）は完全予測をキャンプ全タブの上端へ常設したが、「戦闘前の最終確認」画面
（battlePreview）自体は残っていた。そこにあった EXACT PREVIEW カードは既に消して
あった（同じ数字を二度出さないため）ものの、残った「AUTO BATTLE / PLAN」の内容
——味方の構成、敵の狙い——は、キャンプの戦闘タブ（`renderMap`）が既に出しているものと
そのまま重複していた。加えて、リプレイが最後まで流れきったあとの「結果を見る」、
結果表示のあとの「報酬を見る」という、次へ進むためだけのクリックが毎戦二回必要だった。
勝てる戦闘を繰り返すほど、勝利の手応えより画面遷移の手続きのほうが目立っていた。

- **通常戦は `begin-stage`（「この敵に挑む」）から `battlePreview` を経由せず、直接
  `simulateAndEnterBattle()`（旧 `simulate` action の中身）へ進む。** act boss 前の会話
  がある戦闘は、`enterStory` の `after` を `"battlePreview"` から `"battle"` に変え、
  会話終了後の `finishStory` から同じ `simulateAndEnterBattle()` を呼ぶ。
- **`battlePreview` 画面と `renderBattlePreview` は消した。**当初は灰の門を巻き戻した
  あとの再戦（隊列の直し方を実際に教える一戦。R11 §5 のチュートリアル）と、戦闘エンジンが
  安全弁で停止したあとの構成見直しにだけ残していたが、作者から「チュートリアルも含め、
  常に即戦闘開始でいい」と指摘を受け、`begin-stage` の `state.prologueActive` 分岐ごと消して
  例外を無くした。巻き戻したあとの再戦だけが持っていた手がかり（「同じ影、同じ数。違うのは
  立ち位置だけ」）は、隊列を直す画面（`renderRoster`、編成タブ）へ移した——ちょうど
  プレイヤーがツグミを後列へ動かす、その画面の上に出るようにした。戦闘エンジンの安全弁で
  止まったあとの「戦闘前へ戻る」（`back-battle-preview`）も、確認画面を経由せず直接
  `camp` / `map` タブへ戻すだけにした。「勝敗条件で別ルートを増やさない」（issue の方針）は
  変わらず守っている——分岐点が無くなっただけで、残っていた唯一の分岐（`prologueActive`）も
  勝敗では条件分けしていなかった。
- **リプレイが最後の拍まで進んだら、`scheduleReplayBeat` が結果画面へ自動で遷移する。**
  以前は自動再生が終わっても `state.phase` を `"battle"` のまま止め、「結果を見る」を
  押すまで動かなかった。最後の拍を表示したあと、その拍と同じ長さだけ間を置いてから
  `goToBattleResult()`（旧 `replay-result` handler の中身）を呼ぶよう変えた。**自動再生
  だけの特権にはしていない**——一手ずつ進めて末尾に着いたときも同じタイマーで結果へ
  進む（`scheduleReplayBeat` の「index が末尾」判定を、`state.replayPlaying` の真偽で
  ガードしないようにした）。途中で「結果を見る」を押した場合も同じ `goToBattleResult()`
  を通るので、経路は一本のまま増えていない。
- **通常戦の勝利は、結果画面がそのまま報酬3択を兼ねる。**「報酬を見る」ボタンと
  `reward` phase は廃止し、`renderResult()` が `ensureResultReward()` で報酬候補を
  用意してから `rewardSectionHtml()`（旧 `renderReward()` の中身）をそのまま埋め込む。
  `take-reward` / `reroll-reward` の handler は phase を見ていなかったので変更不要だった。
  最終戦（12戦目）の勝利だけは、以前どおり報酬を出さず精算へ進む。旧いオートセーブが
  ちょうど `reward` phase で保存されていた場合に備え、`hydrateState` で `"reward"` を
  `"result"` へ読み替える一行だけ足した。同様に、`battlePreview` を廃止したあとの
  `hydrateState` にも `"battlePreview"` を `"camp"`（map タブ）へ読み替える一行を足した。

`analysis/ecology-trial.mjs`・`analysis/ecology-tutorial-trial.mjs` の両方を新しい経路
（「この敵に挑む」から直接 `.battle-field` を待つ、勝利の結果画面に報酬3択が同居する、
「報酬を見る」ボタンが存在しないことを確認する）に合わせて更新した。

**追補**——キャンプを下までスクロールした状態で「この敵に挑む」を押すと、盤面
（`battle` 画面の先頭）が画面外のままで、冒頭の動きを見落とすと作者から報告された。
`render()` は毎回全 HTML を差し替えるだけで、ページのスクロール位置には触れていなかった
ため、直前の画面（camp の map タブ）で読んでいたスクロール位置がそのまま残っていた。
`state.phase` が前回の `render()` と変わった呼び出しだけ `window.scrollTo(0, 0)` を呼ぶよう
にした（新しい `lastRenderedPhase` で前回値を覚える）。**同じ画面内の操作では動かさない**
——タブ切り替え・技能選択・報酬引き直しなど、`state.phase` を変えずに `render()` を呼ぶ
操作は多く、毎回動かすとスクロール位置を保ったまま組み替えたい操作まで壊れるため。
`analysis/ecology-trial.mjs` に、下までスクロールしてから「この敵に挑む」を押し、
`window.scrollY` が 0 へ戻ることを確かめる検査を足した。

### 3.32 装備に最低保証と規格外品を入れた（R21、2026-09-05）

生成装備が条件を満たせないと何もせず、高位品でも一つの高品質効果以外が common に沈む問題を
直した。すべての新規生成品へ item rarity と同格の無条件能力上昇を一つ加え、追加効果にも
rarity 別の品質下限と同格効果一つを保証した。trigger・condition・payoff は役割タグで繋ぎ、
condition は1 ruleにつき最大一つへ減らした。payoff は範囲攻撃、多段、貫通、全体防壁、
割り込み軽減、味方への行動点、全体集中、他者回復など25種類へ増やした。

common / rare には、耐久2またはHP15を発火ごとに払う場合だけ最大2段上の効果が付く規格外品を
残した。legendary 以上は発火回数、常時能力、hit数、防壁持続、回復範囲を装備全体で変える
keystone を持ちうるようにし、oopart は必ず一つ持つ。既存 Blueprint は旧 generator version の
定義と descriptor を保持し、新規品だけを現在の generator version で生成する。

### 3.33 回復のtier基準値を抑え、HP消費との組み合わせを整理した（2026-09-06）

回復のtier基準値を `5 / 10 / 30` から `3 / 5 / 12` へ調整した。tierは効果レアリティとは
別の内部強化段階として残し、倍率適用後の端数切り捨てだけを行う。上限は設けず、オーパーツの
tier2が28前後になる幅を残した。また、自分のHPを消費する全コストと自分専用回復を同じ装備へ
組み合わせないようにした。回復の溢れを読む装備triggerは実装しない。

### 3.34 R15 の「調子」の直しを Stage 1〜3 と根城へ広げた（R16、2026-09-06）

§3.8 / §3.10 の直しは **Stage 0 の8断片にしか当たっていなかった。**残り21断片は R13 の
ままで、作者評価は「キャラクターの性格に合っていない／知らないはずの情報を知っている」
だった。数えると出る。

| ゴウ | 行数 | 平均 | 12字以下 |
|---|---:|---:|---:|
| Stage 0（R15で直した） | 22 | 24字 | 8 |
| Stage 1 | 6 | 13字 | 5 |
| Stage 3 | 3 | 14字 | 1 |

R16 のあとは Stage 0 / 1 / 2 / 3 が 23・11・13・6行、平均 26・27・28・28字である。

「ナギ、前。」「断らないから。」「名前は。」「照会は。」「ないな。」は、**§3.8 が
消したはずの寡黙で有能な隊長**である。同じ形で、ナギは19行中9行が「……」始まりで
worry / hurt / shock しか持たず、ゲンゾウは16行中12行が calm だった。設定の**一面だけが
実装されていた。**

**人物ごとに「毎回出る癖」を一つ決めて、21断片へ通した。**ゴウは短く切らず、困ると
逸らす。ツグミは「——」で被せ、言い過ぎて即座に後悔する。ナギは愚痴りながら手だけは
止まらない。ヒバナは返事を待たない。ゲンゾウは敬称と間を保ったまま、甘いものと若者言葉で
崩れる。骨（学習順・footer・pack の対応）は動かしていない。

**知識の矛盾は同じ原因から出ていた。**四件を直し、DESIGN.md §6.4.2 の規約にした。

- `stage_0_prologue_win` … ツグミが自分から「さっきと同じ」と言い、そのうえで**ゴウが
  一度も言っていない「さっき」**を「言いました。二回。」と数えていた。彼女が引用して
  いたのは自分の台詞である。§3.29（巻き戻しの秘密）の仕掛けが、実装で空振りしていた。
  ゴウが漏らし、ツグミが数える形へ直した（三回目でゴウが折れる）。
- `stage_2_join` … 名を聞いた三行あとに照会結果が返っていた。崩れた回廊の只中であり、
  しかも**まだ聞いていない歳**を「この歳の子」と言っていた。投げるのを join、返るのを
  詰所の stageEnd に分けた。歳の申告は根城の `homestead_thick_book` のままである。
- `stage_0_prologue_rewound` … ツグミが敗因（立つ場所）を先に言い当てていた。まだ何も
  起きていない時点で、プレイヤーがこれから学ぶ答えを人物が口にしていた。外した。
  併せて、ゴウが言葉で被せる形（ツグミの癖と逆）をやめ、黙って座らせた。
- `homestead_thick_book` … 加入前のゲンゾウがヒバナの名を書いていた（当初「先月」、R16追補で「先日」）。上の照会が
  彼の机に届いた月として根拠を与え、`stage_3_join` で本人に名乗らせた。これは
  `stage_3_act2`「お兄様は、ご存命ですよ」がなぜ彼から出るかの説明にもなっている。

構造の欠陥も同じ回で潰した。`stage_1_act1` は join の**三戦あと**なのにナギが加入交渉を
やり直していた（Issue #117）。六つ目の椅子が `stage_3_end` と `homestead_six_chairs` で
二度消費されていた。`stage_2_end` / `stage_3_end` の配役から、加入済みのナギが二度とも
落ちていた。`stage_3_join` は `place` が「灰の谷」なのに「扉から。鍵が壊れておりました
ので」と言っていた（根城の戸口へ移した）。兄への助走を `stage_1_act3` に一拍だけ置き
（Issue #118）、ゴウの暗所恐怖を `stage_2_act3` で明かりが離れる形にした（Issue #121）。
「……」始まりは、台詞149行中40行（27%）から221行中43行（19%）へ落ちた。ナギは
9/19（47%）が 7/35（20%）である（Issue #122）。**ゴウの「……」は §3.8 で意図して
足したものなので、検査の対象から外している**（Issue #122 の但し書きどおり、
減らすなら他の四人から先に見る）。

**同じ乖離を三度起こさないために検査を足した**（`ecology/story.test.mjs`、+756 checks）。
誰が何を知っているか、ゴウがどの Stage でも一度は長く喋ること、「……」始まりが台詞の
三分の一を超えないこと、一つの表情が半分を超えないこと、加入場面の一行目は加入する
人物が喋ること（地の文で始めると名前欄が出ず、誰が加わるのか一拍遅れる）。
**台詞に盤面の語（腕力・行動権・反応点・隊列・後列・手番など）が出ないことは、
規約はあったのに検査が無かった。**偶然守れていただけである。

### 3.35 先見機が読めた結果に、人物を驚かせない（R16追補、2026-09-06）

§3.34 で `stage_0_prologue_win` の一行目を「……誰も落ちていません。数も、並びも、
先見機が読んだとおりだったのに。」（shock）へ書き換えた。**これが新しい矛盾だった**
（作者指摘）。

§5 のとおり、戦闘予測は次の一戦の個体・並び・**結果まで**見せる。巻き戻したあとの
一戦には予測が出る（一戦目だけが例外である）。つまり**校正した本人であるツグミは、
数も並びも終わり方も知ったうえで入っている。**驚く理由が無い。

前稿の「敵の数も、並びも、さっきと同じなのに」（＝巻き戻り前の記憶）を潰したとき、
**驚きの理由だけが差し替わらずに残っていた。**同じ行を二度直したことになる。

**戦闘後のツグミにとって、新しい情報はゴウの様子だけである。**一行目を当てた側の確認
（「……当たり前です。校正したのはわたしですので。」）にして、視線をそちらへ移した。
続く追及にも「三分待ったのも、先に中を見せろと言ったのも、今日が初めてです」を足し、
ゴウが小さいほうの咎（「さっき」の言葉尻）へ逃げる形にしてある。

DESIGN.md §6.4.2 に五つ目として書き、`ecology/story.test.mjs` が検査する。予測の出ない
一戦目では驚きが起きることも対で検査した。**この対比が消えると、敗北が §3.8 の言う
「理由の無い敗北」に戻る。**

### 3.36 巻き戻しの露見を、言葉尻の推理から人物の様子へ（R16追補・作者稿、2026-09-06）

§3.35 で直した `stage_0_prologue_win` に、作者から「キャラのイメージと合わない」という
評価と、差し替え案が出た。**採った。**

§3.34 / §3.35 の稿は、ゴウが漏らした「さっき」をツグミが三回数え、ゴウが折れる形だった。
情報の分け方（§6.4）は守れていたが、**十五歳が急に検事になっていた。**設定の
「丁寧語のまま容赦がない」を、冷たさとして実装していた。作者稿のツグミは明るく、軽口を
叩き、そのうえで相手の変化を一発で見抜く。

到達の手がかりを、言葉尻から**ゴウの様子**へ移した。「いつもなら、憎まれ口の一つでも
叩くところなのに」——これ一つで足りる。そしてツグミが直接訊き、**ゴウが認める。**

    ツグミ「目標沈黙。……ふう。先見機で見たとおりでした。ほら、ちゃんと校正しておいて
            よかったでしょ、ゴウ？」
    ゴウ  「……そうだな。お前の言うとおりだ。ありがとな。」
    ツグミ「？ どうしました？ 何か変ですよ。いつもなら、憎まれ口の一つでも叩くところなのに。」
    ゴウ  「うるせえ。さっさと先に進むぞ。」
    ツグミ「……もしかして。「杭」、使いました？ わたし、死にました？」
    ゴウ  「……あー、そうだよ。俺が悪かったんだ。お前は死んだ。杭で戻った。それだけだ。」
    ゴウ  「早く行こうぜ。台帳にも書かねえ。さっさと帰って、全部忘れよう。」

**ゴウはここで折れる。**設定は「本当に素直になるのは、流した先で誰かが傷ついたときだけ。
滅多に無い」で、ツグミが死んだのだからこれがその一度である。§3.8 の「素直は『三分待つ』
一度だけ」は、開幕で理由なくしおらしくなるのを止めるための縛りであって、条件が揃った
場面まで塞ぐものではなかった。DESIGN.md §6.4 に両方書いた。

最後の「全部忘れよう」は**世界の規則を使っている。**外へ出ると遠征の記憶は崩れるので、
彼は嘘をつかずに、この会話が消えることを知って言っている。

`ecology/story.test.mjs` の検査も、数の一致から**情報の分け方の対**へ置き換えた。
巻き戻し直後のゴウは杭と死を言わない（§3.29 から続く検査）／戦闘1後には訊かれて認める。
一行目が予測の確認であること（§3.35）はそのまま残っている。

**隣り合う `stage_0_prologue_rewound` と `stage_1_act1` のツグミも同じ調子へ寄せた。**
「三つとも初めて聞きました」「なぜ怪我をした人と同じ数字になるんですか」は、
作者が R15 で承認した開幕の「もう、ゴウ！ あと二分くらい待てたでしょう！」と
**同じ人物に読めない。**

### 3.32 立ち絵を、生成 SVG から暫定バストアップ画像へ差し替えた（PR #143 続き、2026-09-06）

PR #143 で、会話UI向けの暫定バストアップ5枚（ゴウ／ツグミ／ナギ／ヒバナ／ゲンゾウ、
`docs/art/bustup_v0/*.png`）と、その視覚方向をまとめた `docs/art/CHARACTER_ART.md` が
用意された。以前の立ち絵（`ecology/content/portraits.mjs`）は、決定的・オフライン・
表情差分を安く増やせることを理由に、髪・肌・装いを座標で組み立てる SVG 生成だった
（`docs/DESIGN.md` §10 参照）。作者からの指示で、この見た目の作り込みより実際の
絵の質を優先する方針へ切り替え、生成 SVG をやめて実際の画像を参照する形にした。

- `docs/art/bustup_v0/*.png`（PR #143 の成果物）をそのまま参照する。新しい画像置き場は
  作らず、既存のパスを正本にした。
- `PORTRAITS` から、SVG 生成専用だった `skin` / `hair` / `garb` / `fringe` / `back` /
  `tail` / `shoulder` / `collar` / `accessory` を削除し、`accent`（UI の差し色）と
  `image`（`docs/art/bustup_v0/` 内のファイル名）だけを残した。あわせて `hairMass`、
  `backHair`、`sideTail`、`shoulders`、`collar`、`accessory`、`eyes`、`brows`、
  `mouth`、`blush`、`head` など、SVG のパスを座標計算で組み立てていた内部関数を
  まとめて削除した——使う先が無くなったため。
- `portraitSvg()` は `<svg class="portrait-svg" viewBox="0 0 240 320">` の中に
  `<image>` を1枚敷くだけの薄い関数になった。**呼び出し側との契約は変えていない**
  ——引数の形も、返り値が `<svg ...>` で始まり `</svg>` で終わる決定的な文字列である
  ことも同じなので、`app.js` 側の3箇所（名簿カード、会話画面、もう1つの会話画面）も
  `story.test.mjs` の形状検査もそのまま通る。
- **表情差分は今回作っていない。**表情ごとの絵がまだ無いため、`expression` 引数は
  受け取るが無視し、`EXPRESSIONS` の8キーすべてが同じ1枚を指す。`EXPRESSIONS` 自体は
  「表情 id が実在するか」を検証する側（`beat.mjs`）がまだ使っているため、キーの集合
  だけ残し、中身は空オブジェクトにした。表情ごとの絵が揃ったら、`PORTRAITS` の画像
  参照を表情別の map に分けて対応する。
- `docs/GAME.md` の立ち絵の記述と `ecology/content/portraits.mjs` 冒頭のコメントを、
  画像参照であることが分かる内容に更新した。設計判断そのものの変更点は
  `docs/DESIGN.md` §10 に置いた。

### 3.33 予測と本番の入力を、呼び出し側でも一本にした（issue #148 続き、2026-09-06）

作者から「予測と結果が合っていない気がする。技能レベルの強化が効いていないのでは」
という報告があり、調べ直した。

**直接の原因はすでに直っている。**PR #156 まで、本番の戦闘は `app.js` が
`makeExpeditionBattle` を直接呼んで組み立てており、そこに `skillLevelsFor` が無かった。
技能レベルは予測にだけ乗り、本番の戦闘には乗らない——「予測と結果が合わない」と
「レベルを上げても強くならない」が、**同じ一つの欠落から同時に出ていた。**
現在の main では両方が `simulateExpeditionBattle` を通っており、手元の通しでも
序盤〜第3戦の予測と結果画面のHPは一致する。

問題は、**そのずれが engine 側の検査を一つも鳴らさなかったこと**である。ずれていたのは
engine でも `playable-battles.mjs` でもなく、`app.js` の**呼び出し2箇所のあいだ**だった。
共有関数を一本化しても、呼び出し側が違う options を渡せば同じことが起きる。そこで:

- `app.js` に `expeditionBattleOptions()` を置き、**予測（`previewNextBattle`）も本番
  （`simulateExpeditionBattle`）も、この一つの戻り値をそのまま渡す。**本番が足してよいのは
  結果を変えない `simulationOptions: { captureReplaySnapshots: true }` だけにした。
- `analysis/ecology-screens-smoke.mjs` が、その2箇所が同じ組み立てを通っていること、
  本番が余分な戦闘入力を足していないことを見張る。
- 予測 cache の鍵に `runUnlockedSkills` を足した。技能レベル表は取得表を辿って
  組まれる（`runSkillLevelsFor`）ので、**レベルだけを鍵にすると取得表が動いた回に
  古い予測が残る。**鍵が数えるべき欄も同じ smoke が見張る。
- `content/skill-levels.mjs` の `LEVELED_EFFECTS` と、`effects.mjs` で
  `afterSkillLevel` が実際に掛かる effect 型が一致しているかを
  `analysis/ecology-skill-catalog-smoke.mjs` が見張る。この表がずれると
  「Lv だけ上がって何も強くならない」技能が黙って生まれる——ファイル自身が
  「同じ表でなければならない」と書いていたのに、見張るものが無かった。

**もう一つ、報告の「効いていない」側には、直っていない理由がある。**戦闘予測の帯が
出すのは各人の残 HP と減少量、勝敗、ラウンド数だけで、**攻撃側の伸びは、倒すのに要る
ラウンドが変わらなければ一切表に出ない。**手元の実測（Stage 0 の2人編成、12戦分 ×
装着中の技能）では、1点ぶんの Lv1→Lv2 で帯が動いたのは 36件中3件、序盤の第1戦・
第2戦では一度も動かなかった。engine の側では確かに効いている（`steady_cut` を
Lv1→Lv10 で一撃 65 → 135）。つまり**戻せない技能点を払っても、ゲームの中心の
読み物が「何も変わらない」と言う。**

**当座の対処は、倍率を添えることではなく、説明文の数字そのものを動かすことにした**
（作者指摘）。「確かな斬り」は Lv 1 で「腕力130%の一撃」、Lv 2 で「腕力146%の一撃」と
書かれる。別行に「×1.12」と添えると、読み手が本文の 130% と掛け算する手間が残る。

### 3.33.1 変動量と固定量を、定義の側で分けた（作者指摘、2026-09-06）

最初の実装は、**書かれた数字のどれがレベルで伸びる量かを、あとから推し当てていた。**
説明文の % を集め、定義から引いた係数と一致するものだけを書き換える——「HP30%以下の敵へ
腕力200%」なら 200 だけを伸ばす、という具合である。動きはするが、係数と発動条件がたまたま
同じ数（「HP50%以下の敵へ腕力50%」）になった瞬間に決められなくなる。作者の指摘は
**「初めからスキル定義を変動量と固定量に分けておく」**で、そちらが本筋だった。

分け方はこうなった。

| | 何か | どこに書くか |
|---|---|---|
| 変動量 | その技能の damage / heal / barrier / 増減の amount。レベルで伸びる | **定義だけ。**説明文は `{amount}` と書いて指す |
| 固定量 | 発動条件の閾値、後列減衰、段数、耐久、AP / RP | 説明文に文字で書く |

現行 content を数えたら、**レベルで伸びる量を持つ57技能すべてが、それをちょうど一つしか
持っていなかった。**だから本文は「どの量か」を選ぶ必要すらなく、置き場所だけ書けばよい。
差し込み口は `{amount}`（変動量ひとつぶん）、`{total}`（× 段数）、`{hits}`（段数）の三つで、
**単位（% か素の数）は amount 型が決めるので本文は % すら書かない。**

```
steady_cut: "条件も準備もない、腕力{amount}の一撃。武器なので後列から出すと40%まで落ちる。"
              → Lv1「腕力130%の一撃」／Lv2「腕力146%の一撃」／40% は動かない
```

これで三つが同時に片づいた。

- **推し当てが要らない。**どれが伸びる数かは本文の形で宣言されている。
- **数字が一箇所にしかない。**`coefficientBps` を 13_000 から 17_500 へ変えれば説明文が
  「腕力175%」になる。2026-08-31 に6件出た「係数を変えたのに説明文が旧値のまま」
  （大溜めが実際 1000% なのに「400%」と表示していた）は、**書きようがなくなった。**
- **固定量が巻き込まれない。**40% や 30% はただの文字なので、伸ばしようがない。

`analysis/ecology-readout-smoke.mjs` の技能ぶんは、突き合わせから
**「二重に書いていないか」**（`skillTextIssues`）へ置き換えた。二つの場所に同じ数を書いて
一致を見るより、一箇所にしか書けなくするほうが強い。固定量と、装備・passive の
`statBonus` はこれまでどおり突き合わせる（言い回しが単位ごとに変わるため）。
同じ不変条件は `ecology/phase-b.test.mjs` にも置いてあるので、`node ecology/check.mjs`
だけでも捕まる。

移行は機械的に行い、**57件すべてで Lv1 の表示が1バイトも変わらないことを確かめた**
（例外は余剰治療の一件で、「余剰をそのまま回す」は Lv2 以降で嘘になるため
「余剰の{amount}を…回す」＝ Lv1「余剰の100%を」へ書き直した）。
`*_META` の説明文は**そのまま出せる文字列ではなくなった**ので、content contract の版を
`ecology-content-contract-15` へ上げ、凍結を作り直した。

帯の側で伸びを読ませる（与ダメージ・敵の残り・変更前後の差分）のは issue #148 の本体で、
設計判断が要るのでここでは踏み込んでいない。


### 3.37 技能の前提へ Lv を書けるようにし、勝利ごとの技能点を一箇所へ寄せた（issue #168 / #165 段階1、2026-09-07）

issue #165 は技能体系の全面再設計で、専用 branch（`design/skill-system-reboot-20260907`）
に現状監査と成長案がある。**カタログ本体を書き始める前に置くべき土台**が二つ見つかったので、
盤面を動かさない範囲でそれだけを先に入れた（issue #168）。

**一つ目は、前提に Lv を書けないこと。** ツリーの `requires` は技能 ID の配列で、
「その節を取得しているか」しか表せなかった。#165 の成長案は、系統 Lv や列埋めではなく
個別技能の必要 Lv を推奨している（同じ親から Lv1 で開く横枝と Lv3 で開く深い枝を
分けられる）。`{ skillId, minLv }` へ変え、`maxLv` も節へ出した。

判定を増やすときに一番危ないのは、**判定が四箇所に分かれること**である。解禁 API・画面の
「前提待ち」・加入時の無償閉包・保存の復元がそれぞれ独立に書いていると、片方だけが Lv を
見ない形へ簡単に戻る。そのとき画面は「解禁可能」と出して、押すと断られる。
`prerequisitesMet` 一つを四箇所が読む形にした。

無償閉包は「節を配る」だけでなく「要求された Lv も配る」ようにした。節だけ配って Lv1 に
置くと、加入直後から「取得済みなのに前提 Lv 不足で子が取れない」形が生まれる。

検査は二つ足した。**上限 Lv3 の技能へ Lv5 を要求する子**は永久に開かないので落とす。
そして**技能点の予算**を見る。前提の Lv も値段なので、節の値段を足すだけでは「その節に
何点要るか」が出ない。閉包込みで足し、一遠征ぶんの技能点（現在12点）を超える節を落とす。
現行は Stage 0 で最深3点、Stage 2 以降で最深9点だった。**右端まで初周で取り切れることは
要求していない**（#165 の方針。深い終点は後の進行で届いてよい）。

**二つ目は、同じ一戦から技能点が二度出せたこと。** `app.js` が勝利のたびに
`grantRunSkillPointsToAll(run)` を直接呼んでいた。活動資金の撃破分は
`recordEncounterCleared` が `region:index` を鍵に一度きりにしていたのに、技能点だけが
素通しだった。`grantRunSkillPointsForClear` へ寄せ、鍵を活動資金と揃え、量は
種別ごとの表（`SKILL_POINTS_PER_CLEAR`）から引くようにした。画面の「+n」も同じ表を読む。

**量とツリーは動かしていない。**現行の全前提は Lv1、技能点はどの種別も1点のままである。
SP 曲線（boss を2点にする等）と技能カタログそのものは #165 で比較中の未決事項で、
ここで混ぜると「土台の変更で盤面が変わった」のか切り分けられなくなる。
content contract の差分は `skillTreeNodes` の `requires` の形と `maxLv` の追加だけで、
技能の集合・レベル上限・報酬・合成 encounter は 1 バイトも動いていないことを確かめた。


### 3.38 旧7区画の `ENCOUNTERS` を削除した（issue #173 / #165 段階1、2026-09-07）

#165 の現状監査 §7.1-5 の指摘。遠征の敵配置が `content/encounters.mjs` の `ENCOUNTERS`
（旧7区画）と `content/expedition.mjs` の `EXPEDITION_ENCOUNTERS`（3幕12戦）の二箇所にあり、
`docs/ARCHITECTURE.md` は前者を「既存 content API の正本」と書いていた。実態を追うと、
本編（`progression.composeEncounter` 以下の全経路）が読むのは12戦のほうだけで、7区画は
`ecology/termination.test.mjs` と `analysis/ecology-equipment-gen-smoke.mjs` の二箇所しか
読んでいなかった。

最初は fixture として場所だけ変える案（`ecology/fixture-stage-encounters.mjs` へ移す）で
入れたが、作者から「消して構わない」との指摘を受けて見直した。二箇所の読み手を洗うと、
どちらも `ENCOUNTERS` という表そのものは要らず、`playable-battles.mjs` の
`makeBattle` / `encounterInfo` / `allEncounters`（`encounterLabel` / `stageRule` は
呼び先ゼロの死んだ export だった）を経由していただけだと分かったので、**表と、それを
読むためだけに存在した関数を丸ごと削除した。**

- `ecology/termination.test.mjs`（issue #130 の event budget 検査）は、`makeBattle(7, …)`
  が返す5体の敵（`ash_core` / `gray_breaker` / `gray_shelter` / `gray_stalker` /
  `gray_swarm`）をどのみち stats・hp とも上書きしていた。同じ5体・同じ配置をテスト内
  `ISSUE_130_ENEMIES` に直書きし、`makeExpeditionBattle` へそのまま渡す形にした。
  **遠征の敵編成に依存させない**という元の対案Bへの懸念（「有限に終了すること」を見る
  検査が遠征の今のチューニングに化ける）は、この形なら発生しない。ENCOUNTERS 経由でも
  EXPEDITION_ENCOUNTERS 経由でもなく、検査そのものが持つ固定入力になったからである。
- `analysis/ecology-equipment-gen-smoke.mjs`（装備トリガーの発火可能性を見る診断）は
  逆に、**本物の盤面で試すほうが診断の趣旨に合っている**と判断した。`composeEncounter`
  で `EXPEDITION_ENCOUNTERS` の index 1・7・12（序盤2体・中盤elite・最終boss）を組み、
  `makeExpeditionBattle` で戦闘入力にした。旧7区画より役割の幅が広く、プレイヤーが
  実際に見る盤面でトリガーを鳴らせるかを確かめられる。

結果として `content/encounters.mjs` に残るのは `ENEMY_LORE` / `ENEMY_CODEX` /
`ENEMY_TARGETING`（敵本文・狙いの正本）だけになった。`ENCOUNTERS` は content contract の
凍結対象ではなく（`contract-snapshot.mjs` は `EXPEDITION_ENCOUNTERS` しか読まない）、
`ecology/contract-snapshot.json` に差分は出ていない。`ENEMY_CODEX` / `ENEMY_LORE` /
`ENEMY_TARGETING` の参照経路（`content/index.mjs` → `content/world-lore.mjs` /
`playable-battles.mjs`）は変更していない。

### 3.39 Campaign Stage の ID を連番へ改名した（issue #172、2026-09-07）

#165 の現状監査 §3.3 / §7.1-2 の指摘。`content/campaign-stages.mjs` の Stage `id`
（`stage_0_edge` 等）は語尾が pack 名を示しているつもりだったが、途中で pack の追加順
（care → edge → wall → tempo）を変えたときに ID を追随させず、**「その Stage で追加
される pack」と一つずつずれていた。**書き間違いではなく更新漏れであり、段階2以降で
Stage ごとの技能配分を触るとき、ID を手掛かりに読むと必ず間違える状態だった。

issue は「(推奨) 保持して意味を持たないと明記する」と「(対案) 改名して migration を
書く」の二案を出し、作者は対案を選んだ。**pack の追加順は今後も変わり得るので、ID に
特定の pack を持たせたくない**という理由による。

- `id` を `stage_0` 〜 `stage_3` の連番へ改名した。**表示名（灰の入口／抜ける刃／
  動く隊列／間合いと順番）は変えていない。**
- 改名前の ID は `RETIRED_CAMPAIGN_STAGE_IDS`（`campaign-stages.mjs`）へ理由・
  sequence・displayName を残した。`NAMED_SECTIONS`（characters / activeSkills 等）
  の `RETIRED_IDS` とは別の登録簿にした。Campaign Stage は `content/index.mjs` の
  `NAMED_SECTIONS` に含まれず、`analysis/ecology-contract-smoke.mjs` の凍結 ID
  照合（既存の `RETIRED_IDS`）の対象にもなっていないため。
- 保存済みデータで旧 ID を持ちうるのは Blueprint 取得履歴・装備 provenance の
  `campaignStageId` フィールドだけだった（`blueprints.mjs` の `saveBlueprint` /
  `progression.mjs` の `generatedRewardCandidate`）。**ゲームの進行そのもの
  （`profile.campaignProgress`、`run.campaignStageSequence`）は元々 ID ではなく
  `sequence`（数）で保存しており、この改名の影響を受けない。**
- 由来表示（`app.js` の `blueprintOriginText`）だけが `CAMPAIGN_STAGE_BY_ID` を
  ID で引いていたので、新旧どちらの ID からも displayName を返す
  `campaignStageDisplayNameFor()` を新設して差し替えた。旧 ID で保存された
  Blueprint でも、由来の Stage 名が消えない。
- `STORY_BEATS`（`content/story.mjs`）のキーと `app.js` の `storyBeat("stage_0", …)`
  呼び出し4箇所も同じ連番へ揃えた。物語の会話断片 ID（`dialogue.mjs` の
  `stage_0_act1` 等）はもともと連番プレフィックスだったので、`STORY_BEATS` の
  キーがようやくそれと一致した形になる。
- `ecology/contract-snapshot.json` は Campaign Stage の manifest を持たない
  （`manifests` に載るのは Free/Endless の `frontier-*` だけで、`campaignStageId`
  は全て `null`）ため、**差分は出ていない。**

### 3.40 技能の表示分類名をアクティブ／リアクティブ／パッシブへ統一した（issue #171、2026-09-07）

画面の技能分類を「アクティブ／リアクティブ／パッシブ」に統一した。「常設」は表示名から廃止し、
ツリーのタブ、技能欄、節のバッジ、説明文、検証メッセージも同じ語彙を使う。
一方、内部の `kind` 値（`active` / `reactive` / `passive`）、技能 ID、pack ID、
セーブ形式、CSS クラス名は変更していない。表示名の変更と内部 ID の変更は別物として扱う。

### 3.41 #174 技能点曲線と初期SPアップを確定した（2026-09-07）

Issue #174 は案bを採択し、通常戦1点・精鋭戦1点・boss戦2点とした。精鋭戦は通常戦と同じ報酬量に据え置き、bossを到達点として重くする。現行の12戦（通常6・精鋭3・boss3）は全勝で15点、最後のboss直前までで13点になる。勝利報酬の表と encounter ごとの冪等鍵は ecology/progression.mjs に集約し、ecology/phase-b.test.mjs で種別ごとの値と合計を固定した。

永続強化 starting_skill_points（表示名「初期SPアップ」）を追加した。活動資金で最大3段を購入でき、各段階は新しい遠征の開始技能点を+1する。購入済みの値は ProfileState に残り、newRun の開始時だけ RunState へ写す。途中加入者や進行中の遠征へ遡及せず、遠征中の技能点を永続化もしない。

高難度案として、将来の追加 Stage で通常戦2点・精鋭戦3点を検討することを設計資料へ記録した。現行 Stage には適用せず、今回の変更でboss報酬もこれ以上は動かしていない。

### 3.42 Stage 3 の三構成検査を立ち上げる前提を整理した（issue #176 / #165 段階2、2026-09-08）

#165 段階2の関門は、**Stage 3（5人・4pack）で三つの異なる構成が第4〜6戦までに動く**こと、
失敗の定義は「全構成が同じ主攻撃+回復で倍率だけ違う」ことである。#176 の完了条件もこの検査対象へ
読み替えた。序盤の2人編成は導入として扱い、三構成の差を測る対象にはしない。

そのため、検査用の3構成を「刃で削る」「隊列で守る」「順番を作る」として組み直した。
構成ごとに取得計画・固有の核・代替入口を持たせ、同じ seed と同じ敵で event の種類が変わることを
見る。`hp_percent_asc` / `hp_percent_desc` は、最大HPの違う5人を「残りHP」ではなく傷の割合で
支えるための入口である。

`analysis/ecology-stage3-builds.mjs` は、Stage 3 の manifest と技能点予算、核が第4〜6戦で
取り切れること、各構成に代替入口が2つ以上あること、event 列の種類が異なること、三構成が
第6戦まで到達することを、`simulateNextBattle`（preview と本番が通る同じ経路）で確かめる。

**数値はまだ soft data である。**現状の通し結果と、三構成の新鮮さ・分かりやすさ・育成の手応えは、
6戦後のバランス確認と作者試遊（`05-playtest-gates` の関門C）で評価してから動かす。
### 3.43 狙い先を残りHPへ変え、Stage 0 は導入へ戻した（issue #176 続き、作者指摘、2026-09-08）

3.42 を作者へ渡したところ、四点の指摘が返ってきた。**うち一つは 3.42 の判断そのものの
差し戻しで、一つは前から在った欠陥の発見だった。**

**1. 敵の狙い先が行の先頭固定だった。**作者の指摘は「ツグミを前列に出しても狙われない。
前列右に置くのが最適解になっている」。実測すると、そのとおりで、しかも原因は 3.42 の
変更ではなく**前から在った** `position_asc` 固定である。

| 盤面 | 観測 |
|---|---|
| Stage 0・2人・6戦 | ツグミ後列右で被害 132、**前列右・前列中では被害 0** |
| Stage 3・5人・6戦 | `warden 744 / guardian 430 / tactician 350 / lancer 270`、**mender（後列右）は 0** |

**殴られるのは前列左と後列左だけ**で、主火力の既定位置（後列右）は全ステージで安全地帯だった。
前列と後列の選択が「どこに置くと安全か」ではなく「左端を避けるか」になっていて、隊列の話が
成立していない。作者の案（攻撃は絶対量、庇護は割合）を採り、`front_strike` / `rear_strike` /
`enemy_heavy` を `hp_asc` へ変えた。武器の敵は前列が生きているあいだ前列しか狙えないので、
**「後列へ下げれば届かないが、武器の威力も落ちる」**という R11 §5 の交換がそのまま出る。
直後の実測では、ツグミを前へ出すと6戦の被害が 0 → 224 になり、第6戦で敗ける。
言葉も分けた——**「最もHPの低い相手」＝残りHP（攻撃）／「最も傷ついた味方」＝傷の割合（庇護）**。

**2. Stage 0 の敵編成の変更を撤回した。**作者判断は「最初の2人ステージは単純に勝てるように
すべき」。3.42 は後列を狙う敵を Stage 0 へ残すことで構成の差を作ったが、それは**盤面を
難しくする方向でしか差を作れていない。**`composeEncounter` を元の「後ろの枠から落とす」へ戻した。
少人数 Stage で前から来る敵だけになるのは、いまは欠陥ではなく導入の形である。前列と後列の
選択そのものは、1. の狙い先が担う。

**3. 三構成の検証を Stage 3（5人・4pack）へ移した。**作者判断。旧三構成検査を `ecology-stage3-builds.mjs` へ整理し、構成を組み直した。

| 構成 | 主な出来事 | 核 | 固有の event 種 |
|---|---|---:|---:|
| 刃で削る | 受け崩し・貫き・怯み | 第5戦 | 44 |
| 隊列で守る | 受け構えで止めた拍 → 盾と集中 | 第5戦 | 78 |
| 順番を作る | 行動権を渡し、準備1回の大技を毎ラウンド完成させる | 第5戦 | 36 |

組む途中で**溜め技（`heavy_swing`）が技（focus）で伸びる**ことに突き当たった。腕力50・技術6の
ゴウが溜めても damage は guard で消える。溜め手は技術52のツグミで、`hasten_ally`（隊列の最後へ
行動権）が彼女へ渡す形になる。**検査が「勝ち切れるか」も見ているので、この取り違えは
黙って通らなかった**（第4戦で round 上限に当たって落ちた）。

**4.「守勢って何でしたっけ」。**技能の説明文には「守勢を1つ」としか書いておらず、状態そのものの
意味は**どこにも書かれていなかった。**`content/statuses.mjs` に `STATUS_GLOSSARY` を置き、
技能タブと戦闘画面のヘルプへ「状態（バフ・デバフ）の意味」を足した。段数・持続・向きは定義から
引くので、効果を変えれば説明も動く。`analysis/ecology-readout-smoke.mjs` が、全状態に一行が
あることと、書いた「1段につきN」が定義の量と一致することを見張る。

**5.「2つセットしても片方しか使っていない」。**そのとおりだったので、`chooseTactic` に
**アクターごとのカーソル**を持たせた。現在位置から登録順に循環して読み、固有条件・発動条件・
対象・コストのどれかを満たさない技能はスキップする。選んだ技能の次へカーソルを進め、使える
技能が無い activation では進めない。無条件の技能 A / B は A → B → A と回り、先頭の条件付き
技能が未達でも後続の技能を試す。技能のルールのヘルプと装着行の**無条件／条件つき**表示も
この挙動に合わせた。

`contentVersion` は 0.15 のまま据え置く。0.15 は 3.42 の時点で公開されておらず、この差分も
同じ一本の作業の続きだからである。

### 3.44 作者試遊の一巡目を受けて、検査へ「鳴らない節」を足した（issue #176 続き、2026-09-08）

Stage 3 を通しで遊んだ所見が PR #186 に入った。**主張はほぼ全て実測で再現でき、うち一つは
私自身が組んだ構成の欠陥だった。**

| 所見 | 実測（Stage 3・5人） | 判定 |
|---|---|---|
| 「余りを溜める」は条件を満たす局面が無さそう | 8戦で `resource_unused` は反応点 58回・**行動権 0回** | **到達不能**。engine のフェーズが「使える行動がある限り」回り、通常攻撃が常に出せる |
| 「先に手を打つ」が効いていない | 毎戦1回、確実に発火 | 発火はしている。付与するのが**守勢＝ラウンドで消える**ので、1ラウンド目しか効かない |
| 「身代わり」が効いていない | 69回発火 | 効いている。見えていないだけ（次行と同根） |
| 追い打ちがよく不発／防壁で防ぐと攻撃ログが飛ぶ | `damage_proposed` 106回のうち**20回が帰結 event を一つも出さない** | HP が減らなければ `damage_taken` を出さないので、吸い切った攻撃は盤面にもログにも残らない（#192 / #193） |
| 回復が実被害を超えていそう | 被弾を含む15 chain 中**3 chain で回復＞被害**（最悪 被害23 に対し 回復26） | limit が `actor-instance + rule` なので、5人が全員「手当て」を持つと同じ一撃を人数分だけ治す（#189） |
| 敵アクションがログに残らない | イベント列には `action_declared` が76件 | 表示側の欠落（#192） |

engine とイベント語彙に触る3件（行動権の余り・回復の重ね過ぎ・帰結 event の欠落）は、
**#191 / #189 / #192・#193 で別 PR にする**（作者判断）。#176 の縦切りとは独立で、特に
回復の重ね過ぎは anti-stall の不変条件に触るため、専用の検査を付けて単独で入れる。

このPRで直したのは一つだけである。**「順番を作る」構成が、その到達不能な「余りを溜める」を
実際に買っていた。**「Stage 0 の入口には鳴る節だけを置く」と 3.43 で書いておきながら、
自分の取得計画では守っていない。`analysis/ecology-stage3-builds.mjs` へ
「買った節が6戦のあいだに一度は鳴る」検査を足し（rule を持つ節だけを見る）、
その点を溜め手の技能レベルへ回した。**検査は追加した直後に、私の構成を落とした。**

あわせて、#188 のラウンドロビン化に追随していなかった `docs/GAME.md` と `docs/DESIGN.md`
を現在形へ直した（8.7.5）。

**数値の所見は、この PR では動かしていない**（作者判断）。いまの三構成検査は「差があること」を
守っているだけで、強さは守っていない。ここで数値を触ると、次の試遊で「何を直したのか」の
比較対象が消える。未処理のまま残すのは次の通りで、#176 に一覧を置いた。

- **初見難度が両端に割れている。**装備0・技能点余りで第12戦を14ラウンド勝利する一方、
  無投資の既定編成は第7戦で全滅する。技能点の供給・技能価格・敵の threat のどれを動かすかは
  測ってから決める。
- 強く見える: 号令・急かす・盾の列。弱く見える: 隙を刻む・意趣返し・灰の核心。
- 前列と交代する技能は一度動くと戻れず、行動権を払う意味が薄い。相手を動かす技能も、
  次の攻撃（列・行）へ繋がる形がまだ無い。

一時導線（タイトル画面の `debug-stage`）は作者試遊のあいだだけ残し、**merge 前に外した。**
消し忘れないよう、`ecology/app.js` の `TEMPORARY_DEBUG_ENTRIES` と `docs/OPERATIONS.md`
§3.1 の表を `analysis/ecology-screens-smoke.mjs` が突き合わせる（片方だけ消すと落ちる）。
仕組みは空の配列と空の表として残してあるので、次に一時導線を足すときも同じ挟み方になる。

### 3.45 issue #192 — 敵の行動と0ダメージの因果を戦闘履歴へ戻した（2026-09-08）

作者試遊で、敵が行動しているのに戦闘履歴から `action_declared` などが見えず、防壁が攻撃を
吸い切ると「攻撃そのものが無かった」ように見える問題が出た。前者は engine のイベント列ではなく、
`app.js` の手書き replay whitelist が schema の一部を落としていた。後者は `damage_taken` が
HP減少時だけ出る既存契約に対して、吸収の事実と最終ダメージ0を別イベントで表していなかった。

`RESULT_SCHEMA_VERSION` を `ecology-result-3` へ上げ、`damage_absorbed` と `damage_skipped` を
反応しない結果イベントとして追加した。履歴／replay の入力型は `EVENT_TYPES` から導出し、
`action_canceled`、対象選択、資源消費、防壁・受け構えの記録も敵味方を問わず表示する。
防壁吸収は `damage_proposed` → `barrier_damaged` / `barrier_broken` → `damage_absorbed`
（`finalDamage: 0` を含む）、対象消失は `damage_skipped` の理由で追跡できる。

### 3.46 通常画面から内部版数と自明な予測文を外した（issue #205、2026-09-09）

全画面を包む `shell()` が左上へゲーム版数、下端へ build・run・seed・rule version を常時
出していた。これらはプレイヤーの判断材料ではないため通常 chrome から外し、build 情報は
戦闘履歴内の折り畳まれた技術ログへ移した。自動 E2E が公開 build を照合する非表示の
`build-stamp` は維持する。

戦闘予測は勝敗・ラウンド数・各人物のHPを残し、「この構成のままなら、この通りに終わります」
「装備は付け替え自由」という結果と周辺UIから自明な二文を削除した。チュートリアル本文には
触れていない。共有 shell と予測関数を静的に検査し、タイトル・キャンプ・戦闘・結果・精算へ
内部表示が戻らないようにした。

### 3.47 Campaignの物語イベントを再訪でも同じ表示にした（2026-09-08）

#213 では、既読 Stage の opening / join / 幕の断片 / stageEnd を省略し、初回完走後だけ
stageEnd を表示する分岐を入れた。作者判断でこの再訪差分を取り下げ、Campaign の通常イベントは
同じ Stage を何度遊んでも同じ本文・演出で再生する仕様へ戻した。

実装では `isCampaignStageCleared` を Stage 選択カードの情報表示にだけ残し、物語イベントの
表示条件から外した。stageEnd は完走ごとに通常の一行送りへ入り、読み終えるか SKIP すると
精算へ戻る。Stage 0 の序盤の敗北・巻き戻しと補給案内だけは、専用チュートリアルとして
初回の導線を維持する。

### 3.47 生成装備の発火効果へ耐久消費を統一した（issue #210、2026-09-09）

generatorがcostを任意選択していたため、耐久表示があっても摩耗しない生成装備が存在した。
generator versionを7、content contractを18へ上げ、修理以外の全ruleへ耐久costを一つ付けた。単体・単効果は1、
複数効果・多段・範囲は2を消費する。affixの追加costは併用でき、既存engineのatomic cost処理、
`equipment_worn`、耐久0でrule供給を止める経路をそのまま使う。

修理ruleは自己相殺を避けて耐久costの例外とし、HPまたは防壁の有限costを必須にした。
常時stat bonusはrule外のため摩耗しない。生成監査・Phase C回帰・画面smokeを更新し、
カード説明と戦闘ログに消費量・残耐久・耐久切れ後の不発を表示した。

### 3.48 防壁をHPバー上へ可視化した（issue #208、2026-09-09）

戦闘中の防壁値は数値だけではHPに対する残量を読み取りにくかったため、盤面のHPバー上端へ灰色の帯を追加した。帯の幅は `barrier / maxHP` を100%で上限にし、防壁0では表示しない。数値マークは補助として残し、replay snapshotの更新をそのまま読むことで、付与・吸収・破壊・期限切れの各拍でHPと防壁を同時に確認できるようにした。


### 3.49 build印をデプロイ時生成へ移した（2026-09-09）

これまでは、公開物を変更するたびに `analysis/stamp.mjs` が時刻付きの `core/build.mjs` を生成し、そのファイルをcommitしていた。build印は公開E2Eが古いpreviewを検査しないため、またD1のプレイ記録をどの版か照合するために必要だったが、生成物をtrackedにしたことで、複数PRが同じ1ファイルを更新し続けていた。

`core/build.mjs` を安定したloaderにし、Pages buildがコミットSHAだけを無視対象の `core/build.generated.mjs` へ出力する方式へ変更した。ルール契約の `FINGERPRINT` はcontent定義から直接読み、build識別子と分離した。これでbuild印の用途を残したまま、時刻差分とbuild印だけのコンフリクトをなくした。

### 3.50 戦闘マップの現在地強調を種別表示から分離した（issue #203、2026-09-09）

精鋭・bossノードが種別ごとの枠色を持っていたため、現在地の強調と競合し、どこにいるのかが
一目で分かりにくかった。ノードの状態をクリア済み・現在地・未到達へ明示的に分け、強い金色の
枠は現在地だけに限定した。精鋭は `◆`、bossは `★` の小さな記号と凡例で示し、種別を残しつつ
現在地との意味を混ぜないようにした。表示上の12戦構成と現在地の移動は画面スモークで確認する。

### 3.51 段階2の縦切りを閉じ、通しの物差しへ引き継いだ（issue #176 → #230、2026-09-09）

issue #176（#165 段階2）は PR #186 で main に入り、作者試遊の一巡目まで終わっていた。守れているのは
**三構成が同じ seed・同じ敵で別物になること**で（共通83種に対し固有 43 / 83 / 22種、核は三構成とも
第5戦、前提Lv `sustaining_ward` が実データを通る）、守れていないのは**その後半**である。

取得計画が第6戦の開始前までしか書かれていないため、`STAGE_BUILDS_LAST=12` で通しに広げると
三構成とも第7戦で敗北する。一遠征15点に対し宣言済みは約7点で、残り約8点の使い道が未設計である。
一方で作者試遊は装備0・技能点余りで第12戦を14ラウンドで勝ち、`ecology-trial` の既定編成
（ほぼ無投資）は第8戦で敗北する。**同じ Stage 3 の両端を、いまの検査はどちらも見ていない。**

#176 を完了として閉じ、通し（第12戦）での成立と難度の余白の測定を #230 へ移した。数値の作り直しは
引き続き #150 / #189 / #128 / #190 / #191 / #149 が持つ。**物差しが無いうちに数値を動かすと、直ったか
どうかを判定できない**という PR #186 の判断をそのまま引き継いでいる。

あわせて `docs/skill-reboot/05-playtest-gates.md` と `06-implementation-plan.md` に、段階2の場所が
Stage 0 の2人から Stage 3 の5人へ移った採用済みの変更を追記した。両文書は再開前の候補を含む設計記録
なので、元の記述は書き換えず追記だけを足している。

### 3.52 縦切りを12戦の通しへ広げ、余白を測れるようにした（issue #230、2026-09-09）

#176 の縦切りは第6戦で止まっていた。取得計画がそこまでしか無いので、第7戦から先は
「点を余らせたまま進む」測定になり、三構成とも第7戦で全滅していた。遠征は12戦で一つの
単位なので、`analysis/ecology-stage3-builds.mjs` を通しへ広げた。

一人ぶんの技能点は第12戦の開始前に13点そろう（通常1・幕ボス2）。三構成とも、その13点を
2,3,4,5,5,6,7,8,9,9,10,11,12 の順で使い切る取得計画へ書き直した。戦闘のあいだも本編と
同じ形で通す——報酬は毎回「補給1」を取り（装備は取らない。作者試遊の装備 0/10 と同じ条件）、
倒れた味方から順に蘇生し、生存者が7割を切ったら集中治療する。補給は一戦に1つ・上限5なので、
無限の回復ではなく数えられる余白になる。

第7戦から先の勝敗は**測る対象**であって、負けたこと自体は失敗ではない。失敗は「前に測った
値と違うのに理由が書かれていない」ことなので、各構成は `through` に実測（到達した戦闘と
終わり方）を持ち、そこからずれたら検査が落ちる。核の関門（第6戦）までは #176 のまま
「紙の上だけの構成を残さない」を守る。

実測は 刃で削る 第9戦（時間切れ）・隊列で守る 第6戦（時間切れ）・順番を作る 第12戦（完走）。
**同じ13点・同じ盤面で、到達が6戦・9戦・12戦に分かれる。**負け方はどちらも全滅ではなく
ラウンド上限で、刃で削るの第10戦は敵の総HP1632に対して1619を出して止まっている。

名指しされた技能の実測（三構成の通し合計）も出すようにした。号令42回・行動権42、
急かす30回・準備30段、盾の列27回・防壁420、隙を刻む221回・隙221段、意趣返し8回・damage26。
**行動権を渡す一回は味方の一撃まるごと**で、**隙や集中の+10は一撃50〜100に対して小さい。**
判定を受けた作り直しは #150 / #189 / #128 が持つ。

あわせて、通り道として買った節を「鳴らない節」の対象から人物ごとに外した。前提まで装着
しないと落ちる形は、順送りで主砲の出番を割る方向へ圧力をかけていた（DESIGN 8.7.4.2）。
ゲーム内容は変更していない（`analysis/` のみ。contract-snapshot に差分は無い）。

### 3.53 HPゲージの内訳と低HP警告を分離した（issue #226、2026-09-09）

#221／PR #225で追加した緑・濃い緑・赤・黒の4区分へ、別の低HP枠色を重ねると画面が忙しくなる。
そこでゲージ内の意味と残りHPの色を一つの軸へまとめ、生存中の現在HPが56%以上なら緑、26〜55%なら黄、
25%以下なら赤とした。残HPは主色、回復済みは主色の薄め、回復可能は主色のかなり暗め、回復不能は黒とする。
HPによってカード枠は変えず、従来の行動・狙い・被弾などの枠表示を維持する。

区分の境界は直角、左端は丸角、赤がある場合の赤の右端（赤／黒境界）は丸角とした。赤が無い場合は
黒の手前の最後の非黒区分を丸め、全損時だけ黒を外側として丸める。4区分・色帯・ARIA説明・角丸対象を
`ecology/hp-gauge.mjs` へ集約し、リプレイ snapshotの状態遷移と画面検証が同じ契約を読むようにした。

### 3.53 技能画面を文章から記号へ移した（issue #177、2026-09-09）

「通常のゲームシステムの説明は文章以外で表すべきで、文字はストーリーと技能の説明文に
だけ置く」という作者方針を受けて、技能画面の読み方を作り直した。

節の上にあった5つの文字札（種別・消費・対象／発動条件・現在Lv／最大Lv・状態）を外した。

**初稿は記号へ寄せすぎて読めなくなった**ので、作者の指摘を受けて三点を戻している。
(1) **丸は「払うもの」だけに使う**——初稿では丸が行動点・反応点・代償HP・条件の有無・
取得状態の五つを表していた。取得状態は鉤（✓）、入切は摘み（スイッチ）へ移した。
(2) **段は `Lv1/10` の文字**——ほとんどの節が Lv1 なので、10個の目盛りに1個だけ塗る形は
模様にしかならない。(3) **量は `腕 65` の色つき小チップ**——棒は「どちらが大きいか」しか
出さず、節をまたいで比べるには桁が要る。**発動条件は技能名の下の短い薄字**にした
（「ありかなしか」だけでは解像度が低い）。意味の対応表は折り畳み「記号の意味」に一度だけ置く。

**量のチップは issue #230 の発見をそのまま画面にしたものである。**ゴウの手では
「確かな斬り」が `腕 65`、「溜め突き」が `技 33` と並び、「前提として買った節を装着すると
火力が落ちる」が取得前に桁で見える。計算は `content/skill-levels.mjs` の `leveledEffectOf` /
`leveledValueAt` へ置き、説明文の差し込みと同じ一箇所から引く。発動条件の文も
`intrinsicPredicates` と対象の絞り込みから組み立てるので、定義を変えれば表示も動く。

装着行には「出番」の目盛り（有効な装着本数のうちの一本）と、見出しに1ラウンドで払える点を
出した。反応点が尽きる行には印が付く。戦闘後の結果画面には**「出した順番」の帯**を足し、
誰がどのラウンドに何を出したかをテーマの色と印で並べた（動かなかった拍は点線の枠）。
テーマの絞り込みも文字ボタンではなく色と印の丸にした。

立ち位置は「前列左」ではなく2×3の枠の塗り、HPは棒にした。ビルドの要約からは技能名の
繰り返しを外し、資源と装着数だけにした。**読み上げのために `aria-label` と `title` は
全部の記号へ付けている。**両 trial に、目盛り・量の棒・テーマ絞り込み・順番の帯・
反応点の見出しを実際に読む検証を足した（tutorial 130 / trial 111）。

### 3.54 技能の効果量と取得コストの意味を整理した（issue #177、2026-09-09）

技能画面の量チップは、キャラクターの能力値を掛けた後の実数（例: ゴウの腕力50に
腕力130%を掛けた `腕 65`）ではなく、**掛ける前の技能効果（`腕 130%`）**を表示する形へ
直した。これで「技術が低いから技術技能は弱い」と画面が先回りせず、係数と人物の能力値を
見て優先順位を考えられる。詳細欄の人物別最終値は削除し、技能の説明文だけを残した。

取得コストは取得済みのチェックと同じ実線四角の数字へ統一した。前提がある未取得節では、
前提を満たすまでのコストを破線四角、`+`、その節の取得コストを実線四角で並べ、前提と
最後の取得費用を同じ場所で読めるようにした。`Lv1/10` は小さい数字も縦中央に揃えた。

### 3.55 キャンプの仲間表示を一つの盤面へ統合した（issue #159、2026-09-09）

キャンプで同じ仲間を選ぶ表示が四つ、見るだけの一覧が二つあった。**上端の予測を、`POSITIONS`
そのままの3列×2行の共通盤面へ置き換え、編成・スキル・装備・補給の人物操作を全部そこへ
接続した。**行の見出しと格子は戦闘中の盤面と同じにしてある。

消したもの: 編成タブの隊列盤（`formation-board` / `formation-slot`）と「今回の同行者」の
チェックマーク付きカード、技能タブと装備タブの仲間タブ（`memberTabs`）、立ち位置とHPの
小さな印（`formationMark` / `hpMark`——盤面がその二つを兼ねる）、治療専用の対象一覧
（`treatment-target-picker`）、戦闘タブの「現在の隊列」。

足したもの: `partyBar()` / `partyCell()` / `partyCellPerson()` / `partyCellRole()`。
mode は当時は表示中のタブで、編成は `place-character`、スキルと装備は `select-character`、
補給は治療を選んでいるあいだだけ `select-treatment-target`、戦闘タブは操作なしだった
（issue #235 で編成タブを廃止し、いまは `boardMode()` が盤面の状態から決める。3.58）。
盤面が隊列を変える唯一の入口である、という性質は変えていない。

予測の経路（`previewNextBattle` → `expeditionBattleOptions`）と数値は変えていない。
**予測が出ない灰の門の一戦でも盤面は描き**、勝敗・ラウンド数・開始→終了HPの帯だけを落とす。

集計値も同じ主語へ寄せた。編成タブの人数は固定同行者の区画で分母を出さず、スキルタブは
隊全体の残り技能点、装備タブは装着済みの総数を「手元の装備数と枠の総数の小さいほう」で
割った比にした。隊列の選択は誰も選んでいない状態で開くようにした（保存から戻る経路も同じ）。

検査は `analysis/ecology-screens-smoke.mjs` に片側検査（二つ目の仲間選択が戻っていないこと・
隊列の選択が先頭で初期化されていないこと・予測セルのDOM名が画面とCSSの両方にあること）を足し、
tutorial trial へ隊列交換・技能／装備の対象切替・補給タブの不活性・集中治療と蘇生の対象選択を、
trial へ盤面の形と iPhone 幅での収まりを足した（tutorial 150 / trial 113）。

### 3.56 必殺技を、専用技能ではなく既存技能への変換規則として作った（issue #238、2026-09-10）

作者の要望は「これまで見向きもしていなかったスキルが、必殺技になると輝く」だった。
専用の必殺技を人数ぶん書くと、いずれ**必殺技の表**ができて、そこに載っている技能が
最適解になる。AGENTS.md が禁じている「固有 ID の相方で面白さを作る」形なので採らなかった。

代わりに、**取得済みの技能に掛ける変換規則を一つだけ**置いた（`ecology/ultimates.mjs`）。
単体は全体になり、広がれない効果は量が2倍になり、溜めは消える。AP・RP・hit 数・耐久・
行動権は動かさない。だから「行動権を渡すだけ」「位置を替えるだけ」の技能は必殺にできず、
109本のうち19本が候補から外れる。**外れる技能が残ること自体が、必殺化が一律の強化装置に
なっていない証拠**である。

**広さと太さを両立させないのは実測の結果である。**最初は両方を掛けていて、第6戦が
8ラウンド・被害413から2ラウンド・被害12へ落ちた（5体×2倍＝10倍の一手になる）。
片方だけにすると、第11・12戦が「負け」から「勝ち」へ変わりつつ8〜12ラウンドは掛かる形に
収まった。数は `analysis/ecology-ultimate-smoke.mjs` が毎回出す。

作者は「1人1個って多くね？」「全員で1個」「1シナリオ通しで1回だけ」のあいだで迷っていた。
採ったのは**指定は一人一つ・発動権は隊で三つ**という分け方である。指定は無料でいつでも
変えられ（値段を付けると「指定を変えると損」が生まれる）、払うのは補充されない必殺印
（一遠征3つ）で、構えても放たなければ減らず、**勝った戦闘でだけ**減る。
補給を必殺ゲージに使う案は、作者自身の「装備が減るインセンティブが働く」という懸念どおり、
既に三用途で取り合っている補給に四つ目を足すことになるので採らなかった。

**engine と schema は一行も変えていない。**「1戦闘に1回」は、規則を持たない状態
`ultimate_spent`（表示名「必殺」）を自分へ付け、それが付いていないことを発動条件にすることで
書けた。必殺技の定義は固定 content に入れず、遠征ごとの装備と同じく `runContentBundle` が
bundle へ混ぜるので、validator が毎回通り、契約 ID の表は増えない。

RunState は `ultimateSeals` を、loadout は `ultimates` / `ultimateArmed` を持つようになったので
run schema を `ecology-run-4` へ、状態の語彙が一つ増えたので content contract を 19 へ上げた。
検査は `ecology/ultimate.test.mjs`（812件）と `analysis/ecology-ultimate-smoke.mjs` を新設し、
trial へ指定・構え・盤面の印・結果画面の支払いを足した（tutorial 150 / trial 122）。

### 3.57 必殺技を、作者試遊の指摘で作り直した（issue #238、2026-09-10）

PR #239 の初版（隊で共有する3回・広さと太さは両立させない・専用の枠）に対して、
作者から八つの指摘が来た。実装したのは六つで、二つは issue へ切り出した。

**解禁を Stage 1 からにした。**Stage 0 は2人・1 pack で、武器と技の違い・隊列・応急手当を
覚える回である。そこへもう一つの仕組みを載せると覚えることが多すぎる。

**発動権を「隊で三つ」から「一人一つ」へ変えた。**共有の枠だと「一番強い必殺を持つ一人へ
全部寄せる」が最適になり、残り四人の指定が死ぬ。5人いれば通しで5回ぶんある。

**広さと太さを両立させ、倍率を2倍から3倍へ上げた。**初版は両立させると一戦が終わるという
実測を根拠に片方だけにしていたが、作者の「ダメージ2倍はしょっぱい。強くしたら当然その分
敵も強くするべきで、到達ステージが増えるのは妥当」という判断で両立へ戻した。

**リアクティブを二点強くした。**反応は装着順に上から反応点を払うので、下の行に置いた必殺が
「点が尽きて出ない」で終わることがある。一戦に一度きりの一手が資源競合で消えるのは選択
ではなく事故なので、**必殺だけ反応点の外へ出した。**防壁も**戦闘のあいだ残る**ようにした。
作者が挙げた「回復の限界突破」は、engine の回復上限契約（直前の攻撃の実HPダメージが上限）を
必殺だけ破ることになるので採らず、#241 へ切り出した。

**1ラウンド目に出さないようにした。**時間ではなく状況で止める——全必殺の共通条件を
「**隊の誰かがHP70%未満**」にした。敵の一撃を受けてから切り返す形になり、作者の欲しかった
「不慮の全滅を必殺で防ぐ」絵が条件そのものから出る。序盤の楽な一戦では出ないが、それは
要らなかったということなので回数も減らない。**不発を黙って起こさない**ために、
`previewNextBattle` が `ultimateFiredBy` を返すようにし、構えた時点で ✹ が
「この一戦で出る／出ない」を示すようにした。

**専用の枠を画面から消した。**「UIがダサい（というか邪魔）」という指摘のとおりで、
たまにしか触らない操作に常設の枠を割くと毎回読み飛ばすことになる。**装着行の長押し**で
指定・解除し、行に出る ✹ で構える（作者の提案そのまま）。画面に増えたのは、見出しの
小さな菱形（必殺を残す仲間の数）と畳んだ説明だけである。

チュートリアル（負けそうな一戦を必殺で切り返す）と、カットイン演出は #240 / #242 へ。

trial は Stage 0 ではなく Stage 1 を選んで出るように変えた（Stage 0 では必殺の経路が
一つも踏めないため）。tutorial trial には「Stage 0 に必殺技が出ない」片側検査を足した
（tutorial 153 / trial 133）。

### 3.58 キャンプの固定領域を測り、タブ構成を組み直した（issue #235、2026-09-11）

作者の要望は「もっと全体的に文字を減らせないか、全体的にすっきりさせたい」だった。
issue が挙げた三つ（編成タブを残すか・戦闘を並列タブに置くか・セーブと撤退をどこへ置くか）は、
どれも作者から「プロコンがある」と返ってきた。**編成を統合すると上端の盤面が大きくなり、
戦闘を下へ固定すると固定ウィンドウが大きくなる。**固定領域が画面を奪うことが共通の理由だった。

そこで先に iPhone 幅（390×844）で実測した。貼りついたままの `.camp-top` が 234px、その上に
常設の「安全に撤退する」と「セーブ / ロード」で 64px、**本文が始まるまで 298px**。
同時に、編成タブだけ `scrollHeight` がビューポートちょうど・本文230文字（スキルタブは
2077px / 603文字）で、**スクロールする中身が無い**ことも出た。

数字を見せたうえで、削る順番を変えた案を出して合意した（DESIGN.md 8.7.11）。

- **常設ボタンの 64px を消した。**旧「戦闘」タブを「遠征」へ改め、次の一戦・撤退・セーブを
  そこへ集めた。`renderCamp()` は `hideHeaderAction: true` で shell のヘッダー操作を出さない。
  ハンバーガーや三点リーダーは採っていない（作者指摘「コンシューマゲームでそれはしない」）。
- **盤面のセルを4行から2行へ畳んだ。**名前の行へ AP/RP のピップを並べ、HP の数値をバーの上へ
  重ねた。出す情報は一つも減らしていない。1セル 67px → 46px。
- **編成タブを廃止した。**能力値4軸は `statAxesHtml()` にまとめ、スキル・装備タブの人物帯
  （`memberContext`）へ入れた（固定領域は増えない）。仲間の入れ替えと固定同行者の理由は
  遠征タブへ移した。

代わりに `boardMode(tab)` を足し、盤面の役をタブではなく盤面の状態で決めるようにした。
予測の見出し行の「⇅ 隊列」（`toggle-formation-mode`）で、**どのタブからでも**隊列を組み替え
られる——編成タブがあった頃より一手短い。この状態は `persistableState()` が落とし、読み込みでも
`false` へ戻す（組み替えの途中で開くと、人物を選ぶつもりの一押しが移動になる）。

実装中に二つ壊して直した。**セーブが物語の最中に消えた**のは、撤退と一緒に隠したからで、
セーブは離脱ではないので分けた。**`partyCellRole` に `none` の分岐が無く**、補給タブのセルが
治療を選ぶ前から押せる形になっていた（#159 の判断を壊していた）。どちらも tutorial trial が
拾った。

本文が始まるまで 298px → 203px（-32%）。

作者試遊で一つ出た。**貼りついた帯の下の余白は `margin` では取れない。**下をくぐって
スクロールしてくる本文は margin の領域にも入るので、帯の下端に文がそのまま接する。
`padding-bottom` にすると、ぼかした帯そのものが下へ伸びて余白になる（帯は 203px → 215px）。

### 3.59 スキル画面の語彙を他画面へ広げ、説明文を削った（issue #236、2026-09-11）

「説明文は削り、ヘルプは残す」という作者判断で進めた。畳んだヘルプの中身は圧縮していない。

削ったのは、同じ数の二度出し（補給の「3 / 5」、装備の「手元 3 / 12」）、見出しと直下の重複
（「技能ツリー」「ゴウの装備枠」）、押せる形が既に言っている誘導（カード一覧の「選んでください」、
金の主ボタンの「次の操作」の札）、数が言っていることの言い直し（「負けても、ここまで確定した分は
持ち帰ります」「この組み合わせは通りました」）である。規則は DESIGN.md 8.7.12 に書いた。

野営治療の一行から開発語を落とした（「一人をmaxHpの40%回復する。tank・背水役等、一人へ損傷を
集める構成向け」→「一人を最大HPの40%回復」）。戦闘マップの凡例は畳んだ「遠征のルール」へ移した
（各節は `title` と読み上げラベルで「第3戦・精鋭・未到達」と名乗るので、意味は消えていない）。
同じ種類の敵が並ぶ回は、拾い屋の一行を最初の1枚にだけ出すようにした。

4タブ合計の本文 1604字 → 1444字（-10%、廃止した編成タブの230字は別に消えている）。
補給タブは -34%、装備タブは -18%。trial の二つの検査は、消した「次の操作」の札ではなく
主ボタンそのものの位置を見るように書き直した（tutorial 154 / trial 138）。

### 3.60 「取得済みだが未装着」を廃止した（issue #236、2026-09-11）

作者試遊で「いま特に意味のない『取得済みだが未装着』という状態があって、いらない」と
出た。技能ツリーで取得済みを強調しようとすると強調を2種類用意することになる、という
見た目の話として挙がったが、調べると見た目の話ではなかった。

**技能枠には上限が無い**（`SLOT_LIMITS` の active / reactive / passive は
`Number.MAX_SAFE_INTEGER`、上限があるのは装備の2枠だけ）。取得したのに装着できない場面が
存在しないので、未装着が表していたのは「出さない」ことだけで、それはオン／オフが既に
やっていた。**同じことを二通りに表していた。**

廃止の条件は「engine から見てオフと未装着が同じであること」で、`allyInput()` が
tactics・reactives・passives のすべてを `enabled()` に通してから battle input を組んで
いることを確かめてから進めた（必殺技も `enabled()` 後の列へ差し込む）。

不変条件は「取得済みは必ず装着欄に並ぶ」。`unlock-skill` は `equipSkill` を通して**オンで**
装着し、`joinRun` と保存の読み込みは `installUnlockedSkills()` で**オフのまま**末尾へ足す。
オフで足すのは、starter の無償閉包で取得済みになる親の節を急に回し始めないためで、
これで過去の遠征の結果が動かない（通しの検査が出す勝敗・ラウンド数・被害は一つも変わって
いない）。DESIGN.md 8.7.13。

消したもの: 「装着する」の釦と `equip-skill` の handler、`skillNodeState` の `unlocked`
状態、`.skill-node.unlocked` と `.node-mark.owned` の見た目。

あわせて、技能ツリーの要約帯を直した。`position: sticky; top: 8px; z-index: 5` で
**画面の上端**に貼りついていたため、キャンプの固定帯（200px超、z-index 3）の上に乗って
盤面を隠していた（作者試遊）。CSS は貼りついた兄弟の高さを知らないので、`render()` ごとに
実測して `--camp-top-h` へ入れ、`top: calc(var(--camp-top-h) + 6px)` で固定帯の真下へ貼る。
帯の中身は「いま誰に払っているか」と「あと何点あるか」だけにした——AP/RP は盤面セルが、
装着した技能の名前と順番はスロット行が、節の値段と前提と釦はツリーの節そのものが
出しているので、帯に並んでいたのは全部再掲だった（作者指摘「今の表示じゃ意味ない」）。

検査で一つ見つけた。**`analysis/ecology-trial.mjs` の解禁の経路は一度も踏まれていなかった。**
技能点は0で始まり戦闘をクリアして初めて貯まるのに、台本はキャンプに着いた直後に
`.skill-node.available` を探しており、常に0個で `if` の中が素通りしていた。保存へ点を
入れてから踏み、取得した技能がその場で装着行に並ぶところまで見るようにした
（tutorial 154 / trial 143）。

### 3.61 巻き戻しを、結果画面の操作から会話の演出へ移した（作者試遊、2026-09-11）

作者試遊で「『時間がまきもどる』は本来めっちゃかっこいい演出なはずなので、こんな
システム画面の一部にしてほしくない。ノベルゲームみたいに、負けイベント会話の最後に、
会話画面に被せる形でど真ん中にボタンを配置してほしい」と出た。

これまでは 倒れた会話 →**結果画面**（勝敗カード・味方HP損失・敵HP損失・反応発火・
装備摩耗・戦闘後の状態・出した順番）→ その主操作［時間が巻き戻る］→ 巻き戻しの会話、
という順だった。杭を引いて時間を戻すのはゴウがその場でやったことで、会話の最後の行は
もう「坑が来た道を逆にたどっていく」と言っている。**演出の真ん中に決算書が入っていた。**

`STORY_GATES` を足した。beat の id で引く表で、`storyGate()` が「その beat に門があり、
最後の行で、積んだ断片も尽きている」ときだけ門を返す。門があるあいだは舞台に被せて
`.vn-gate` を描き、進む合図を出さず、舞台を叩いても AUTO でも越えない。文字送りが
終わるまで出さないのは CSS（`.vn.typed .vn-gate`）の役で、JS は足していない。

倒れた会話の `after` を `"prologueResult"` から `"prologueRewind"` へ変え、
`finishStory()` が `rewindPrologue()` を呼ぶようにした。**門の釦もスキップも同じ関数を
通る。**結果画面は序盤の敗北の経路から外れた（保存枠が尽きたときの minimal snapshot は
phase を result へ寄せるので、その保険として `resume-prologue-defeat` を残した）。

「この一戦は遠征に数えません」も消した。この拍のプレイヤーは活動資金をまだ一度も
見ていないので読んでも意味が取れず、演出の真ん中にシステムの断り書きが立つ。
答えは次の会話が、時間が戻ったこと自体で返す。DESIGN.md 8.7.14。

**門は序盤の一戦専用である。**通常の敗北で時間は戻らない（作者判断）。表は1件しか
持たず、増やす前提も置かない。

`analysis/ecology-screens-smoke.mjs` に片側検査を足した（結果画面に「時間が巻き戻る」が
戻っていないこと・`after === "prologueResult"` が無いこと・門の五つの経路と、門が
文字送りの終わりを待つ CSS があること）。tutorial trial は、門が会話の最後で出ること・
結果画面を挟まないこと・釦が舞台に被さっていること・舞台を叩いても越えないことを踏む
（tutorial 158 / trial 143）。

### 3.62 巻き戻しに、戻っていく過程を足した（作者試遊、2026-09-11 / issue #200）

3.61 で釦を会話の舞台へ移したが、作者試遊で「いまは押した瞬間次の会話に遷移していて、
巻き戻っている感覚がないです。もっと演出を強くしたいです」と出た。置き場所は直っていた
が、**「時間が巻き戻る」という出来事そのものは一度も画面に出ていなかった。**

phase `rewind` を足した。`rewindPrologue()` は、状態を触る前に逆走の材料（倒れた会話の
舞台と、`state.story.log` を逆順にした行）を写し、巻き戻してから
`enterStory(..., { via: "rewind" })` で**会話の手前に場面を一度だけ挟む。**
`mountRewindView()` が DOM 側で、閃光 → 逆走（読んだ行を末尾から消す。名前も立ち絵も
行と一緒に戻る）→ 静止 → 白へ抜ける、を進める。**新しい台詞は足していない。**
舞台を叩けば追い越せ、`prefers-reduced-motion` では行の差し替えだけに落ちる。
長さは 2〜3 秒（短縮時は約1秒）。DESIGN.md 8.7.15。

状態は演出へ入る前にもう巻き戻してあるので、途中でリロードしても進行を失わない
（`persistableState()` が phase を `story` として保存し、再開は巻き戻し後の会話から続く）。

**一押しで二つ鳴っていたのも直した。**会話の門の釦は `.vn-stage`（叩いて進む）の中に
あるため、押すと釦と舞台の両方が反応し、巻き戻し後の会話の一行目「同じ朝。同じ光。」——
時間が戻ったことを見せる行——が読み飛ばされていた（3.61 からの持ち越し）。
**`finishStory()` の巻き戻しの枝の位置**も直した。履歴を初期化したあとに置いていたので、
会話をスキップして巻き戻すと逆走させる行が空になり、演出が黙って消えていた。

続けて作者から「スキップする時、そもそも『時間が巻き戻る』のボタンまでのスキップに
できませんか。そうすれば、巻き戻った時にも逆走の材料がたくさんあることになりませんか」と
出た。そのとおりなので、`storySkipStop()` / `skipStoryToGate()` を足して、**門のある会話の
スキップは門の行で止める**ようにした（飛ばした行は履歴へ積むので、読み返せるし逆走も
使える）。門の無い会話はこれまでどおり丸ごと飛ぶ。これで門を越える手段は釦だけになり、
`finishStory()` の `prologueRewind` の枝は通常の操作では通らない受け皿になった。

`analysis/ecology-screens-smoke.mjs` に、演出の五つの経路・割り当て表・CSS の keyframes・
`prefers-reduced-motion` の短縮に加えて、**この二つの順序**（門の押しを舞台へ落とさない、
巻き戻しの枝は履歴の初期化より前）を見る検査を足した。tutorial trial は、押した直後に
演出が入ること・そのあいだ会話へ進まないこと・逆走が読んだ行の前方一致であること・
放っておけば会話へ渡ること・その会話が一行目から始まること、そしてスキップが門で
止まること（門は途中の行では出ないこと・飛ばした行が履歴に残ること）を踏む
（tutorial 165 / trial 143）。

### 3.63 巻き戻したあとの一手を、手取りのチュートリアルにした（作者指摘、2026-09-12）

3.62 で巻き戻り自体は見えるようになった。その先——**ツグミを後列へ下げる一手**——は
遠征タブの一段落（「上の『⇅ 隊列』からツグミを後列へ、ゴウを前列へ置いて…」）だけで
渡していた。作者から「最初のチュートリアル、並べ替えてツグミを後ろに下げる部分を
ちゃんとしたチュートリアルにしてくれますか。押すべき場所が光って、そこしか押せなく
なるみたいな、よくあるチュートリアルです」と出た。

**その一段落は、操作を知っている人にしか読めない。**初めて開いたキャンプにはタブが
四枚・盤面が六枠・釦が十数個あり、「⇅ 隊列」がその中のどれかを探すところから始まる。
導入の山（一手で予測が敗北から勝利へ変わる）へ着く前に、技能ツリーや装備を一周できて
しまう。

巻き戻し直後だけ、キャンプを三段の手取りチュートリアルにした（`formationTutorialStep()`）。
`⇅ 隊列` → 動かす仲間のセル → 後列の空き枠、と**押す場所だけが光り、そこ以外は
押せない。**段ごとに次の一押しと理由を一つだけ出す（`formationTutorialNote()`）。
錠と光は描画のあとに一度で掛け（`applyFormationTutorialGate()`）、**判定は光らせる先と
同じ選択子**（`formationTutorialSpotSelector()`）なので、「光っているのに押せない」枠が
構造として作れない。経路側（`handleAction`）でも同じ条件で弾く。

**錠は並べ替えの三手だけ。**ツグミが後列へ入った瞬間に外れ、盤面も自分で通常へ戻る。
そこから先は技能も装備もセーブも予測も自由で、`この敵に挑む` は光るだけで塞がない
（配置を戻して予測がどう動くかを見る遊びは、この導入の中心なので取り上げない）。
錠の最中も盤面の HP と予測は沈めない——**この一手の理由はそこに出ている。**
**教える一手は content が持つ**（`PROLOGUE.tutorial` の `characterId` / `row`）ので、
app.js は人物 id と行を書き写さない。DESIGN.md 6.4.4 を足し、「この一戦を進める操作は
塞がない」と書いていた 6.4.3 の範囲を離脱経路だけに絞った。

`analysis/ecology-screens-smoke.mjs` に段・錠・光・CSS（`prefers-reduced-motion` の短縮を
含む）の構造検査を足した。tutorial trial は camp に着いた直後へ移し、三段それぞれで
**光るのが一箇所だけであること・他が押せないこと**、光っていない場所を押しても戦闘が
始まらないこと、一手で予測が勝利へ変わること、そのあと錠が外れて盤面が通常へ戻ること、
次の一押しが光ることを踏む（tutorial 176 / trial 143）。

### 3.64 必殺技を長押し一回のトグルにし、構えているあいだ行を光らせた（作者要望、2026-09-12）

#238 の初版は「装着行を長押しして指定 → 行に出た ✹ を押して構える」の二手だった。
作者から「いま必殺技を使うにはスキル長押し→ボタン押し、が必要ですが、長押しだけで
必殺技orNotをトグルできるようにして、かつ必殺技を使おうとしている時はもっとかっこよく
スキルを光らせてほしい」と出た。

**指定と構えは概念としては別だが、操作として分ける価値が無かった。**指定だけして構えない
状態には使い道がなく、必殺を出すたびに毎回二手を要求していた。一人一遠征に一度きりの
一手に二手を掛ける理由は無い。`toggleUltimateForBattle`（playable-battles.mjs）で長押し
一回が両方を動かすようにし、行内の ✹ は押す釦ではなく**状態の印**にした。行の中で長押しの
親と釦の子が当たり判定を取り合う問題（PR #244 で一度直した箇所）も、押す先が一つになった
ことで消えた。model 側の `setUltimate` / `toggleUltimateArmed` は原子操作として残してある。

代わりに**行そのものを光らせる**。段は三つで、段の差がそのまま状態の差になる——指定＝金の
縁、構え＝金が脈打つ、**予測が「この一戦で本当に出る」と言っているあいだは光が走る**。
放ち終えた行は脈を止めて沈める。`prefers-reduced-motion` では光を残して動きだけ止める。
文字を足していないので、行の説明は一行も増えていない。

### 3.65 必殺技を教える一戦を Stage 1 の第1戦に置いた（issue #240）

#238 で仕組みは入ったが、**どこで覚えるのかが無かった。**Stage 1 から解禁されるだけで、
画面には畳んだ説明があるだけだった。作者の要望は「冒頭の先見機（灰の門）みたいに、
負けそうになって必殺技で切り返す」である。

**巻き戻しは使わなかった。**灰の門の巻き戻しが教えているのは「予測という道具がある」
ことで、Stage 1 ではもうその道具を持っている。もう一度負けさせてから巻き戻すのは同じ
教材を二度やることになるので、**予測そのものを教材にした**——構えない予測は「敗北」、
構えた予測は「勝利」で、帯が変わるのを見てから挑む。

盤面（`ULTIMATE_LESSON`、content/story.mjs）は盾の灰殻2枚＋後撃ち2で、構えなければ
7ラウンドで時間切れ・ツグミが落ち・盾兵が残る。構えれば5ラウンドで勝ち、誰も落ちない。
**差は必殺ひとつぶんだけ**で、`ecology/story.test.mjs` が両方を engine で実際に走らせ、
予測（`previewNextBattle`）が両方を先に出すことまで固定している。教えるのはナギの
溜め突きで、**溜めが要るせいで使いにくい技能**が「溜め不要・全体へ・量3倍」になる。

  - **12戦の梯子の中の第1戦**である（灰の門は第0戦＝遠征の外）。勝てば普通に報酬へ進み、
    負ければ普通に再挑戦へ落ちる。印（`ultimate_lesson_seen`）は**勝った時点**で押すので、
    負けた回は同じ教材がもう一度出る。
  - 手取りは二段（ナギのセルを押す → 行を長押しする）。錠の作りは隊列チュートリアルと
    共有し（`tutorialGate()` / `applyTutorialGate()` / `tutorialAllows()`）、**手取りの型を
    二通り作らない。**
  - 手書きの一戦の組み立ても一つへ寄せた（`scriptedEncounter`）。灰の門と必殺技の一戦が
    別々に敵を作っていると、いつか片方だけ直る。

### 3.66 必殺の拍にカットインを載せた（issue #242）

必殺が出たことは、技能名（「必殺・溜め突き」）・結果画面の帯・状態「必殺」からしか
読めなかった。**拍そのものは既にイベント列にあった**ので、`replay-beats.buildBeats` に
`kind: "ultimate"` の拍を一つ足し、そこだけ盤面を沈めて立ち絵・技能名・掛かった変換を
一枚の帯で見せる。着弾は次の拍に分ける（準備付き行動が準備完了と着弾を分けているのと
同じ理由で、同じ拍に重ねるとどちらも読めない）。

**engine も schema も触っていない。**必殺は新しい event を持たないので、読むのは ID の形
（`ult_`）だけである。アクティブは宣言の event（`action_started`）があるので盤面が動く前に
置けるが、**リアクティブは宣言を持たない**（`fireRule` は効果だけを出す）ので、最初の効果で
拍を開き、見せる盤面を一つ前にして「効果が乗る前」を出す。同じ firing から二度は出ない。

拍はイベント列だけから決まるので、一時停止・一手送り・戻す・速度変更・自動再生のどれでも
順序が変わらない。**飛ばす手段は既にある**（速度変更と一手送り）ので専用のスキップは
置かない。`ecology/replay-beats.test.mjs` が両方の形を見て、
`analysis/ecology-ultimate-smoke.mjs` が12戦×2通りの指定で**放った数とカットインの拍数が
一致すること**を見る。画面側は両方の trial が踏む（tutorial 197 / trial 142）。

### 3.67 長押しの手応え・チュートリアルの三手目・必殺の残りを人物ごとに（作者指摘、2026-09-13）

3.64〜3.66 を実際に触った作者から、三つ来た。

**「長押しの時、左から光ゾーンが出てきてだんだん右に向かって増えていく（右端まで
到達したら必殺技セット完了）ようなアニメーションにできませんか？」**

長押しの弱点は**いつ入るのか分からないこと**だった。押した瞬間は何も起きず、450ms 後に
突然状態が変わる。行の左から右へ伸びる光の帯（`.installed-row.pressing::before`）を足し、
**帯が満ちた瞬間＝必殺が入る瞬間**にした。途中で離すかスクロールすれば帯ごと消えるので、
取り消しもそのまま見える。既に構えている行は帯が灰色（＝解く操作）。帯の長さの正本は
`LONG_PRESS_MS` ひとつで、`--long-press-ms` として CSS へ渡す。**この帯だけは
`prefers-reduced-motion` でも止めない**——装飾ではなく、押している時間の目盛りである。

**「チュートリアルの長押しの後すぐに遠征タブに遷移されてしまうので、遠征タブに進むのも
ユーザーにおさせてチュートリアルの一部にできませんか？」**

初版は構えた拍に `state.tab = "map"` で画面を跳ばしていた。**勝手に動く画面は手取りでは
ない。**三手目「光っている『遠征』タブを押す」を足し、タブの閉じ込め
（`ultimateLessonTabLocked()`）は最初の二手だけに掛けた（閉じ込めたままでは三手目が
打てない）。構えた拍は `state.tab = "skills"` で**その場に留める**（run の既定が遠征タブ
なので、揃えないと三手目が最初から済んだことになる）。

**「誰が必殺技を残していて誰が使用済みかわからないので、それがわかるようなUIに変えて
くれませんか？隊全体の合計必殺技数はいらないです。」**

技能タブの見出しに出していた合計（`seal-pips`）を削除。一人一遠征に一度きりで隊で
分け合う枠でもないのだから、合計は「あと何回あるか」にしか答えず、「**誰の**一回か」に
答えない。印は**上端の盤面のセル**（常にその人物ひとりを指す場所）へ移し、四段にした——
残っている（薄い ✹）・構えている（金の ✹）・この一戦で出る（光る ✹）・もう放った
（灰の ✧）。結果画面の一行も人数から名前へ変えた（「必殺を残す仲間 2 / 3」→
「まだ残しているのは ゴウ・ツグミ」）。

trial は三つとも踏む——帯が伸びること・途中で離すと取り消されること、三手目のタブ操作と
そのあいだ他のタブが押せないこと、放った仲間だけが使用済みの印になること
（tutorial 203 / trial 142）。

### 3.71 顔背景を主役にして情報を下部へ寄せた（2026-09-11）

戦闘予測の小さいキャラクター枠では、顔を薄く敷くだけでは目元まで背景に埋もれた。そこで、
戦闘と予測の味方枠から名前と職種アイコンを外し、顔を識別の主語にした。HP・AP/RP・状態・演出は
下部の情報帯へ集め、その帯だけを濃いグラデーションで覆う。これで目元を常に見せたまま、顔画像を
濃くできる。敵枠は既存表示を維持する。

### 3.72 顔をさらに濃くし、戦闘行動をHPの上へ置いた（2026-09-11）

作者試遊で、名前や職種アイコンが顔へ被らなくなったなら、顔はもっと濃くできると判断した。また、
戦闘中の行動内容をHP等の下へ置くと、行動欄が通常時も顔の面積を奪う。そこで味方カードの情報順を
**顔 → 行動内容（表示時のみ） → HP・防壁・状態**へ変更し、非表示の行動欄は高さを畳んだ。顔の不透明度と
彩度も上げ、下部の情報帯の可読性は維持した。予測セルは行動欄を持たないため、顔を同じ方向へ濃くした。

### 3.73 5人の顔サイズを共通基準へ揃えた（作者指摘、2026-09-12）

作者試遊で、ツグミとゴウを並べると元画像の余白差によって顔の大きさが揃っていないことが分かった。
そこで `character-face-watermark[data-character]` に人物別の共通補正を持たせ、戦闘と戦闘予測の両方へ
同じ値を適用した。名前と職種アイコンを顔へ戻さず、目元と下部の情報帯の関係を保ったまま、5人を同じ
比較条件で見られる `/ecology/portrait-test.html` も追加した。確認画面には予測セル相当・戦闘カード相当の
2段と目元の基準線を置き、実画面へ反映される補正を一度に調整できるようにした。

### 3.74 装備を選ぶのをボス戦だけにし、補給を遠征あたり3個で固定した（作者指摘、2026-09-12）

作者試遊の訴えは「装備を毎回選ぶのと、補給と比較するのが、思考負荷が高くてつらい」だった。
毎戦の勝利が3択（装備2 + 補給1）を出していたので、12戦のあいだ判断が途切れず、しかも
**二種類の判断が同じ画面で混ざっていた**——装備どうしを比べる判断と、装備を捨てて補給を
取るかという別軸の判断である。

そこで軸を分けて、片方を画面から外した。

- **装備を選ぶのはボス戦（4・8・12戦目）を突破したときだけ。**候補は装備2件で、補給は
  候補に入らない。12戦目も外さなかった——12戦目もボス戦なので、そこを抜くと「ボス戦突破後
  だけ」という規則に穴が空く。遠征はそこで終わるが、拾った品は**精算で残す設計図の候補**
  になるので選ぶ意味が残る（結果として遠征あたり3回）。
- **補給は遠征の開始時に3個で固定。**報酬で増えず、表記も残り／総数（3/3）にした。総数を
  動かすのはギルドの「開始補給」だけ（最大 +2、上限5）。屑から戻せるのは使った分だけで、
  総数は超えない。これで「いまどこへ使うか」だけが判断として残る。
- **必然的に、ボス戦以外の勝利は結果画面を挟まずキャンプへ戻る。**決めることが何も無い
  画面を一枚挟んでいたので外した。直前の一戦の要約（ラウンド数・味方HP損失・技能点・
  装備摩耗・必殺の印・戦闘不能）はキャンプの上端の一枚が預かる。**先見の予測がよほど
  悪くなければ、そのまま次の一戦へ進める**ことを想定した形である。

**負けたときの経路は変えていない。**敗北は決めることがある（再挑戦するか、精算するか）。

補給の総量が減ったぶん、遠征の後半は明確に厳しくなった。`analysis/ecology-stage3-builds.mjs`
は一戦ごとに報酬で補給1を取る前提（遠征を通して最大11個）で測っていたので、遠征あたり3個へ
直した。三構成の到達は 9 / 6 / 12 戦で変わらず、「隊列で守る」の終わり方だけが時間切れから
全滅へ変わった。この構成のゴウが第10〜12戦で止めを刺さなくなったので、鳴らなくなった
「研ぎ澄ます」を取得計画から外し、実際に鳴っている手当て（mend）へ点を回した。

### 3.75 装備の候補を1画面に収めた（作者指摘、2026-09-12）

「装備は2つとも画面に収める。少なくとも iPhone 16e ではスクロールなしで選べてほしい。
ヴァンパイアサバイバーズを参考に。もし可能なら、今後ギルド画面から候補を3つにする
アップグレードが来ても大丈夫なようになると理想。」

選ぶ画面なのに、勝敗の大札（✓ と4つの指標）と装備の全文が縦に積まれていて、2枚目の
「拾って次へ」が折り返しの下にあった。**比べられない比較画面**だった。

読む順で三層に分けた。(1) 名前と等級、品全体を変える一行（keystone・規格外の代償）、
(2) 常時効果と追加効果の要約（格・名前・量。常時の行には「常時」の印）、(3) 条件・代償・
発火回数・耐久まで入った全文は畳んだ段の中。**全文はいつでも拾う前に読める**（完全開示は
畳むこととは別の話である）。勝敗の大札はこの画面だけ一行へ畳み、4つの指標は下の
「戦闘後の状態」へ移した。上端の「安全に撤退する」も出さない——先に拾ってから撤退できる。

候補が3件になったときは、畳まない効果の行数だけを一つ減らす（高さは `data-count` から
CSS が締める）。実測は trial が持つ：iPhone 16e の実機 Safari は 390x844 の CSS viewport の
うち上下のバーで**およそ 660px しか残さない**ので、`window.innerHeight` ではなくこの予算で
収まりを見る。いま 2件で 477〜520px、3件で 647px である。

### 3.76 残す設計図をプレイヤーが選ぶようにした（issue #151）

遠征終了時の Blueprint 保存は、等級順に勝利2・安全撤退2・敗北1を自動で残していた。装備の
組み合わせを見つけるゲームで、**最後の価値判断だけがレアリティに置き換わっていた**——
今回の構成を成立させた低レア品より、使わなかった高レア品が残る。

候補と上限を `progression.mjs` の外へ出し、残せる件数より多く見つけているときだけ選択画面を
挟むようにした（選ぶ余地が無いときに画面を増やさない）。**選ばなかった品は残らない**——
埋まっていない枠を自動で埋めると、選ばせた意味が消える。候補の並びは等級 → 表示名 → id で
決定的、全文は残す前に読める。選択を渡さない経路（既存の自動精算とテスト）は従来どおり
等級順に上限まで残すので、既存セーブと保存件数の不変条件は動かない。

**持ち帰れるのは、12戦を抜けて生還したときの1件だけにした**（勝利1・安全撤退0・敗北0。
作者判断 2026-09-12）。ボス戦だけで装備を拾うようになって候補が遠征あたり最大3件へ減った
のに保存数が2〜1のままだと、「見つけた品はだいたい残る」になって遠征の終わりの判断が
消える。撤退と敗北を0にしたのは 12戦を抜けることそのものに意味を戻すためで、確定した
活動資金を持ち帰る規則は変えていない（撤退は敗北ではない、という区別は資金と Stage 解禁の
側が持つ）。

したがって選択画面が出るのは**完走して2件以上拾った回だけ**で、**毎回は出ない**。出るときは
必ず「どれかを捨てる」判断になる。持ち帰れない回は、精算画面がなぜ残らないのかを書く。
tutorial trial は完走の側（12戦目のボス報酬 → 選択 → 持込）を、trial は完走しなかった側
（持ち帰れないという表示）を踏む。

### 3.77 装備に与ダメージの伸びと、刻んだ状態を読む払い先を足した（作者指摘、2026-09-12）

「装備のバランスも直せるといいかも……。『回復が仲間全体につく』のに回復効果のない装備が
あったりするし、ダメージ増加系の装備がないし、いまの装備にはスキルやキャラとのコンボの
ワクワクがあまり感じられないです。」

三つとも目録（`content/affixes.mjs`）の穴だった。

- **「ダメージ増加系が無い」は trigger が無かったから。**`damage_proposed` を読めるのは
  「自分が受ける直前」（`src_incoming`）だけで、**与える側の直前を読む trigger が無かった**。
  だから量を太らせる払い先も作れず、装備の払い先が守り・支援・追撃へ偏っていた。
  `src_outgoing`（研ぎの）と `pay_amplify`（増幅）を足した。追撃＝別インスタンスではなく、
  いま決まろうとしている一撃そのものを増やす。`key_honed`（研ぎ澄ました、ダメージ +50%）も
  足した——hit を増やす `key_twin_edge` は受けに二度払うので、重い単発では伸びない。
- **「コンボのワクワクが無い」は、装備が自分だけで完結していたから。**隙も裂傷も技能が
  配るものなのに、それを読む語彙が装備側に無かった。`cnv_exposed` / `cnv_bleeding`（条件）、
  `pay_rend`（敵全員へ隙）、`pay_rupture`（裂傷を負った敵すべてへダメージ）を足した。
  どれも**誰かが先に刻む構成の中でだけ強く鳴る**。裂傷を配れない trigger へ付くと満たし
  ようのない死に効果になるので、敵を的にした出来事の隣にしか置かない。
- **「回復が全体につくのに回復効果がない」は量の問題だった。**`key_overflowing` は元から
  heal effect を要求していたが、回復の基準値が 3/5/12 で barrier の 1/5 しかなく、「全員へ
  届く」と書いてあるのに実質ゼロだった。10/18/30 へ上げた。anti-stall の形（被弾 chain の
  中だけ・有限コスト・chain 1回）は変えていないので、回復量は依然その攻撃で受けた傷が
  上限である。

生成物の内容が変わるので content contract は 22、generator version は 8。**保存済みの
Blueprint は定義そのものを持つので、版が上がっても動く。**

### 3.78 選べる札にするには、条件と消費が畳まれていてはいけない（作者試遊、2026-09-12）

§3.75 で札を三層に畳んだあと、作者から四つ出た。

**「結局、条件と消費も見ないと選べないです。」**畳んだのは「長い文」だと思っていたが、
畳まれていたのは**判断の材料そのもの**だった。効果の要約（格・名前・量）だけでは、
いつ鳴るのか・何を払うのかが分からない。そこで目録の側に `ruleReadout()` を作り、
rule ごとの「いつ（trigger と condition）／何を払い（耐久・HP・防壁・反応点）／何回まで」を
文へ畳む前の形で外へ出した。全文の一文も同じ構造から組むので、**札と全文が同じ材料**を
読む。畳むのは高さが尽きた rule だけで、そのときは**rule ごとまとめて**畳む——見出しだけ
出して何が起きるかを隠すのが一番悪い。件数は「全文を読む（ほか2件・条件・代償・回数）」と、
**それを開く行**が言う（別の一行にすると高さを食ったうえで押す場所を言わない）。

**「戦闘をスキップするボタンが『キャンプに戻る』になっていて、戦闘前に戻るのかな？ と
勘違いしてしまいます。」**行き先を札にしたのが間違いだった。この釦がするのは再生を
打ち切ることだけで、行き先は場面で変わる（結果画面・キャンプ・精算）。**操作の名前**
にして「再生をとばす」にした。

**「戦闘後の『HP損失』が、戦闘開始時に減っていた分のHPも含んでいそうです。」**そのとおりで、
engine は `maxHp - hp` を数えていた。HPは戦闘をまたいで持ち越すので、これは「いま欠けている
量」であって「この戦闘で減った量」ではない。無傷で抜けた一戦でも損失が出ていた。
`startingHp - hp` に変えた（回復で開始時を上回った人物は0と数え、誰かの回復で別の誰かの
被害を相殺しない）。

**「報酬画面の『戦闘後の状態』も複雑すぎて要らないかも。戦闘後キャンプの『LAST BATTLE』と
同じモーダルでいいです。」**装備を選ぶ画面に、勝敗の大札・4つの指標・人物ごとのHP・装備耐久の
一覧が並んでいた。決めるのは候補なのに、詳細のほうが面積を取っていた。キャンプと同じ一枚へ
置き換え、**候補の下**へ置いた（直前の一戦は候補を選ぶ材料ではない）。組み立ては純関数に
分けて、キャンプと報酬画面が同じ一枚を読む。

畳む量を減らしたので高さを測り直した。行の予算を候補数から決め（2件で4行、3件以上で3行）、
勝敗の大札を外して見出しと余白を詰めた結果、iPhone 16e の予算 660px に対して
**2件で451〜539px、3件で603〜608px**になった（trial の実測。seed ごとに品が変わるので
幅がある）。
### 取得予約を実装（2026-09-12）

技能ツリーからキャラクターごとに1つの目標技能を設定し、現在の技能点で目標まで完了できる場合は「取得」、
不足する場合は「予約」と表示するようにした。未取得ならLv1まで、取得済みなら最大Lvまでという目標を選べる。
戦闘勝利で技能点が入った直後に、前提・必要Lv・目標技能・目標のレベルを決定的な順序で自動取得する。
目的以外の自動取得技能はオフ、目的技能はオンで装着する。経路の表示は、取得済みの前提を通常色のままにし、
未取得の部分だけを水色で示す。RunStateへ予約と目標Lvを保存し、タイトルへ戻った後の再開でも同じ方針を継続する。
Run schemaを更新し、旧形式の予約文字列は最大Lv予約として後方互換に扱う。

### 技能ツリーを「地図」と「操作盤」に分けた（2026-09-13）

作者の報告は「横スクロールも縦スクロールもできて、とても見やすい。一方で操作はしにくい」。
見やすさの正体（実座標の森・交差しない線・固定幅の列）は R19 の判断そのものなので残し、
操作しにくさの出どころだけを外した。

原因は、**地図を読む道具と手を決める道具が同じ箱に入っていた**ことである。節を押すと、
その節の中で説明と取得の釦が開いていた。釦は列幅（iPhone では 176px）の中に入るので、
押す前に横スクロールが要る。開いた節だけ背が伸びるので、押した瞬間に同じ行の節も線も動く
（実測で節の高さが 64px → 338px、ページ全体で +296px）。

節が持つのは「どこに何があるか」だけにして、押した節の中身は画面の下端に貼りつく操作盤
（`.skill-sheet`）へ移した。盤は帯の外に居るので、地図をどれだけ動かしても操作の場所は
変わらず、押しても地図は組み変わらない。釦は画面幅（実測 340px）を使う。あわせて、盤の
「前提」「派生」の札で辿ったときに、その節が帯の窓の外に居るときだけ地図を寄せる
（横は窓の中央、縦は貼りつく帯の下から盤の上までの上寄り 1/3）。すでに見えている節を
押したときは動かさない。

`analysis/ecology-tutorial-trial.mjs` に実寸の検査を足した（押す前後で節の実座標が変わらない・
盤と釦が画面の横幅に収まる・✕ で盤が閉じる）。

続けて作者から「さすがに狭すぎる。技能説明をもう少し狭くしたい」。盤は画面の下端に居るので、
高いぶんだけ地図の見える帯を食う（最初の形は 290px で、選んでいるあいだ地図が2行しか
見えなかった）。盤に残すのは**その節を取るかどうかを決める材料**だけにして 130〜155px へ
落とした。落としたのは、いずれも別の場所が同じことを言っているものである。

- 入切の釦と「取得状態は変わりません……」の一行 → 盤の頭の摘み（装着行と同じスイッチ）。
  `skillToggleSwitch()` を装着行と盤で共有し、同じ操作に二つの見た目を持たせない。
- 前提・派生の札（`skillRouteChip`）→ 真上の地図。線と色が、どこから来てどこへ行くかを
  すでに見せている。前提が足りないときだけ、足りない前提と必要な段を一行で出す。
- 予約の説明文 → 畳んだ「技能のルール」へ一度だけ。いまの予約先は上の要約帯が出す。

取得・段上げ・予約・予約取消は一行に並べ、いま押せる「解禁」がある節には同じ結果になる
「Lv1まで取得」を出さないことにした（Lv1 は、いま解禁できないときだけ予約として置く）。
前提の札を消したので、前提の必要 Lv を目盛りで重ねる `.level-meter need` も一緒に落ちた
（足りない事実は盤の一行が言う）。
### 3.79 ラウンド持続効果の期限切れを次ラウンド開始へ移した（作者指摘、2026-09-13）

戦闘の最後の攻撃を解決した直後にラウンド持続の防壁・状態を削除すると、replay のアニメーション上は
最後の攻撃が防壁を大量に削ったように見えた。そこで `round_ended` と未使用資源の処理では期限切れを
行わず、次の `round_started` event を記録した直後に、前ラウンド中に作られた効果を削除するようにした。
期限切れ event は新ラウンドの開始 event より後に並ぶため、盤面では次ラウンド開始の拍に防壁の消失が
現れる。ラウンド終了フェーズ中に作られた効果は次ラウンドを通過する既存の境界も維持した。
### 前提表示を現在の残りレベルへ直した（作者指摘、2026-09-13）

技能カードの破線四角が、前提技能の取得コスト（SP）を表示していたため、すでに前提を
満たしていて今すぐ取れる技能にも `1` などが残っていた。また、無料入口の技能を価格として
数えないため、ツリー上の「他技能のLv数」とも一致していなかった。

破線四角の意味を、**そのキャラクターが現在のLvから、対象技能を取得できる状態までに
追加で取るべき他技能のLv数の合計**へ統一した。前提の前提まで辿り、同じ技能は必要Lvの
大きい方だけを一度数える。既に条件を満たす前提は差し引くので、取得可能な技能は `0 + 取得コスト`
として表示される。実線四角の対象技能の取得コストと、予約・解禁の判定は変更していない。

### タイトル画面を、記号ではなく組み文字で立て直した（作者指摘、2026-09-13）

作者から「タイトル画面、もう少しかっこよくなりませんか。シンプルめがいい。でもいまの
◆＋タイトル名はダサすぎる」。旧画面は金色の ◈ を 42px で置き、題名を一行で書き、
通常画面と同じ角丸の箱ボタンを三つ積んでいた。

記号を外し、題名そのものを組み文字（上段「ONE BATTLE」＋下段「AHEAD」）にした。
箱ボタンは細い罫で区切った品書きへ替え、金はいま押せる一つにだけ残した。空気は CSS の
層だけで作り、画像は増やしていない（起動の読み込みを増やさないため）。方針は
`docs/DESIGN.md` §8.16。

`analysis/ecology-trial.mjs` の「初期表示」は `/One Battle Ahead/` を本文から探していたが、
表題が CSS の大文字化と改行を挟むようになったため、大小と改行を無視して読む形へ直した。
検査の意図（題が出ているか）は変えていない。

### 題の強弱を色へ移し、タイトルへ5人の画と読み込み画面を足した（作者指摘、2026-09-13）

前項の組み文字（上段「ONE BATTLE」＋下段「AHEAD」小）について作者から
「one battle だけ強く、ahead だけ弱くしたのはどういう意図か。題の意味がだいぶ変わりそう」。
題は "one step ahead" の掛けで、「次の一戦が見えている」（先見機）と「あと一戦だけやりたい」
を同時に指すものだった。ahead を副題の位置へ落とすとその掛けが消えるので、二語を同じ
大きさ・同じ太さで一行に並べ、**強弱は大きさではなく色**（ahead だけ金）へ移した。

同じ指摘で「画像ありも見たい。読み込みを最初にキャッシュへ載せて、読み込み画面を作って
ほしい。最初のチュートリアルの会話も、いつも立ち絵が遅れて表示される」。原因は配信して
いた画像そのもので、`docs/art/bustup_v0/*.png` は 1枚 2.3〜3.1MB・5人で 13MB あった
（画面では 240x320 前後にしか出ない）。

- `analysis/art-web-assets.py` を足し、原本から配信用の派生を作るようにした。立ち絵は
  長辺 640px の WebP（5枚で約 360KB）、タイトルの画は5人を並べて暗くした1枚（約 250KB）。
  **原本は触らない。**
- `app.js` の入口を `render()` から `boot()` へ変えた。タイトルの画と5人の立ち絵を
  `Image.decode()` で先に取り、読み込み画面で待つ。200ms より速ければ読み込み画面を
  出さず、7秒を超えたら待たずに始める。会話に入ってから立ち絵が出るまでは実測 0.2 秒。
- タイトルの画は読み終わってから現れる（`html.title-art-ready`）。`index.html` は
  app.js が届くまでの静的な読み込み印と、画の `<link rel="preload">` を持つ。
- `_headers` に `/ecology/art/*` の一日キャッシュを足した。

`analysis/ecology-screens-smoke.mjs` の「呼び先がすべて存在するか」検査へ、実行環境の名前
として `Image` を足した（画面が増えたわけではない）。

### 入口を二つに絞り、視線を題へ戻した（作者指摘、2026-09-13）

画を入れた直後のタイトルについて作者から「『つづきから』『はじめから』『ロードゲーム』
という並びもいまいち。ロードゲームだけ浮いている」「視線がタイトルよりキャラの顔へ誘導
され、そのすぐ上に、あまり押すことが想定されていないロードゲームが来ているので変。
タイトルに視線が誘導されないのも気になる」。

押す頻度の違いが画面に出ていなかった。入口の列は「つづきから」「はじめから」の二つに
絞り、保存枠は控えの一行の下の小さな一行（`.title-link`）へ移した。言葉も、一つだけ
片仮名の名詞だった「ロードゲーム」を「セーブデータを選ぶ」へ改め、開いた先の見出しも
同じ言葉にした。

視線は、題を大きくして後ろに淡い明かりを置き、画をぼかして（1.1px）彩度を落とし、文字が
乗る高さの幕を濃くすることで、題 → いま押す一つ → 画の順に落ちるようにした。
方針は `docs/DESIGN.md` §8.16。

検査（`analysis/ecology-trial.mjs` / `analysis/ecology-tutorial-trial.mjs`）は、入口の列が
二つであること、保存枠の一行が別にあることを見る形へ直した。

### 3.80 第一部を10 Stage にし、Stage ごとに12戦を持たせた（作者指摘、2026-09-13）

作者の言葉は「1シナリオ目はチュートリアルとしてちょうどいい。ただ2シナリオ目以降が
全く調整されていない気がする。敵も使い回しだし」でした。

**調整不足ではなく、構造でした。**12戦の定義（`EXPEDITION_ENCOUNTERS`）は一組しか
無く、Stage 0 から Stage 3 まで**同じ12戦を人数で切り詰めて**遊んでいました。Stage が
進んでも出る敵も並びも同じで、増えるのは味方の人数だけです。実測でも、出荷済みの
Stage 1 は Stage 0 より軽く（難度指数 793 対 964）、基準編成が Stage 1 のほうが
先まで進めていました。**二番目のシナリオが一番目より易しい**状態です。

直したのは四つです。

1. **`STAGE_ENCOUNTERS`（10 Stage × 12戦）**。Stage が自分の12戦を持ちます。
   幕ボス・その公開法則・Stage が名乗る敵の家系は、**その Stage の12戦から導出**します。
   以前は `REGION` の値を placeholder として全 Stage が名乗っており、Stage を足した
   瞬間に嘘になる欄でした。
2. **敵を家系で組み直し、19体を追加**（灰塵・灰織・灰炉）。**新語彙は一つも足して
   いません。**公開済みの行動技能（薙ぎ払い・突き通し・引きずり出す・抉る・受け崩し…）を
   敵側から出しているだけです。家系ごとの出力（`FAMILY_POWER`）が Stage 帯を決める
   唯一のつまみで、個体表は「その家系の中での役割の差」だけを書きます。
3. **説明文と挙動の不一致を構造的に潰した**（下の §3.81）。
4. **難度曲線の物差し**（`analysis/ecology-campaign-curve.mjs`）。10 Stage を同じ
   基準編成で走らせ、圧力（1ラウンドあたりの被害÷隊の総HP）と分量（敵の総HP÷隊の総HP）
   を掛けた難度指数で並べます。単調増加と、一段あたり 5〜30% の伸びを検査します。

threat budget の梯子も言い直しました。旧判定は12戦を一列で単調増加と見ていましたが、
boss law の threatCost が boss の budget に乗るので、**幕の切り替わりで必ず一段
跳ね上がる**ことになり、敵の水増しでしか満たせません。いまは「通常戦の列」「ボス戦の列」
「各幕のボスはその幕のどの通常戦より重い」の三つに分けて見ます。旧 Stage 0 の数値は
この三つを全部満たしていたので、**契約を緩めたのではなく、boss law の重みに
引きずられずに言い直した**だけです。

### 3.81 説明文と挙動の不一致は、文を直すのではなく構造で潰した（作者指摘、2026-09-13）

作者は「1シナリオ目も敵の説明文と出てくる敵が違ったりしてますね」と書きました。
数えると三種類ありました。**どれも人が二箇所に書いていたことが原因です。**

1. **反響体の反応技能が production で空でした。**chassis が持っていた `damage_echo` は
   fixture の termination 立会人（費用なし・99回）なので、`enemies.mjs` の末尾で一律に
   剥がしていました。剥がした結果、「殴ると返ってくる」という噂・狙い・幕2ボスの法則
   「反響の法則」・精鋭戦「反響の坑道」が、**全部ただの殴り合い**になっていました。
   → 反応点1を払う `counter_blow` を持たせて、**文ではなく挙動のほうを本当にしました**。
2. **走り手と群れは「速い」と書いてあるのに、走者との差が maxHp だけでした。**
   → 速さは行動権（AP2）で表します。一撃は軽くしたので、受けの厚い相手には最も弱い、
   という裏も付きました。
3. **腕力・技術が全個体で同じ値でした**（中立40 × 出荷難度）。「重い一撃の砕き手」と
   「後ろから撃つ後撃ち」が同じ打撃力で、役割の違いが盤面に出ていませんでした。
   → 家系と役割ごとに三能力を分けました。

そのうえで、**狙いの説明文（`ENEMY_TARGETING`）を tactics から導出**することにしました
（`content/encounters.mjs`）。挙動を変えれば文が必ず追随し、文だけを変えることは
できません。噂と図鑑は引き続き人が書きます（そちらは規則の言い換えではないので）。

**未使用の敵も消えました。**走り手・潜み手・砕き手・籠り手・追い手・群れは定義と噂と
図鑑を持ちながら、12戦に一度も出ていませんでした。いまは Stage 1〜3 の主役です。

### 3.82 資金を投下しても強くならない問題（作者指摘、2026-09-13）

作者の言葉は「ギルドに戻った時の能力上昇も全然ダメですね。資金を投下しても全然
強くならない」でした。**比喩ではなく、数がそうなっていました。**

旧実装の鍛錬は **一段 +0.1%・一段 2,000** でした。腕力50のゴウを 1 上げるのに 20 段 =
40,000 掛かります。一方、遠征一回の実入りは rank 0 の完走で **3,800** でした。
つまり**一遠征ぶんの資金を全部つぎ込んでも、一能力が +0.19%** にしかならず、
丸めで表示も動きません。永続投資が「ある」だけで「効かない」状態でした。

両側を作り直しました。

- **鍛錬** … 一段 +8%、上限12段（+96%）、費用 1,000 × (段+1)。一枠を上限まで積むと
  78,000。四能力×五人の20枠を全部積むと 156万。
- **入り** … 通常240・精鋭450・ボス840、到達距離60/戦、完走1,500、初クリア1,800。
  これに **Stage 倍率**（Stage 9 で ×17.5）が掛かります。第一部を一度ずつ完走して**約64万**。
- **常設の強化**も帯を揃え直しました。旧値は目利きの上二段が 300,000 / 750,000、
  初期SPアップの3段目が 240,000 で、**第一部の総収入を超えていて一生買えません**でした。
  装備枠（2→3）と野営の手当て（回復量 +25%/段）を足しています。

64万に対して20枠全部で156万なので、**買えるのは全体の3〜4割**です。
「どこにどのくらい投下するか悩むフェーズ」は、この比から出ます。

**数は当てずっぽうではありません。**`analysis/ecology-campaign-curve.mjs` に
「投資の効き」を足しました。第一部の資金を宣言した順で使い切った隊で難度指数を
測り直し、生の伸び（Stage 3 → 9 で ×2.81）に対して投資後の伸びが ×1.83 へ縮むことを
見ています。**縮まなければ、難度だけが上がって投資が効かない後半に戻ります。**

Stage 倍率を **Stage 番号の一次式にしない**のは R8 §3.7 の決めごとです。いまは
`analysis/ecology-campaign-curve.mjs` が測った難度指数に合わせてあり、危ないところほど
払いが大きくなります。**同じ Stage を周回するより次へ進むほうが資金効率が良い**ので、
稼ぎのための周回で遊びが伸びません。

### 3.83 技能の名前を五つ直した（作者指摘、2026-09-13）

「味方のスキル名も『確かな斬り』とかいう意味不明な名前のスキルだったりする」。
名前が中身を指していないものを直しました。**ID は動かしていません**（表示名だけ）。

| ID | 旧 | 新 | 理由 |
|---|---|---|---|
| `steady_cut` | 確かな斬り | 踏み込み斬り | 「確か」が何を指すか読めない。前へ踏み込む武器攻撃だと分かる名前にした |
| `sweeping_barrage` | 連ぎ払い | 乱れ薙ぎ | 「連ぎ」は造語で読めない |
| `shatter_point` | 積もる刻印 | 積み撃ち | 刻印は「積もる」のではなく、積んでから叩く技 |
| `steady_breath` | 息を合わせる | 息を渡す | 「息を整える」と一字違いで別技だった。渡す系（刃を渡す・守勢を渡す）へ寄せた |
| `block_focus` | 受け返しの集中 | 受けて澄ます | 名詞を三つ並べた説明で、名前になっていなかった |

### 3.84 戦闘アニメーションを作り直した（作者要望、2026-09-13）

「戦闘アニメーションを全体的にもっとずっとかっこよくしてほしい」。

**足す前に、出ていなかったものが見つかりました。**味方が受けたダメージの数字は、
一度も画面に出ていませんでした。数字は箱の中（`.unit-floats`）に置いてありましたが、
味方の箱は立ち絵を切り抜くために `overflow: hidden` なので、箱の外へ昇る数字が
まるごと切られていたためです。敵側でも数字は**一つ上の箱の中**に出ていて、
誰が受けたのか読めませんでした。数字は盤面の層（`.battle-floats`）へ移し、
受けた箱の真上へ座標で刺すようにしています。

そのうえで足したのは次です。**拍は一つも増やしていません**（§8.14）。

- **重さの三段**。最大HPに対する割合（15% / 30%、撃破は最大）で、箱の揺れ・数字の
  大きさ・盤面の揺れが同じ段で動きます。絶対値で切らないのは、HP110の人とHP300の人で
  同じ50が違うことだからです。
- **向き**。踏み込みが狙った相手の列へ向かい（`--lunge-x`）、攻撃側から被弾側へ線が
  一本走ります（`.strike-line`）。狙われている箱には金の四隅が付きます。
- **手応えの層**（`.unit-fx`）。閃き・衝撃輪・斬線を箱の中だけで動かすので、
  盤面の並びは一切動きません。攻める側の斬線は金、受ける側は白熱で、光り方を分けています。
- **幕の帯**。戦闘の開始・ラウンドの頭・決着に、BATTLE START / ROUND n /
  VICTORY・DEFEAT・DRAW を一度だけ出します。
- **拍の行**。拍の種類で左の帯の色が変わり、行が変わるたびに光が一度横切ります。
- **盤面の空気**。上（敵）と下（味方）から差す光と四隅の影を敷き、カットインのあいだは
  一段深く沈めます。カットインには速度線と斬線を足しました。
- 最後の一行が `戦闘終了 · win` と engine の語のまま出ていたのを、帯と同じ
  「勝利 / 敗北 / 相打ち」へ揃えました。

`prefers-reduced-motion` では動きだけが止まり、帯・カットイン・照準・数字は出たままです。
`analysis/ecology-trial.mjs` と `analysis/ecology-tutorial-trial.mjs` を iPhone 実寸で
通し、横あふれが無いことも見ています。

### 3.85 ナギ加入時に「身代わり」を解禁する（作者指摘、2026-09-13）

`cover_ally`（身代わり）は実装済みだったが、`pack_wall` の core に置かれていたため、
実際に解禁されるのは Stage 2（ヒバナ加入時）でした。Stage 1で加入するナギの役割が
「庇護」なのに、加入時の初期リアクティブは Stage 2 の `shield_handoff` で、
味方への攻撃を直接引き受ける `cover_ally` が主力の初期装着にありませんでした。

`cover_ally` を `pack_edge` の coreへ移し、Stage 1のナギ加入時から解禁・初期装着します。
`shield_handoff` は `pack_wall` の coreに残し、Stage 2で「止めた結果を味方へ渡す」
発展形として続けます。ツリーの `brace_after_hit → cover_ally → shield_handoff` の前提順は
維持し、engine / schema は変更していません。manifestと初期技能の整合を検査するテストも追加しました。

### 3.86 庇護前倒し後の難度を敵側で再調整する（作者指摘、2026-09-13）

`cover_ally` を Stage 1 から使えるようにした結果、基準編成の Stage 3 が安全側へ動き、
難度曲線の Stage 3→4 が47%まで跳ねました。判定の上限を広げるのではなく、Stage 3 の
第7・8戦だけ敵HPを1.6倍・攻撃を1.05倍にし、Stage 8 以降の灰炉もHP・攻撃を底上げしました。
これで元の一段5〜30%判定は Stage 3→4 が29%、Stage 8→9が22%で通ります。

同時に、Stage 1 の入口技能数は10以内へ戻しました。`opportunist` と子の
`whetted_by_pain` は `pack_edge` の fullへ回し、ナギの `cover_ally` を残しています。
テストの許容幅を増やさず、Stage 3の三構成の実測値だけを新しい敵配置に合わせて記録し直しました。

### 3.87 先見機の窓から、未来を一戦ぶん試映できるようにする（作者要望、2026-09-13）

キャンプ上端の完全予測を、照準レンズ・計器の四隅・走査光を持つ先見機の投影窓へ改めました。
見出し行へ `試映` と `実戦` を収め、遠征本文にあった重複する実戦ボタンは外しています。固定領域に
新しい行を足さないため、下のスクロール領域はほぼ減りません。

`試映` は本番と同じ戦闘を replay と結果まで表示しますが、青緑の投影演出を戦闘・結果へ通し、
Run / Profile、HP、報酬、技能点、図鑑、戦歴を更新せず先見機へ戻ります。通し検査は試映前後の
保存された Run / Profile を丸ごと比較し、副作用が無いことを確認します。

### 3.88 テキストで指示するチュートリアルの押下誘導を補給にも揃えた（作者指摘、2026-09-13）

隊列と必殺技では、次に押す一箇所を光らせ、そこ以外を錠で閉じ、押したら次の段へ進む
手取り型がすでに成立していた。一方、補給だけは文章で「集中治療」を探させ、ボタンの強調と
個別の無効化を別経路で持っていたため、同じ種類の導入なのに操作の読み方が揃っていなかった。

補給にも `supplyTutorialStep()`（集中治療／対象選択の二段）と
`supplyTutorialSpotSelector()` を追加し、隊列・補給・必殺技を共通の `tutorialGate()`、
`applyTutorialGate()`、`tutorialAllows()` で処理するようにした。これで光る先、画面上で押せない
要素、handler 側の拒否が同じ選択子を読む。集中治療を押すまではそのボタンだけ、押したあとは
回復可能な負傷者の盤面セルだけが光る。対象を確定するまで補給は消費せず、治療結果と完了後の
状態は従来どおり同じ画面へ返す。

札の見た目も `tutorial-note-card`、`tutorial-steps`、`tutorial-spot`、`tutorial-blocked` の
共通 CSS へ揃え、画面の静的検査と通し試験に「手順1の治療ボタン」「手順2の対象セル」「光って
いない遠征タブを押しても留まること」を追加した。

### 3.87 決着の帯で再生を止め、次へ渡す道を一本にした（作者試遊、2026-09-13）

「戦闘終了後、victory or defeat 表示が出たところで止まって、『次へ』的な表示を出せますか？
『再生をとばす』も、文言をよりかっこよくした上で、次のキャンプまでとばすのではなく
『victory』表示まで飛ばしてほしい」。

3.31（issue #138）は「次へ進むためだけのクリック」を潰すために、再生が最後の拍まで
流れたら自動で次の画面へ渡すようにしました。手続きは確かに減りましたが、**その拍こそが
VICTORY / DEFEAT の帯**でした。帯は 0.66 秒で流れて消える作りだったうえ、消え際に画面が
入れ替わるので、勝ったのか負けたのかを読む間がありません。［再生をとばす］も同じ問題の
裏返しで、押すと戦闘ごと畳んでキャンプまで出てしまい、**勝敗の帯を一度も見ないまま**
次の一戦の準備画面に立っていました。

- **再生の終点を「最後の拍」から「決着の拍」へ移しました。**`endingBeatIndex()` が
  `kind === "ending"`（`battle_ended`）を探し、`atReplayEnding()` が終点判定を一箇所で
  持ちます。自動再生・一手送り・釦の出し分けが同じ答えを読むので、どこから来ても
  止まる場所は同じです。
- **決着の帯は消えません。**`battleBannerFor()` が ending にだけ `hold` を返し、
  `banner-*-hold` の keyframe が開いた状態で止まります。帯が残っているあいだは
  `.battle-field.verdict-hold` が拍の行（同じことを言う一行）の見た目を譲らせます。
  読み上げは拍の行（aria-live）が持ったままなので、耳で追う側は変わりません。
- **［再生をとばす］は［一気に決着へ ▶▶］になりました。**行き先は次の場面ではなく
  決着の帯で、押しても戦闘画面から出ません。飛ばした先の盤面・HP・履歴は拍から
  引き直すので、一手ずつ進めた先とまったく同じものが出ます。
- **次の場面へ渡すのは［次へ ▶］だけです。**`goToBattleResult()` を呼ぶ経路をこの一本に
  寄せたので、序盤の会話の既読印（R12 の `prologueClear`）を飛び越える穴も開きません。
  前進の釦は常に一つだけ出ます（決着の前は［一気に決着へ］、決着からは［次へ］）。
  決着では自動再生の釦から金色も外し、押すべき一つを迷わせないようにしています。

灰の門の一戦だけは `truncateAtFall` で倒れた拍まで切ってあるので（R11 §5）ending 拍を
持ちません。終点は末尾の「倒れた」拍へ落ち、帯は出ないまま［次へ］で会話へ渡ります。
`analysis/ecology-tutorial-trial.mjs` は、その一戦で**押さずに待っても次へ進まない**ことと、
巻き戻したあとの勝利で**VICTORY の帯が残ること**の両方を見ています。

### 3.88 キャンプの操作に、短い反応を一つずつ返した（issue #237 / 作者要望 2026-09-13）

「ゲーム全体に細かいアニメーションをつけてください。ゲームらしく、雰囲気を統一して、
かっこよくしてほしいです」。

戦闘（3.8x の手応え・カットイン）と物語（巻き戻し・立ち絵）には動きがあるのに、
**遠征の中心の操作があるキャンプだけが静かに切り替わっていました。**タブを移る、仲間を
選ぶ、隊列を入れ替える、技能を取る・入り切りする、装備を着ける、補給を使う、報酬を
引き直す——押したことが通ったのか、何が変わったのかが画面から返ってきません。

画面は `render()` が毎回まるごと描き直すので、描き直したあとの要素は全部が生まれたてで、
前の姿を持っていません。**「いま何が変わったか」を知っているのは操作の側だけ**です。
そこを二つの口で埋めました。

- **`fx(key, kind)`** … 離散な出来事を、操作した本人が申告します。印は `data-fx`。
  立ち位置（`cell:front_left`）・技能（`skill:<id>`）・装備枠・タブのように、
  **何が変わったかの名前**を key にします。隊列を入れ替えた回は動いた枠を二つとも
  鳴らします（片方だけだと、どこから来たかが消えます）。
- **`data-fx-watch`** … 数の変化は、読み値のほうが自分で見つけます。前の描画と文字列が
  違った回だけ、増えたら金（`fx-up`）、減ったら赤（`fx-down`）で光ります。
  **どの操作がどの数へ響くかを操作の側へ書き写しません。**書き写すと、書き漏らした経路
  だけが黙り、しかも誰も気づきません。装備を替えれば盤面の予測HPが、技能を取れば技能点が、
  一戦終えて戻れば補給・装備耐久・現在地が、申告なしで動きます。

予測だけは読み値ではなく予測そのもの（`forecastSignature()`）を見張ります。窓の文字だけを
見ると「勝利・3ラウンド」のまま終了HPだけが上がった回を取り落とすからです。動いた回は
先見機の走査線が一度速く通り、**窓が読み直したこと自体**を出します。

時間は `--fx-tap` / `--fx-quick` / `--fx-step` / `--fx-accent` の四段しかなく、曲線は戦闘の
手応えと同じものを使っています（キャンプと戦闘が同じ速さの世界に見えます）。**横に揺れるのは
「拒まれた」だけ**にしたので、足りない・できないを成功と取り違えません。危険（このまま挑めば
負ける）には速い動きを使わず、窓の縁が 2.8 秒周期でゆっくり脈を打つだけにしました。

貼りついた上端（`.camp-top`）は立ち上げません。固定領域が毎回跳ねるとタップ対象が動きます。
`prefers-reduced-motion` では動きだけを止めます（色・記号・数・ラベルは元から動いていません）。

### 3.89 試映と実戦を、先見機の窓の下へ大きく置き直した（作者指摘 2026-09-13、二度目）

「先見機ウインドウの『試映』『実戦』を右上ではなくウインドウの下に配置してください。
大事なボタンなのに、いまは小さすぎます」。

3.8x（8.7.12）でこの二つを見出し行の右端へ畳んだのは、固定領域の高さを新しい段で増やさない
ためでした。意図どおり高さは増えませんでしたが、**この窓で一番大事な操作が、画面で一番
小さい釦**になりました。

窓の下に一段取り、二つを等分で並べ、高さ 44px（指で狙う的の下限）を下回らせない形へ
変えました。増えた一段は二字の見出しだけでなく、「結果を先に見る」「この敵へ進む」という
一行を添える余地に使っています。固定領域は約 50px 伸びましたが、伸びたぶんがそのまま
一番押す操作になりました。通し試験は、実戦の釦が**盤面より下にあること**・高さが 44px 以上
あること・二つが同じ幅で並ぶことを実寸で見ています。
### 3.90 技能の値段と見せ場を揃え直した（issues #190 / #189 / #128、2026-09-13）

作者指摘は三つだった。前提9点の「捨て身の一振り」が基礎攻撃より弱い、後列減衰を受けず
回復・防壁とも同じ技術を伸ばせる技術攻撃が腕力攻撃と同倍率、溜め突きが2行動550%なのに
同時期の狙い撃ちLv2は140%。さらに隙・怯み・守勢・裂傷は固定値が短く一度しか効かず、
殴るほうが早かった。取得点だけでなく前提の閉包と出現Stageを数えない調整が原因だった。

- 深い腕力攻撃を引き上げた。散らし斬り195%、二の太刀240%、捨て身275%、手負い285%。
  捨て身Lv1は斬撃Lv10（249.6%）を越え、自分へ隙を付ける代償にも見返りがある。
- 技術攻撃を90〜145%へ抑え、溜め突き240%/2行動、大溜め500%/4行動へ変更した。
  一行動平均は120%/125%で、安定した腕力攻撃130%を越えない。一撃へまとめる価値は残る。
- 隙・怯み・守勢を1段20%として多段の各hitへ適用し、集中は次のdamage/heal/barrierを50%、
  裂傷は1段につき最大HP5%の受け無視へ変更した。固定量だった「衝撃を殺す」も一撃の25%にした。
- 位置替え→自分へ守勢、陣を組み直す→前へ出した味方へ守勢、引きずり出す→敵へ隙を追加した。
  踏み固め等の `actor_moved` 反応と同じ一手で連鎖し、入る・戻す・引くの三方向を作った。
- 号令・背を押すは追い打ち無しのchannelにし、自分以外だけを対象にした。身代わり、余剰治療、
  状態付与反応も、有効な他者・負傷者・上限未満の対象がいるときだけ資源を払う。
- 発生しないAP余りを待っていた「余りを溜める」は、AP2の一手目の直後を読み、二手目へ集中を
  渡す常設へ変更した。深い「狙いを澄ます」は集中に加えてRP1も整え、早い節と役割を分けた。

`analysis/ecology-skill-balance-smoke.mjs` に係数だけでなくproduction戦闘のevent列を使う回帰を
追加した。3hitすべてへの状態効果、裂傷の最大HP参照、AP2の一手目→二手目、位置替え→踏み固め、
自己標的の身代わり不発を検査する。Stage 3の三構成は全て第6戦まで通る。突出していた溜め/AP構成は
旧12戦完走から第7戦時間切れへ下がったが、第6戦までに固有の準備・資源イベント列は成立している。

### 3.90 先見機の読み直しを、光らせるのではなくブラウン管の同期外れにした（作者指摘 2026-09-13、三度目）

「特に先見機のアニメーションがやや納得行っていません。せっかく先見機ウインドウには
メカニカルな表現が掛かっているので、変更があったところはブラウン管テレビのように
ジジッと左右にブレてから変更されるか、それが難しければ別の何らかメカニカルで
SFチックな表現になってほしいです。いまはウインドウと数字が一瞬光るだけで、全く
機械っぽさがなく、大変さみしい」。

3.88 で予測が動いた回に出していたのは、窓の縁の一度の発光（`fx-rescan-frame`）と、
数の色の残光（`fx-up` / `fx-down`）だけでした。**「何かが変わった」とは言えていましたが、
言い方が装置のものではありませんでした。**発光はこの画面のどこでも起きる反応なので、
先見機だけが持っている「機械が像を作り直している」という手触りが乗りません。

読み直しを、四枚の層でブラウン管の同期外れとして出し直しました。

1. 像を引き直す硬い線が一度降りる（`fx-rescan`）。常時ゆっくり流れている一本とは
   **別の面**へ置きます。同じ面を class で塗り替えると、動きが終わったあとも明るい線が
   上端へ居座ります（class は次の描画まで外れません）。
2. 面の横の目が濃くなって転がる（`fx-crt-roll`）。目の濃さは `.forecaster-scan::before`
   の一箇所だけが持ち、読み直しの回は opacity を上げて戻すだけにしています。
3. 見出し行と盤面が、**別の振り幅・別の拍で**左右へ飛ぶ（`fx-crt-desync` /
   `fx-crt-tear`）。同じだけ動かすと「窓ごと揺れた」になり、帯ごとに像がずれる
   ブラウン管には見えません。
4. 変わった数だけが赤と青緑へ色分離してブレ、**前の値が横へ千切れてから**新しい値が
   座ります（`fx-crt-value` / `fx-crt-ghost`）。影に入れる文字は `readoutValues` が
   既に持っている前の描画の読み値なので、新しく覚えるものはありません。

曲線は `steps(1, end)` です。**ブラウン管は補間しません。**滑らかに寄せると「光った」に
戻り、ジジッという当たりが消えます。時間は他の反応と同じ `--fx-accent` 一段のままで、
終わるのを待たずに次を押せます。

三つ、意図して動かさなかったものがあります。**釦（`.forecaster-actions`）は左右へ飛ばしません**
——ブレている最中に押される的なので、貼りついた上端を動かさないのと同じ理由です。
**窓の外の読み値はブラウン管にしません**——タブの札の数まで割れると、画面中が壊れて見えます。
**読み値の差し替えそのものは遅らせません**——遅らせると、速く押した回に古い数が残ります。
消えるのは影のほうだけです。

あわせて、作者の疑い（「もしかして PR268 で誤って消えました？」）を確かめました。#266 の
走査線は消えていません（`git show` で差分を確認）。ただし窓は 3.89 で釦の一段ぶん高くなり、
**一本だけでは面がブラウン管であることが読めなくなっていました。**面そのものへ横の目を
薄く敷いたのはそのためです。

`prefers-reduced-motion` では、前の値の影と走査線を**置くこと自体をやめます。**止めた姿が
残るほうが、動かないことより読みにくいからです。

### 3.91 各packへ無料反応と条件付き常設を足した（issue #271、2026-09-14）

各6packへ、AP/RPを消費しないリアクティブ2本と条件付きパッシブ2本、合計24本を追加した。
裂傷・怯み・隙・守勢は、付けた後に追加段、条件倍率、集中、別の防御へつながる。
準備・移動・瀕死・前列・RP残量も発火条件に使い、弱めのアクティブをコンボの発生源として残した。
条件付きアクティブを含め、既存技能は削除していない。

基礎能力4種はLv10まで取得可能にした。既存Lv1の値は保ち、Lv2以降を最大HP+10／
腕力・技術・受け+1ずつ伸ばして、技能点の余りを小さな確実性へ換えられるようにした。
技能Lvは戦闘効果だけでなく `statBonusPerLevel` にも届き、予測・入力検証・本戦・画面上限が
同じ `skillLevels` を読む。

回帰は定義の本数では終わらせずproduction battleのevent列を読む。裂傷3段、瀕死治療から守勢、
溜めから守勢と重撃、刻印2段から三連撃・裂傷・集中、隙の引受から敵の怯みと味方の受け構えが
実際に成立することを固定した。

### 3.92 文章で説明する画面をやめた（作者要望 2026-09-13）

作者の指摘は一行だった——**「文章で説明するゲームはダサい（会話を除く）。文章の多いページを
全部かっこよくしてください。ギルドページとか、敗北画面とか。」**

issue #236（8.7.12）で画面本体の重複と冗長は既に削ってあった。だから残っていたのは、
削ると事実が消える説明である。ギルド投資の三段落（精算の仕組み・投資先の二種類・全部は積めない）、
敗北画面の「この組み合わせでは届かなかった……」、精算の「持ち帰るのは活動資金と設計図だけです。
技能点・解禁・装備・補給はここで消えます」、戦闘の「表示の説明」四段落、遠征準備の
「3幕12戦。4・8・12戦目にボスが立つ」——どれも本当に要る。

そこで削る代わりに、**事実が持っている形で出す**ことにした。位置は目盛り、量の比は棒、段は
12個までの目盛り、順序は矢印の帯、対は二列、分類は記号つきの一段、色と形そのものは同じ色と形の
見本。共通語彙は `app.js` に一箇所だけ置き、画面ごとに作り直さない。

段落のまま残したのは、会話・世界の側の一行（敗北の一行、精算の締め、報酬の一行、名簿と図鑑の
本文）・content が持つ説明文の三つだけである。

この付け替えの途中で、**画面へ書き写した数が二つ実装とずれていた**ことが分かった。ギルドの
「一段で基礎値の6%、上限まで72%」は R23 の実装（一段 8%・上限 96%）と合っておらず、設計図
アーカイブの「勝利2件・安全撤退2件・敗北1件」は PR #255 の上限（勝利1・撤退0・敗北0）と
合っていなかった。文が定数を書き写していたのが原因なので、図へ移すときに定数から引くようにした。

あわせて表示欠陥を一つ直した。Stage の学び（`learningGoals`）は content の `**強調**` を
そのまま出していて、画面に星印が並んでいた（`emphasize()` で太字にした）。

なお、野営治療の行は投資の行と同じ `.purchase-row` を共有している。投資の行を三列
（記号・中身・値段）へ組み替えたときは治療の行も一緒に組み替える——片方だけ直すと、
治療の文字が記号の枠へ押し込まれて一列に潰れる。


### 3.93 手取りの順を「隊列 → 技能 → 補給」にし、札を一枚の作りへ揃えた（作者要望 2026-09-14）

作者要望 —「いまのシナリオ1は 一戦目前: 隊列 / 一戦目後: 補給 だが、一戦目後: スキル取得・
予約、二戦目後: 補給にしたい。ついでに、主にデザイン面でチュートリアルを改善してほしい」。

**入った点より先に、まだ一度も減っていない資源の使い方を教えていた。**技能点は一戦ごとに
全員へ1点ずつ入るのに、その使い道（ツリー・取得・予約）は畳んだ「技能のルール」の中にしか
無く、開く人にしか届かなかった。一方で補給の手取りは第1戦の直後に必須で入っていた。

そこで第1戦の直後を技能へ渡し、補給を第2戦の直後へ送った（DESIGN.md 6.4.6）。技能
チュートリアルは既存の手取りと同じ形で、四手のあいだだけ錠が掛かる。

  節「まとめて手当て」を押す → 「解禁」で1点を払う → 節「長く守る」を押す → 「Lv1まで予約」

**教えるのは取得と予約の違いだけにした。**予約の相手は「点が足りないだけの節」ではなく、
前提の段（傷へ盾を Lv3）が足りない節にしてある。前者では予約が一回ぶんの貯金にしか
見えないが、後者なら**前提の段上げから順に自動で払う**ことが、次の数戦のあいだ画面の側から
読める。教える二つの節は content の `SKILL_LESSON.tutorial` が持ち、app.js は技能 ID を
書き写さない（灰の門・塞ぐ二枚と同じ作り）。完了印 `skill_lesson_seen` は、`done` の段
（予約は取り消せる、を言う一枚）を残すために**次の一戦へ出るとき**に押す。

補給を後ろへ送ったぶん、手取りに入る時点で二戦ぶんの傷が溜まっている。「傷は次の一戦へ
持ち越す」が、札の文ではなく盤面の数字として立つようになった。

導入の遠征を指す画面状態は `supplyTutorialRunId` から `tutorialRunId` へ改名した（補給だけの
印ではなくなったため）。古い保存は `hydrateState` が読み替えるので、進行中の導入遠征が
印の無い普通の遠征へ落ちることはない。

デザイン面では四つ直した（DESIGN.md 6.4.7）。

- **札の組み立てを一つにした**（`tutorialNoteCard()`）。以前は四つが別々に組んでいて、
  手順 n/N が出るのは補給だけだった。いまはどの手取りでも「あと何手あるか」が読める。
- **段の一覧を横一列にした。**縦に積むと、四手ある技能チュートリアルで札が本文より高くなる。
- **錠の最中だけ札を貼りつけた。**技能の押し先は本文をかなり送った先にあり、静かな札は
  そこで画面の外へ出る。錠が外れた段では普通の一枚へ戻す（自由な探索で高さを食わない）。
  貼りついた札の高さは `--tutorial-note-h` として渡し、技能点の要約帯はその下へ貼る。
- **光る先が窓の外なら寄せる**（`focusTutorialSpot()`）。押せる場所が一つしか無いのに
  画面の外にあると、錠は「探させる仕掛け」になってしまう。

あわせて表示欠陥を一つ直した。技能の説明文も content の `**強調**` をそのまま出していたので
（「HP50%以下の味方**全員**へ」など8件）、Stage の学びと同じ `emphasize()` を通した。


### 3.94 手取りを「タブを押す」から始め、「自分で決める」で終わらせた（作者要望 2026-09-14、二度目）

3.93 の四手・二手を作者が試遊して、五つ返ってきた。

- **技能: 「まとめて手当て」の前に、スキルタブとツグミを押させる。**キャンプが勝手に
  技能タブを開いていたので、**その画面がどこにあるのか**と、**点が人ごとの持ち物である**
  ことを一度も言っていなかった。一手目「スキルタブを押す」、二手目「ツグミのセルを押す」
  を足して七手にした。二手目が空振りしないよう、キャンプへ戻る時点では教える相手以外を
  選んでおく。
- **「長く守る」を押す前に、操作盤の「✕」も押せるようにする。**——「でないと画面が狭すぎて
  スキルツリー上で『長く守る』を選べない」。取得した節の盤は地図の下端に貼りつくので、
  iPhone の窓では次に押す節がその裏に隠れる。錠は「光っている先だけ」なので、このとき
  画面が詰む。段ごとに**光らせないが通す先**（`allow`）を持てるようにし、`tutorialOpenings()`
  が「光る先＋通す先」を一つの選択子に畳む（画面の錠と handler の錠は同じ文字列のまま）。
- **予約の値打ちを一行で言う。**「この後戦闘を繰り返しても都度スキルポイントを振らなくて
  いい」。想定されている遊び方は、先に行き先だけ決めて数戦を飛ばし、負ける予測が出た回に
  初めて手を入れる軽い往復である（DESIGN.md 6.4.6）。
- **最後にゴウを押させて、そこで手を離す。**「ゴウのスキルを悩んで決めて、自由に選んで
  戦闘開始、としてほしい」。受け渡しの一手で錠が外れ、そこから先は**何も光らせない**。
  光らせる先を置くと、渡したはずの判断を画面が奪い返す。錠が外れたあと誰を選び直しても
  段が戻らないよう、受け渡しの印は画面状態（`skillLessonHandedOff`）で覚える——画面の状態
  から導くと、教えた相手をもう一度押した拍に錠が復活してしまう。
- **補給は「補給というものがある」から教える。**こちらも一手目を「補給タブを押す」にし、
  その札で、この遠征へ持ってきた3個だけの道具であること、再挑戦・引き直し・野営治療が
  同じ3個を取り合うこと、戦って増えないことを言う（三手になった）。

あわせて CI（公開先の通し）で落ちていた `門は途中の行では出ない` を直した。文字送りの
途中で叩くと、その一度は「全文を出す」に使われて行が進まない。**叩いた回数に検査を
縛っていた**ので、回線ぶんだけ遅い公開先では 2 / 3 に居るとは限らなかった。中ほどの行へ
着くまで叩いてから見るようにした（`tapUntil`）。


### 3.95 攻撃の絵を、腕力＝斬撃と技術＝銃撃に分けた（作者要望 2026-09-14）

「戦闘時の攻撃アニメーション、腕力スキルと技術スキルで分けられませんか？ 腕力は斬撃、
技術は銃撃のイメージです」。

3.84 で作った手応えの層は、**どの攻撃も同じ斬線**で出していました。踏み込み斬りも
狙い撃ちも、同じ刃が箱を横切るので、盤面からは「誰が殴ったか」は読めても
「何で当てたか」は読めませんでした。

新しい軸は作っていません。R11 §5 が既に「武器は後列から届かない、技は届く」を
数字の側へ入れてあるので、**その違いをそのまま絵にした**だけです（DESIGN.md 8.14.1）。

- 型を決めるのは `ecology/attack-style.mjs` の純関数で、読むのは engine が出す着弾
  イベントの `tags` と content の定義だけです。時計も乱数も入らないので、同じ seed から
  同じ絵が同じ順で出ます。新しい event も新しい拍も増えていません。
- 画面の側に「この技能は銃」という手書きの表は置きませんでした。技能を足すたびに
  更新を忘れる側が必ず出て、そのとき黙って斬撃になるからです。
- 攻撃でないダメージ（裂傷の刻み、装備の破片）には型を付けません。誰も振っておらず
  撃ってもいないので、絵が嘘を言わないように既定の被弾のまま出します。

実装は class を三つ増やしただけで、絵は styles.css が持ちます（`strike-technique` /
`hit-technique` / `.strike-line.technique`）。`prefers-reduced-motion` では動きを止めますが、
**弾着の火花だけは出したまま**にしました——止めた画面でも、線と火花でどちらの型かは読めます。

作者の実機試遊で**出ていませんでした。**初稿は型の差を箱の中（`.unit-fx`）だけで描いて
いたので、100×72 の枠と丸角に切られ、残ったのは「どちらも光る線が一本走る」だけでした。
印を盤面の層へ、箱よりひと回り大きく置き直し、**光り方ではなく形で分け**ました——斬撃は
交差する二本の太刀（×）、銃撃は芯から棘が伸びる星です。色や明るさの差は、300ms の拍と
箱の小ささに負けます。銃口の閃光も線とは別の札にしました（線は `clip-path` で削られながら
走るので、同じ札に乗せると閃光まで切り落とされていました）。

さらに二つ、作者の試遊から戻して直しました。

- **線の飛び方は型で変えない。**「従来の攻撃モーション（相手に線が飛ぶ）はかなりかっこいい
  ので、これをベースに、これプラス斬撃モーション or 弾着モーション」。初稿は銃撃だけ光を
  一粒ずつ走らせる弾道にしていましたが、同じ距離が遅く見えて速さが消えていました。線は
  どちらの型でも同じ keyframe で飛ばし、銃撃は太さと色だけ細く白くしています。
- **切り分け場を作った**（`ecology/fx-test.html`）。「自分の環境の問題かもしれません」。
  線と印を `ecology/battle-fx.mjs` へ切り出し、見本の画面が**一戦も進めずに本番と同じ
  関数・同じ CSS で同じ絵を出す**ようにしました。画面の先頭には、絵が出ない側の原因に
  なりうるもの——「視差効果を減らす」の入切、build の印、型の表——を先に出します。

四度目の試遊で、二つ戻ってきました。

- **撃破の拍で攻撃の絵を二度出していた。**`actor_defeated` は着弾とは別の拍なのに、
  被弾と同じ枝で `is-hit` と型の class と印を付け直していたので、一度の攻撃が二度
  当たったように見えていました。撃破の拍で出すのは「倒れた」動きだけにしました。
- **斬撃が「ペタッと出るだけ」だった。**×を静止した形として出していたためです。
  一の太刀・二の太刀の順に**上から下へ引かれ**、同じ向きへ消えるようにしました。
  引き方は飛ぶ線（`.strike-line`）と同じ clip-path の作りで、盤面の動きの語彙を
  増やしていません。

### 3.96 上部の共通ボタンを画面内のカード／タブへ収めた（作者指摘 2026-09-14）

3.55〜3.56 でキャンプの固定帯からセーブと撤退を遠征タブへ移したあとも、共通の
`shell()` が戦闘・結果・セーブ画面などへ「安全に撤退する」「戻る」を上部へ足し、
ギルド画面にも上部の「タイトルへ」が残っていた。

- `shell(body)` を本文だけのラッパーにし、上部の共通操作を生成しないようにした。
- セーブ画面の戻る操作はカード末尾、ギルドの「タイトルへ」は各タブ内容の末尾へ移した。
- キャンプの「遠征」タブにあるセーブ／安全撤退はそのまま遠征単位の操作として残した。
- 画面スモークと公開先通しに、上部導線が再発していないことを確認する検査を足した。


### 3.97 ギルドとキャンプで、その Stage の全12戦を完全開示した（作者要望 2026-09-14）

これまではギルドで三体の幕ボスと法則だけ、キャンプで次の一戦だけを見せ、先の通常戦・精鋭戦の
個体編成は意図的に伏せていた。作者の狙いは、技能を取る時点で将来の敵まで材料にし、戻せない技能点の
配分を悩ませることだった。そこで伏せる設計を撤回し、ギルドとキャンプの両方へ同じ「全戦投影」を置いた。

12個の節はすべて押せ、選ぶと同じ投影面へ戦闘名・種別・危険度・最大ラウンド・ボス法則・敵の3×2配置が
走査される。敵を押すと、HP・腕力・技術・受け・AP・RP、変異、行動／反応／常時技能の名前と効果、
技能列から導出した味方の狙い方が一体分だけ開く。説明段落で機能を案内せず、押せる節・青緑の走査・
選択枠の移動で「先を覗く装置」にした。`prefers-reduced-motion` では走査だけを止め、情報は減らさない。

盤面は `composeEncounter`、技能は `PLAYABLE_CONTENT.enemyActors` と `componentInfo`、狙いは
既存の tactics 導出を直接読む。先見機用の別データや画面専用の敵能力表は増やしていない。
閲覧中の戦闘と敵は保存しない UI state で、戦闘進行・seed・Run / Profile は一切変えない。
`analysis/ecology-trial.mjs` はギルドとキャンプの双方で12戦目まで選べること、敵詳細に能力6軸・
使用技能・狙い方が揃うことを通しで確認する。

### 3.98 全戦盤で、踏破記録と未来予測を分けた（作者指摘 2026-09-15）

3.97の初版は、選んだ戦闘が過去でも青緑の走査線を出し、通常見出しの「全戦投影」を
上に置いていた。直前戦の要約も別カードのまま全タブに出たため、何が先見機の予測で
何が確定した戦歴なのか、見た目の境界が無かった。

- 現在地と未到達戦だけを `forecast` とし、端末枠・信号・走査アニメーションを付けた。
- 踏破済みは `record` とし、走査を止めた通常カードと緑の確認印へ切り替えた。
- 通常の「全戦投影」見出しを廃止し、未来では `FUTURE SCOPE` の機器表示、過去では
  `BATTLE RECORD` の記録印が同じ位置を使うようにした。
- 全タブ共通の `LAST BATTLE` カードを外し、選択中の一戦へ技能点・戦闘後HP・
  ラウンド数・味方HP損失の4枠を置いた。技能点とHP処理は規則から、踏破実績は既存の
  `run.results` から読む。未踏破ではラウンドと損失だけを「？」にする。
- 「敵N体・配置」の `details` を外し、敵盤面と選択中の一体の詳細を常時表示にした。

新しい戦歴 state は増やしていない。公開先通しは未来にだけ機械演出があること、踏破済みが
通常表示へ戻ること、4指標が実績へ置き換わること、敵欄にトグルが無いことを390×844で確認する。

### 3.99 上下の先見機を一つの窓に揃え、投影から文章を落とした（作者要望 2026-09-15）

3.98 まで、キャンプ上端の戦闘予測（`.forecaster-window`）と全戦盤（`.encounter-archive`）は
同じ先見機の像なのに、別の機械に見えていた。上端は照準レンズ・角の括弧・走査線を持つ窓、
全戦盤は `FUTURE SCOPE / LINK ACTIVE` の銘を掲げた端末カードの中に、さらに枠付きの投影面が
入る三重の箱だった。作者の指摘は「同じ先見機なのだから揃えてほしい。上の方がイメージに近い」。

- 全戦盤へ `.forecaster-window` をそのまま着せ、下地・角の括弧・走査線・縁を上端と共有した。
- `encounterConsole()` の二段の銘を捨て、上端と同じ `.forecast-head` の並びにした。左に照準
  レンズ（踏破済みは緑の確認印）と「第N戦 · 名前」、右に「NN/12」だけを置く。
- 投影面 `.encounter-projection` から枠と下地を外した。窓の中身が箱の中の箱にならない。
- 未来は青緑の読み値と走査、踏破済みは走査を止めた緑の窓、という 3.98 の区別は残した。

同時に、投影の文章をすべて落とした（作者要望「全体的に文章がまだまだ多い」）。

- 「第N幕 · 種別 · 危険度 x / y · 最大NR」の銘を廃止した。種別は節の◆★が、進み具合は
  「NN/12」が既に言っている。
- 区画の説明文（`encounter.description`）を出さなくした。敵の配置・HP・狙いが同じ面に
  出ている以上、「後列を狙う後撃ちが混じる」は盤面の言い直しだった。
- ボス法則の解説（`boss-law` と `counterChips()`）をまるごと外した。唯一の使い手だったので
  関数と CSS も落とした。
- 遠征の敵盤面から赤い側名札「敵」を外した。ここには敵しか出ないので、区別する名札が要らない。
- 敵の技能名を `componentLabel()` から引くようにした。敵側の `enemy_heavy` / `front_strike` は
  `COMPONENTS` に居ないため、`componentInfo()` だけを見ると画面へ内部 ID が出ていた。

投影に残るのは4指標と敵盤面だけで、engine・content・保存データは変えていない。画面スモークは
窓を共有していることと、落とした文章が戻っていないことを見る。公開先通しは、上端と全戦盤の
下地・角の括弧・縁が実際の描画で一致すること、幕と危険度の銘とボス法則が出ないこと、
敵盤面に側名札が無いことを390×844で確認する。

全戦盤が窓になった副作用を一つ直した（作者指摘 2026-09-15、PR #279）。見出しが
`.forecast-head` の `z-index: 2` を持ったのに、窓がその重なりを閉じていなかったため、
貼りついたチュートリアルの札（`.tutorial-note-card.pinned`、同じ z-index 2 で DOM 上は前）
の手前へ見出しが出ていた。上端の窓は `.camp-top`（z-index 3 の sticky）が既に文脈を
作っていたので、漏れていたのは全戦盤だけである。`.encounter-archive` へ
`isolation: isolate` を入れて、窓の中の重なりを窓の中で閉じた。チュートリアル通しは、
札と見出しが実際に重なる送り位置で `elementFromPoint` を突き、一番手前が札であることを見る。

## ギルド画面の整理（2026-09-15）

作者指摘は四つ並んでいた——「同じことが何度も書いてある」「テキストが多すぎる」
「タブが整理されていない」「アニメーションが全然ない」。調べると四つとも同じ一つの
怠りの面で、**キャンプで三度やった整理（8.7.10〜8.7.12.1、8.18）を、ギルドへ一度も
適用していなかった。**判断の理由は `docs/DESIGN.md`「ギルドは五枚の札を持つ一つの画面に
する」に置いた。ここには測った数だけ残す。

- 札が二段に割れていた。`.tabs` が 4 列固定で、五枚目（図鑑）だけがぶら下がっていた。
  列数を `--tab-count` で渡し、一段に収めた。
- 活動資金は札と見出しで二度、根城の人数は札・見出し・名簿の見出しで三度、設計図の
  持込枠は札・見出し・規則の段で三度出ていた。**資金は貼りついた上端（`.guild-top`）に
  一つだけ**にし、他は「その中にいくつあるか」で単位を揃えた。
- 行き先の札が同行者を名前で並べていたので、全員が揃ったあとの第2章は同じ一行を
  6 枚繰り返していた。名前は選んだ一枚ぶん（`stageCastStrip`）へ移した。
- 根城の札は 7,901px（iPhone の画面 12 枚ぶん）あった。会った全員の名簿を同時に
  開いていたためで、一度に一人ぶんへ改めて 3,208px になった。選ぶ相手は鍛錬と共有する
  （`state.guildCharacter` 一つ）。
- 遠征タブの並びを「どこへ行くか → 何を連れて何と戦うか → 出る」に直した。旧版は
  行き先の選択が全12戦の投影の下にあり、変えるたびに画面を往復していた。
- 反応の語彙は増やしていない。`fx-pick` / `fx-select` / `fx-mode` / `fx-down` は
  キャンプが既に持っていたもので、足したのは節が一枚ずつ着地する `guild-card-enter`
  だけである。貼りついた上端は動かさない。

engine・content・保存データは変えていない。画面スモークはギルドの反応と札の作りを見る。
通しは 390×844 で、札が一段に収まること・活動資金が画面に一つだけであること・
仲間の帯が 5 人とも見えていること・横スクロールが出ていないことを確認した
（`ecology-trial` 183/183、`ecology-tutorial-trial` 277 手すべて通過）。

## ギルドの導線の組み直し（2026-09-15、二度目）

前項でギルドの中身を整理したあと、作者はカスタマージャーニーで指摘し直した。
「①遠征が終わって②会話のアップデートを見て（③軽く図鑑を眺めて）④資金を使って
⑤設計図をセットして⑥最新のシナリオに進む、ときに、最初と最後で同じタブを二度
タップする必要があったり、タブの順番がバラバラだったり、実用的なタブとフレーバー的な
タブが混ざっていたり、遠征に進むボタンがめっちゃ下にあったりします。」

判断の理由は `docs/DESIGN.md`「札の並びは、人がたどる順そのものにする」に置いた。
ここには測った数だけ残す。

- 札の並びをジャーニー順（根城・図鑑 │ 投資・設計図・遠征）にし、開く札を出口の遠征から
  先頭の根城へ移した。通しで測って **4 タップ・同じ札の重複なし**（旧版は 5 タップで
  遠征を最初と最後に押していた）。
- 読み物（根城・図鑑）と仕度（投資・設計図・遠征）の境目へ縦線を一本引いた。**分ける
  ために順は崩していない。**
- 遠征の札から全12戦の投影・「今回の遠征」・「札の読み方と、この区画で分かること」を
  落とし、釦を札の頭へ出した。札は 1,931px → 1,256px、釦は上から 348px（iPhone の
  第一画面の実効 660px に入る）。
- 行き先の一覧は釦の下の「行き先を変える」へ降ろし、選べる先が一つしか無い回は節ごと
  出さない。選び直した回だけ出発の一枚を寄せる（`focusLaunchCard`）。
- `learningNotes()` と `.learning-notes` を撤去した（唯一の使い手が消えたため）。
  content の `learningGoals` は区画の定義として残している。
- ギルドの投影を落としたぶんの検査は、通しのキャンプの節が全部踏んでいる（同じ
  `encounterArchive` を共有しているため、検査を二重に置く必要が無い）。

engine・content・保存データは変えていない。

## ギルドの見出し・敵一覧・隊の一覧（2026-09-15、三度目）

作者が実機の写真つきで三点を指摘した。判断の理由は `docs/DESIGN.md` の三節
（「札の名で言えることを…」「敵一覧はギルドに要る…」「『誰が足りないか』は問いで…」）
に置いた。ここには測った数だけ残す。

- **札を言い直す見出しを五つ落とした**（遠征へ出る・資金を使う・残した品の設計図・根城・
  会った灰殻の記録）。見出しの右にあった読み値は札の meta が引き取り（設計図＝持込 0/1、
  根城＝8箇所）、名簿の人数は二つ目の節の見出しへ移した。札の外で出す根城の一枚だけは
  `homesteadBody({ titled: true })` で見出しを保つ。通しで全5札の見出しを集めて、
  残るのが「連れていく隊・仲間を鍛える・隊の名簿・行き先を変える」だけであることを見る。
- **全12戦の投影をギルドへ戻した。**前回落としたのは判断の誤りで、投資と設計図を決めるとき
  に先の敵を見たい、という用があった。代わりに遠征の形の帯（`expeditionShapeRail`）を
  撤去した——どの区画でも同じ並びで、区画を選び直しても一つも動かない帯だった。関数と
  `.act-*` の CSS も落とした。
- **連れていく隊の一覧を足した。**issue #236 の共通4軸（`statAxesHtml`）をそのまま使い、
  行の「鍛える」がその人を選んだまま投資の札を開く（`go-train`）。まだ越えていない区画の
  加入者は名前も能力も出さない。判定に `availableCharacterIds` だけを使うと漏れる
  （あれは**次の区画の加入者を既に含む**）ので、区画の踏破も併せて見る。実機幅で
  Stage 2 を未踏破のまま開き、ヒバナが伏せられることを確認した。
- 隊の行の CSS を書いたとき、キャンプの先見機が使っている `.party-row` と名前が衝突して
  行が崩れた（`grid-template-columns: 24px 1fr` を継いでいた）。`.roster-row` へ改名した。

engine・content・保存データは変えていない。

### 3.100 Stage 1以降の敵を部隊化し、最終戦を五人必殺の境界へ上げた（作者要望 2026-09-15）

作者の指摘は「いまは簡単すぎる。3人になるところ以降は、後衛治療・庇う盾・状態異常・
高火力・多段を組み合わせ、最終bossは全員が必殺を使わないと勝てないくらいでよい」だった。

- 灰殻へ抱え手・縫い手・傷印・刻み手を足し、Stage 1第3戦から庇護＋治療、第6戦から
  全体への隙＋三段攻撃を置いた。Stage 2は4体、Stage 3後半は5体の役割編成に組み直した。
- 灰塵・灰織・灰炉の家系出力を上げ、Stage 6〜8のHP帯も再調整した。検査を緩めず、5人Stageの
  難度指数は `1329 → 1519 → 1667 → 2072 → 2664 → 3405 → 4423` の梯子に収めた。
- 最終戦へAP2の灰炉の主心、抱壁、継ぎ手を追加した。固定seedの上限鍛錬・技能Lv10・装備なし
  隊は、五人全員の必殺で6ラウンド勝利（4人生存）。必殺0人と、任意の一人を欠いた4人では
  すべて敗北する。

`analysis/ecology-enemy-tactics-smoke.mjs` は説明文ではなく、敵側の `target_changed`、
`healing_applied`、三人への `status_added`、hitIndex 0/1/2 の `damage_taken` を実戦eventで見る。
敵専用のengine分岐は作らず、`cover_ally` / `mend` / `mark_spread` / `barrage_strike` という
共有語彙だけで成立させた。

### 3.101 人物の表示を、画面をまたいで一つの札に揃えた（作者要望 2026-09-16）

作者の指摘は「キャラクターの表示が統一されていない。戦闘画面と戦闘予測画面はいいが、ギルドの
遠征前ステータス表示と、スキル・装備選択時のステータス表示がイケてない。統一して、スキルや装備は
そこに加えて必要な情報も明示してほしい。見た目も、いまの [拳] や『強打』ではなく、立ち絵を使って
キャラクターがわかる見た目に。顔を見れば分かるので、キャラクター表示にフレーバーテキストは要らない」。

3.32 で立ち絵を画像へ替えたとき、顔を使い始めたのは会話・戦闘・戦闘予測の三つだけだった。ギルドの
「連れていく隊」（2026-09-15）と技能・装備タブの人物帯（issue #235/#236）は、それより前に書いた
`[拳]` の図像＋「強打」＋紹介文の形のまま残っていた。**同じ人物が、画面ごとに三通りの姿で出ていた。**
判断の理由は `docs/DESIGN.md` 5.2.1 に置いた。ここには測った数と、直した経路だけ残す。

- `characterPanel()` を一つ作り、ギルドの隊・技能タブの帯・装備タブの帯の三箇所を全部そこへ寄せた。
  札が持つのは顔・名前・AP/RP・HP・能力3軸の五つで、HP は予測セルと同じ `.forecast-hp-bar` を使う。
  能力の並びから HP を外したのはこのためで、`statAxesHtml(id, { hp: false })` を足した。
- 人物を指す図像（`.avatar`）と紹介文（`option.summary`）を人物表示から全部外した。結果画面の
  行と「出した順番」の帯、技能ツリーの貼りつく帯は札が入らないので、小さな顔
  （`characterFaceChip()`）へ替えた。人物別の余白補正は札・粒・盤面のセルで共有する。
- **画面ごとに足す読み値を入れ替えた。**旧版は技能タブの帯に装備の名前を、装備タブの帯に技能の
  名前を並べていた（隣のタブの中身の写し）。技能タブは技能点（本人・隊）と装着本数・オフの数、
  装備タブは枠の埋まり・耐久の残り・装備が乗せている常時補正にした。常時補正は**これまで画面の
  どこにも無かった数**で、`staticStatBonuses()`（engine と同じ関数）から出す。
- 技能の見出しにあった「技能点 · 隊全体」を落とした。タブの札（`0pt`）・見出し・ツリーの帯で
  三度出ていた数が、札・タブ・ツリーの帯の三つになり、見出しの言い直しが消えた。
- **札の顔は、背に敷かず左の縦長の枠（126px）に立てた。**初版はギルドだけ2列の札にして顔を背に
  敷いたが、実機でヒバナとツグミの頭が切れた（作者指摘 2026-09-16、二度目）。原因は二つある。
  一つは読み値の帯（名前・HP・能力3軸で約95px）が顔の窓を潰すことで、窓へ頭を収めるには札を
  260px 近くまで高くするしかなく、そうしないと目元が名前の行と重なる（DESIGN 5.2 が禁じている
  「名前を顔へ重ねる」そのものになる）。もう一つは CSS の詳細度で、札用の上書きを
  `.side-character-face`（クラス一つ）で書いたため、人物別補正
  `.character-face-watermark[data-character="mender"]`（クラス＋属性）に負けて、札にもセル用の
  持ち上げ（−22%・−30%）が掛かったままだった。**札の形は一つに畳み**（一列・顔は左・読み値は
  右）、上書きは `[data-character]` を付けて詳細度を合わせた。原本の顔が大きいヒバナだけ、札では
  +5% 下げる。5人ぶんを 390×844 の実寸で一枚ずつ確認した。
- 札が「装備 n/枠」を数で言うので、**枠の数を買った数から出すようにした。**`renderEquipment()` は
  枠2つを直接書いていたため、ギルドで「装備枠」（既定2・上限3）を買っても3つ目の枠が画面に出ず、
  装着比の分母（`equipmentFillLabel`）も 2 のままだった。どちらも `slotLimits` を読む。

engine・content・保存データは変えていない。画面スモークは札の組み立て（`characterPanel` /
`characterFaceChip` / 画面ごとの `extras`）と、図像・紹介文・旧い隊の行が戻っていないことを見る。
通しは 390×844 で、隊が札で出ること・札が役どころを出さないこと・札の名前から投資へ飛べることを
踏む。5人ぶんの顔の切り出しは実寸の描画で一枚ずつ確認した。

### 3.102 一戦目で補給を使わせず、踏破記録に戦った盤面を出した（作者指摘 2026-09-17）

実機の試遊から四点。どれも「画面が、起きていることと違うことを言っている」型である。

- **補給チュートリアルの前に、補給を使い切れた。**一戦目（灰の門を巻き戻したあとの一戦）で
  負けるたび、再挑戦が補給を1個取っていた。3個とも使うと、二戦目のあとの補給チュートリアルが
  「集中治療へ1個使う」を要求したまま進まず、そのあいだタブも撤退も閉じているので**詰む**。
  資源を教える前に、その資源を消す経路を置いていたのが誤りである。**第1戦のあいだは補給を
  一つも減らさない**ことにした（再挑戦は何度でも無料、野営治療と引き直しは押せない）。
  補給が初めて動くのは補給チュートリアルの一手で、そこで「3個の取り合い」が意味を持つ。
- **踏破記録が、戦っていない敵を出していた。**巻き戻したあとの勝利はそのまま第1戦の勝利になるのに、
  遠征タブは第1戦の盤面を index から `composeEncounter` で組み直していた。ラウンド数も味方損失も
  灰の門のものなのに、敵だけ「灰の入口」の走者二体という札になっていた（必殺技の一戦も同じ）。
  戦った時点で `run.results` へ印（`script`）を残し、記録はその盤面を組み直して出す。
- **段の添え字（/10）が列の右端で切れていた。**節の計器は一列固定で、列幅（iPhone で 176px）に
  収まらないと `overflow: hidden` が末尾を食っていた。数を削らず、間を詰めたうえで**入らない回
  だけ下の段へ折り返す**。
- **手引きの札の手前へ、技能の操作盤が出ていた。**札（固定帯の下へ貼る）と盤（画面の下端へ貼る）が
  同じ `z-index: 2` で、DOM の順で盤が勝っていた。画面が低い回（iPhone の Safari は 660px ほど
  しか残さない）に札の本文が隠れる。**いま押す場所を言う唯一の文**なので、札を手前（4）へ置いた。

engine と content は変えていない。通しの画面試験に、一戦目の補給の錠・再挑戦の値段・記録の盤面・
計器の折り返し・札と盤の重なりを足した。
