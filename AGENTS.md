# 作業の約束

## この企画が何をしているか

**最終目的は、作者自身が中身を知った後も繰り返し遊びたい、面白いゲームを作ること。**
現在は、その目的へ近づくために仮説検証をしている。関門を通すこと、実験数を増やすこと、
きれいな分析を書くことは手段であって、成功条件ではない。

`agents/PROTOCOL.md` の P は**仮説であって、守るべきルールではない**。
通らないときに疑うのは、まずゲームではなく仮説の側。
条件を満たす版が面白いか（Precision）だけでなく、**満たさない版がつまらないか（Recall）も要る。**
→ [`agents/HYPOTHESIS_TESTING.md`](./agents/HYPOTHESIS_TESTING.md)

## Sol / Terra 分業

戦略・実装・結果整理の分担、トークンを使いすぎない実験手順、結果パケットの書式は
[`docs/SOL_TERRA_WORKFLOW.md`](./docs/SOL_TERRA_WORKFLOW.md) を正とする。

- Sol：戦略、仮説、事前登録、最終判定。**サブエージェントを起動しない**
- Terra：実装、テスト、デプロイ、事実整理。目的との矛盾を先に探し、HOWは改善してよい。仕様や閾値は独自に変えない
- 作者：原則1〜2ランを遊び、可能なら24時間の自発的再プレイを見る

## 判断（機械に置けないので、ここにある）

**作者は 23:30〜07:30（JST）に寝ている。** 止めるとその間は何も進まない。
`TZ=Asia/Tokyo date` で時刻を見る。**手が一択なら聞かずに進める。**
止めてよいのは作者にしか決められないとき（好みの分岐／公開の可否／これまでの合意を変える判断）だけ。

**数字を見てから条件を緩めない。** ただし「測り方が間違っていたので直す」は妥当。

| してよい | してはいけない |
|---|---|
| 登録文と実装の食い違いを、登録文に合わせて直す | 通らないから閾値を動かす |
| 作者の言葉と指標の対応のずれを直す（理由を原文つきで残す） | 適用範囲を後から広げて通す |
| 標本・分解能の不足を直す | 分母をすり替えて数字を良くする |

**関門が満たせないとき、まず「既に良いと分かっているもの」を同じ関門にかける。**
参照点の無い関門は、自分が壊れていることを教えてくれない。
（2026-08-22、これを後回しにして半日を溶かした。RELAY 0.1 を最初にかけていれば済んだ）

**検証を書くときの5規則**（第11回で8つ踏んだ結果。詳細は `DESIGN_LEARNINGS.md` #52）
1. 高価な探索の前に、恒真・恒偽・構造的反例を探す
2. 標本が端（全部／ゼロ）を指したときだけ確かめる
3. 閾値を判定できない標本数なら、数字を出さずに止める
4. 二つの条件を一つの探索に入れない
5. 参照点を先にかける（上と同じ）

Solの文書は**目的・不変条件・委任するHOW**を分ける。Terraは字面どおりの完遂より目的に対する信用できる測定を優先し、矛盾を見つけたら高価な処理の前に反例と修正案を返す。HOWは変えてよいが、仮説、ゲーム仕様、閾値、公開可否は変えない。

## 手順

細部は [`docs/OPERATIONS.md`](./docs/OPERATIONS.md)。通知・自分で再開する方法・公開・記録の取り出し・調律機。

## 機械が見ているので、ここには書かない

| 見るもの | どこ |
|---|---|
| 未コミット・未 push | stop hook |
| **やりかけがあるのに起床を予約していないか** | `.claude/stop-check-unfinished.sh` |
| **作業ツリーが巻き戻っていないか** | `.claude/stop-check-tree.sh` |
| 登録文とコードの閾値が一致しているか | `analysis/smoke-gate.mjs` |
| 下がるだけの法則が無いか／表が正直か | `analysis/smoke-laws.mjs` |
| **規則が変わったのに版が据え置きでないか** | `analysis/smoke-version.mjs` |
| リロードで進行が壊れないか | `analysis/smoke-resume.mjs` |
| **検査が全部通っているか（push ごと）** | `.github/workflows/checks.yml` |

**散文に書いたルールは落ちる。** 瞬間に発火するルールは機械へ移すこと。
**そして、移したら鳴ることを確かめる。** リポジトリ側の hook はこの環境では呼ばれない
（2026-08-22、3つ書いて3つとも黙っていた）。配線の手順は `docs/OPERATIONS.md`。


## 実装担当と人間テスト公開条件

現在の実装・検査担当はGPT-5.6 Terra。Lunaの試用は終了し、今後の担当から外れた。Terraを追加したのではなくLunaから置き換えたものであり、Sol＋実装担当1名＋作者の構成を維持する。サブエージェントや別レビュー担当は追加しない。Solが目的・因果機構・不変条件・受入証拠を定め、実装・探索・テストのHOWはTerraへ委任する。Terraは目的との矛盾を先に検査し、HOWは改善してよいが、仮説・ゲーム仕様・閾値は独自変更しない。

作者へプレイURLを渡す前に、必ず [docs/HUMAN_TEST_RELEASE.md](./docs/HUMAN_TEST_RELEASE.md) を満たす。未確認項目が1件でもあれば、作者をE2E代行に使わず停止する。
